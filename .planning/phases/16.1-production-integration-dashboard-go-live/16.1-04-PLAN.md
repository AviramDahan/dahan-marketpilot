---
phase: 16.1-production-integration-dashboard-go-live
plan: "04"
type: execute
wave: 2
depends_on:
  - "16.1-01"
  - "16.1-02"
  - "16.1-03"
files_modified:
  - render.yaml
  - docs/render_dashboard.md
  - docs/production_scheduler.md
  - docs/configuration.md
  - tests/test_dashboard_render_config.py
requirements:
  - PROD-02
  - PROD-03
  - PROD-04
  - PROD-08
  - PROD-10
  - SAFE-06
  - SAFE-07
must_haves:
  truths:
    - "Secrets are never committed."
    - "Render `sync: false` placeholders may require manual Dashboard entry for existing services."
    - "Render Key Value must be in same region as web and worker services."
research_anchors:
  - "https://render.com/docs/blueprint-spec"
  - "https://render.com/docs/configure-environment-variables"
  - "https://render.com/docs/key-value"
---

# Phase 16.1 Plan 04: Render Blueprint, Secrets, And Runbook

## Objective

Update Render deployment configuration and operator documentation for dashboard, worker, Key Value, secrets, logs, restarts, and rollback.

## Context

@.planning/phases/16.1-production-integration-dashboard-go-live/16.1-CONTEXT.md
@.planning/phases/16.1-production-integration-dashboard-go-live/16.1-RESEARCH.md
@render.yaml
@docs/render_dashboard.md
@docs/production_scheduler.md
@docs/configuration.md

## Tasks

### Task 1: Add Render Key Value and service wiring

Planned files: `render.yaml`, `tests/test_dashboard_render_config.py`

- Add or document the Render Key Value resource and `REDIS_URL`/shared-store env wiring.
- Ensure dashboard web service and scheduler worker reference the shared store.
- Keep secret values as `sync: false`, `generateValue`, or manual Render Dashboard setup as appropriate.
- Preserve pinned runtime versions and existing service boundaries.

### Task 2: Document secret setup and deployment runbook

Planned files: `docs/render_dashboard.md`, `docs/production_scheduler.md`, `docs/configuration.md`

- Document required Render env vars for dashboard, scheduler, QuantConnect, Telegram, and shared storage.
- Document manual steps for Blueprint `sync: false` values and Key Value internal URL.
- Document logs, restart, rollback, and how to verify the local computer is not involved.
- Document external gates that remain pending after 16.1 if Phase 15 or 16.2 evidence is not complete.

## Verification

- `python -m pytest tests/test_dashboard_render_config.py tests/test_ci_workflows.py -q`
- Secret scan over `render.yaml`, docs, `.planning`, scripts, tests, and source.

## Success Criteria

- Render config names the production services and shared storage without committing secrets.
- Operator runbook is clear enough for another AI/operator to deploy and verify without losing context.
- Dashboard and worker deployment boundaries are documented and test-covered.

