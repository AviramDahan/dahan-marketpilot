# Phase 16.1 UAT

Status: deployed go-live evidence captured for Phase 16.1; multi-session
burn-in remains Phase 16.2.

## Local UAT Passed

- Shared Render Key Value adapter supports JSON records, dashboard payload publication, activity records, and scheduler lease locking.
- Production runner builds optional dashboard, Telegram, and shared lock dependencies from environment variables only.
- Dashboard uses `data_source_kind: shared_state` and degrades visibly when no shared production payload exists.
- Dashboard controlled auto-refresh is display-only.
- Render Blueprint defines dashboard web service, scheduler background worker, and shared Key Value service.
- Go-live scripts report missing external evidence as blocked/not-run rather than passed.

## External UAT Evidence

- Render Blueprint was created as `dahan-marketpilot-production`.
- Render services were created for `dahan-marketpilot-dashboard`,
  `dahan-marketpilot-scheduler`, and `dahan-marketpilot-state`.
- Dashboard URL `https://dahan-marketpilot-dashboard.onrender.com` returned HTTP
  200 with the Streamlit shell on 2026-06-17.
- Render Blueprint/package fixes were committed after deployment so both
  dashboard and worker install the local package with `pip install -e .`.
- Dashboard and worker deployed on Render from commit `950a652` on 2026-06-17.
- Worker environment includes `MARKETPILOT_TELEGRAM_ENABLED`,
  `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, QuantConnect credentials, and
  `REDIS_URL` as external Render values or Render-managed service injections.
- Password-authenticated dashboard login was verified in-browser on
  2026-06-17. After login, the dashboard displayed tabs, Refresh/Logout,
  controlled auto-refresh, paper-only warnings, and source/freshness metadata.
- Dashboard shared-state production data was verified in-browser on
  2026-06-17: Overview showed fresh QuantConnect-authoritative portfolio data,
  last sync/poll timestamps, paper-only mode, and no open positions.
- Runtime Telegram delivery through the production dependency path was verified
  with `scripts\telegram_runtime_smoke.py`; result was `status=delivered`,
  `correlation_id=runtime-telegram-smoke-test`, `paper_trading_only=true`.
- Render-hosted dashboard and worker evidence proves the system is not served
  by the local computer. Long-running local-computer-off burn-in remains part
  of Phase 16.2 multi-session UAT.

## External UAT Pending

- Multi-session operational burn-in under real market conditions is Phase 16.2.
- Full signal -> scoring -> risk -> Paper order -> fill -> dashboard ->
  Telegram proof remains Phase 16.2.

## Operator Commands

```powershell
python scripts\verify_render_golive.py --require-dashboard-url --require-shared-state
$env:MARKETPILOT_RUNTIME_TELEGRAM_SMOKE_ENABLED="1"
python scripts\telegram_runtime_smoke.py
```

Phase 16.1 go-live evidence is complete. Do not mark v1.1 complete until
Phase 16.2 burn-in is externally verified.
