---
phase: 09-render-dashboard
plan: "07"
type: execute
wave: 6
depends_on:
  - 09-01
  - 09-02
  - 09-05
  - 09-06
files_modified:
  - dashboard/app.py
  - dashboard/cache.py
  - dashboard/fx_view.py
  - dashboard/data.py
  - config/dashboard.yaml
  - tests/test_dashboard_cache.py
  - tests/test_dashboard_fx.py
  - tests/test_dashboard_render_config.py
  - tests/test_dashboard.py
  - docs/dashboard.md
  - docs/render_dashboard.md
  - docs/safety.md
autonomous: true
requirements:
  - DASH-01
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07
must_haves:
  truths:
    - "DASH-04/D-08/D-09/D-10: Dashboard reads are cached transparently, refresh remains read-only, and stale warnings appear at approximately 10 and 30 minutes."
    - "DASH-05/D-17/D-18/D-19: USD remains source/accounting currency; NIS is display-only with FX rate, source, timestamp, and stale/unavailable labels."
    - "DASH-06/D-14/D-15/D-16: Cache/API/auth/FX/Telegram/Render failures render safe fail-visible states and redact secrets."
    - "DASH-07: Final dashboard tests cover parsing, caching, stale data, authentication, read-only behavior, error presentation, FX display, Render config, and secret masking offline."
    - "DASH-01: Render documentation and tests remain aligned with the final cache/FX/dashboard behavior from 09-06."
  artifacts:
    - path: dashboard/cache.py
      provides: "Pure cache/freshness classification helpers and Streamlit cache integration boundary"
    - path: dashboard/fx_view.py
      provides: "USD/NIS display helpers with FX source/timestamp/staleness behavior"
    - path: tests/test_dashboard_cache.py
      provides: "Offline cache and stale-state coverage"
    - path: tests/test_dashboard_fx.py
      provides: "Offline USD/NIS and missing/stale FX coverage"
    - path: tests/test_dashboard_render_config.py
      provides: "Static Render/start command/env var/cache threshold checks"
    - path: tests/test_dashboard.py
      provides: "Final dashboard safety smoke coverage"
    - path: docs/render_dashboard.md
      provides: "Render operator setup, stale-data, FX, and health-check documentation"
  key_links:
    - from: dashboard/cache.py
      to: dashboard/data.py
      via: "Cached read wrapper preserves source/cache timestamps"
      pattern: "cache.*source_timestamp|freshness"
    - from: dashboard/fx_view.py
      to: marketpilot/fx.py
      via: "Reuse FxSeed-style USD/NIS validation and display-only rules"
      pattern: "FxSeed|USD|NIS"
    - from: dashboard/app.py
      to: dashboard/cache.py
      via: "Manual refresh/cache clearing remains read-only"
      pattern: "refresh|cache"
---

<objective>
Finish Phase 9 with cache/stale behavior, USD/NIS FX display helpers, and final deterministic dashboard test hardening.

Purpose: DASH-04, DASH-05, DASH-06, and DASH-07 require transparent cache/stale handling, display-only NIS conversion, fail-visible errors, and complete offline test coverage. This plan runs after page and Render/dependency work so final hardening can verify the whole dashboard surface without exceeding the checker file threshold.

Output: Cache/stale helpers, FX display helpers, final app/data/config wiring, cache/FX/render/safety tests, and final dashboard/Render/safety documentation updates.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-render-dashboard/09-CONTEXT.md
@.planning/phases/09-render-dashboard/09-RESEARCH.md
@.planning/phases/09-render-dashboard/09-PATTERNS.md
@.planning/phases/09-render-dashboard/09-01-SUMMARY.md
@.planning/phases/09-render-dashboard/09-02-SUMMARY.md
@.planning/phases/09-render-dashboard/09-05-SUMMARY.md
@.planning/phases/09-render-dashboard/09-06-SUMMARY.md
@dashboard/app.py
@dashboard/data.py
@dashboard/models.py
@dashboard/auth.py
@dashboard/config.py
@config/dashboard.yaml
@marketpilot/fx.py
@marketpilot/telegram.py
@docs/dashboard.md
@docs/render_dashboard.md
@docs/configuration.md
@docs/safety.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cache and stale-data behavior</name>
  <files>dashboard/cache.py, dashboard/data.py, dashboard/app.py, config/dashboard.yaml, tests/test_dashboard_cache.py, tests/test_dashboard_render_config.py, docs/dashboard.md, docs/render_dashboard.md</files>
  <behavior>
    - Test 1: Freshness classification treats data under the configured warning threshold as fresh, data at approximately 10 minutes as stale warning, and data at approximately 30 minutes as strong stale/error state per D-09.
    - Test 2: Manual refresh and cache clearing are read-only observability operations and never call QuantConnect mutation endpoints, Telegram send, Paper mode switch, recovery approval, order submit/cancel/modify, or Object Store upload/delete per D-10 and D-21.
    - Test 3: Failed source reads with a last-good display cache render source/cache timestamps and stale/error warnings; failed reads without cache render safe error/not_available states per D-14 and D-15.
    - Test 4: Cache config thresholds are explicit, test-covered, and consistent with Render documentation per D-08 and D-09.
  </behavior>
  <action>Create failing cache/stale tests first, then implement `dashboard/cache.py` as pure stale/freshness helpers with an optional thin Streamlit `st.cache_data` boundary in `dashboard/app.py` or `dashboard/data.py`. Keep cache copies display-only and labeled with source/cache timestamps per D-03 and D-08. Extend config thresholds near 10 minutes and 30 minutes, keeping them explicit and test-covered. Manual refresh may clear/retry display reads only; it must not submit orders, mutate Paper Trading state, trigger Telegram delivery, change QuantConnect state, upload/delete Object Store objects, switch modes, or approve recovery per D-10 and D-21. Update docs with QuantConnect snapshot cadence, refresh semantics, stale thresholds, and Render cold-start/stale expectations.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_cache.py tests/test_dashboard_render_config.py tests/test_dashboard_read_only.py -q</automated>
  </verify>
  <done>Cache/stale behavior is implemented and tested, refresh remains read-only, stale thresholds are explicit, and docs explain source/cache timestamps and degraded states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add FX display helpers and final dashboard test hardening</name>
  <files>dashboard/fx_view.py, dashboard/app.py, config/dashboard.yaml, tests/test_dashboard_fx.py, tests/test_dashboard_cache.py, tests/test_dashboard.py, docs/dashboard.md, docs/render_dashboard.md, docs/safety.md</files>
  <behavior>
    - Test 1: USD values render normally when present; NIS values require an explicit FX rate, source, timestamp, and freshness status per D-17 and D-18.
    - Test 2: Missing or stale FX marks NIS unavailable/stale without inventing a conversion while preserving USD source values per D-19.
    - Test 3: Safe error rendering redacts secret-like values across cache/API/auth/FX/Telegram/Render/system status per D-13 and D-16.
    - Test 4: Final dashboard test set covers API parsing, caching, stale data, authentication, read-only behavior, error presentation, secret masking, FX display, and Render config offline per DASH-07.
  </behavior>
  <action>Add `dashboard/fx_view.py` using `marketpilot.fx.FxSeed`-style validation so USD remains source/accounting currency and NIS is display-only with rate/source/timestamp/staleness labels per D-17 through D-19. Integrate FX display helpers into app/page DTO rendering only where validated FX metadata exists; if FX is missing or stale, render USD normally and mark NIS unavailable/stale. Add final tests across cache, FX, Render config, and the existing dashboard suite. Update docs to state FX unavailable behavior, safe errors, final test coverage, and no mutation controls.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_data_contracts.py tests/test_dashboard_secret_masking.py tests/test_dashboard_auth.py tests/test_dashboard_read_only.py tests/test_dashboard_pages.py tests/test_dashboard_cache.py tests/test_dashboard_fx.py tests/test_dashboard_render_config.py tests/test_dashboard.py -q</automated>
  </verify>
  <done>FX display, safe errors, final dashboard coverage, and Render/cache documentation pass deterministic offline tests without real external services or credentials.</done>
</task>

</tasks>

<source_audit>
Plan-set audit: This plan completes the cache/stale/FX/final-test portion of GOAL Phase 9. It covers REQ DASH-04, DASH-05, DASH-06, and DASH-07, and keeps DASH-01 Render documentation aligned with 09-06. RESEARCH items for Streamlit cache APIs, stale thresholds, QuantConnect snapshot cadence, FX display, safe errors, Render free/cold-start notes, and offline test strategy are covered. CONTEXT decisions D-08 through D-10, D-13 through D-19, and D-20 through D-21 are implemented here; D-22 remains explicitly excluded from the action surface.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QuantConnect read/cache -> dashboard | Cached data could be stale or from a failed last-good read and must be labeled. |
| FX metadata -> portfolio display | Display-only currency conversion could be mistaken for accounting authority. |
| Runtime subsystem errors -> dashboard/docs | Error text can expose secrets or imply unavailable data is healthy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07-01 | Spoofing | dashboard/cache.py | mitigate | Separate source timestamp from cache timestamp and show freshness/authority labels on cached data. |
| T-09-07-02 | Information Integrity | dashboard/fx_view.py | mitigate | USD remains source currency; NIS display requires explicit FX metadata and marks stale/unavailable states. |
| T-09-07-03 | Tampering/Elevation | dashboard/app.py | mitigate | Refresh/cache operations are read-only and tests reject mutation endpoints/actions per D-10/D-21. |
| T-09-07-04 | Information Disclosure | dashboard/app.py and docs/render_dashboard.md | mitigate | Redact secret-like values in safe error rendering and document secret-store-only behavior. |
| T-09-07-05 | Denial of Service | dashboard/cache.py | accept | Short-lived cache reduces repeated external reads; failed reads render last-good display cache or safe errors without mutation. |
| T-09-SC | Tampering | npm/pip/cargo installs | transfer | Dependency legitimacy was handled by blocking checkpoint in 09-06; this plan must not add or change package declarations. |
</threat_model>

<verification>
Run final dashboard checks:

```powershell
python -m pytest tests/test_dashboard_data_contracts.py tests/test_dashboard_secret_masking.py tests/test_dashboard_auth.py tests/test_dashboard_read_only.py tests/test_dashboard_pages.py tests/test_dashboard_cache.py tests/test_dashboard_fx.py tests/test_dashboard_render_config.py tests/test_dashboard.py -q
python -m pytest -q
```

The full suite must remain deterministic offline and must not require QuantConnect, Telegram, Render, internet, broker credentials, market access, or real secrets. If local Python is below the project requirement, record the local result and note that release/runtime verification must use Python >=3.11.
</verification>

<success_criteria>
Plan 09-07 is complete when cache/stale behavior, refresh read-only semantics, FX display, safe errors, and final dashboard coverage pass offline; docs explain stale and FX behavior; and dashboard actions remain limited to view, refresh, login, and logout.
</success_criteria>

<output>
Use atomic commits for each completed task following the execute-plan workflow. Create `.planning/phases/09-render-dashboard/09-07-SUMMARY.md` when done. The summary must include tests run, stale/FX decisions, any Python-version caveat, and final confirmation that dashboard actions remain limited to view, refresh, login, and logout.
</output>
