---
phase: 09-render-dashboard
plan: "08"
type: execute
wave: 7
depends_on: ["09-07"]
gap_closure: true
files_modified:
  - "dashboard/config.py"
  - "dashboard/data.py"
  - "dashboard/app.py"
  - "config/dashboard.yaml"
  - "tests/test_dashboard_runtime_source.py"
  - "tests/test_dashboard_read_only.py"
  - "docs/dashboard.md"
  - "docs/render_dashboard.md"
autonomous: true
requirements: [QC-05, DASH-04, DASH-06, DASH-07]
must_haves:
  truths:
    - "GAP-09-01: Authenticated dashboard runtime no longer hard-codes `DashboardDataClient.not_configured(...)` in `dashboard/app.py`."
    - "QC-05/DASH-04: Runtime dashboard can load a configured QuantConnect-approved Object Store/export snapshot from a safe local/export path or configured read source adapter, while missing setup still returns explicit `not_configured`."
    - "DASH-02/DASH-06: The runtime source loader remains read-only, rejects mutation-like paths, masks errors, and does not print or store secrets."
    - "DASH-07: Offline tests prove configured source loading, missing-source degradation, malformed-source degradation, and app wiring."
  artifacts:
    - path: "tests/test_dashboard_runtime_source.py"
      provides: "Deterministic runtime data-source tests for configured, missing, and malformed dashboard source behavior"
    - path: "dashboard/config.py"
      provides: "Fail-closed dashboard source configuration fields with safe diagnostics"
    - path: "dashboard/data.py"
      provides: "Read-only runtime snapshot loader over approved dashboard export data"
    - path: "dashboard/app.py"
      provides: "Authenticated app wiring to the runtime source loader instead of a hard-coded not_configured snapshot"
    - path: "docs/dashboard.md"
      provides: "Operator-facing dashboard source configuration and degraded-state documentation"
  key_links:
    - from: "dashboard/app.py"
      to: "dashboard/data.py"
      via: "load configured runtime snapshot after authentication"
      pattern: "load_dashboard_snapshot"
    - from: "dashboard/config.py"
      to: "config/dashboard.yaml"
      via: "non-secret source config defaults"
      pattern: "data_source"
    - from: "tests/test_dashboard_runtime_source.py"
      to: "09-UAT.md"
      via: "closes Runtime QuantConnect Data Source gap"
      pattern: "DashboardDataClient.not_configured"
---

<objective>
Close GAP-09-01 by wiring the authenticated Streamlit dashboard to a configured, read-only dashboard data source instead of always rendering a hard-coded `not_configured` snapshot.

Purpose: Phase 9 already has DTOs, page modules, Render config, cache/stale, FX, and secret masking. The missing product behavior is runtime source loading. This plan adds a safe source configuration and loader while preserving the explicit degraded states required when setup is absent.

Output: Runtime dashboard source loader, app wiring, deterministic tests, synchronized docs, and updated Phase 9 verification.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-render-dashboard/09-UAT.md
@.planning/phases/09-render-dashboard/09-VERIFICATION.md
@.planning/phases/09-render-dashboard/09-01-SUMMARY.md
@.planning/phases/09-render-dashboard/09-07-SUMMARY.md
@dashboard/app.py
@dashboard/config.py
@dashboard/data.py
@dashboard/models.py
@dashboard/cache.py
@dashboard/redaction.py
@config/dashboard.yaml
@docs/dashboard.md
@docs/render_dashboard.md
@tests/test_dashboard_data_contracts.py
@tests/test_dashboard_pages.py
@tests/test_dashboard_read_only.py
@tests/test_dashboard_render_config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime dashboard source tests</name>
  <files>tests/test_dashboard_runtime_source.py</files>
  <behavior>
    - Test 1: Default config has no runtime source and `load_dashboard_snapshot(config, now=...)` returns `not_configured` with reason `dashboard_data_source`.
    - Test 2: A configured local/export snapshot path loads a valid dashboard snapshot through existing DTO parsers and produces available portfolio/source metadata.
    - Test 3: Missing configured file returns `not_available` or `not_configured` degraded state with redacted safe errors, not an exception that crashes the dashboard.
    - Test 4: Malformed or fixture-missing payload returns `error` or safe degraded state with no secret leakage.
    - Test 5: `dashboard/app.py` calls the runtime loader and no longer contains `DashboardDataClient.not_configured(missing=("dashboard_data_source",))`.
    - Test 6: Runtime source config rejects remote mutation/write-ish schemes, raw secret-like values, and path traversal outside the approved configured path semantics.
  </behavior>
  <action>Create `tests/test_dashboard_runtime_source.py` first. Keep tests deterministic and offline by using `tmp_path` JSON fixtures, not QuantConnect, Render, Telegram, network access, real credentials, or market data. Prefer existing `yaml.safe_load`, DTO, and fixture patterns. Tests should make the current hard-coded app wiring fail visibly before implementation.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_runtime_source.py -q</automated>
  </verify>
  <done>Tests define the exact gap closure and fail before implementation only because runtime source wiring is missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement safe runtime source config and loader</name>
  <files>dashboard/config.py, dashboard/data.py, dashboard/app.py, config/dashboard.yaml</files>
  <behavior>
    - `DashboardConfig` includes safe non-secret dashboard source settings, defaulting to no source.
    - `load_dashboard_snapshot(config, now=...)` returns explicit `not_configured` when no source is configured.
    - Configured local/export JSON source loads through approved parsers and returns typed `DashboardSnapshot`.
    - Missing or malformed source returns safe degraded states with redacted diagnostics and no dashboard crash.
    - `dashboard/app.py` uses the loader after auth and no longer hard-codes not-configured data.
    - Loader remains read-only and does not add new runtime dependencies or HTTP clients.
  </behavior>
  <action>Add a minimal safe source model to `dashboard/config.py` and `config/dashboard.yaml`. Implement the source loader in `dashboard/data.py` using structured JSON parsing over approved Object Store/export-style payloads and existing DTO builders. Wire `dashboard/app.py` to call the loader after `shell.data_visible` is true. Keep absent setup honest as `not_configured`, preserve cache/source timestamps where available, and mask errors through existing redaction helpers. Do not add order controls, mutation endpoints, POST/deploy hooks, broker access, Telegram sends, new external packages, or real QuantConnect API calls.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_runtime_source.py tests/test_dashboard_data_contracts.py tests/test_dashboard_pages.py tests/test_dashboard_read_only.py tests/test_dashboard_render_config.py -q</automated>
  </verify>
  <done>The authenticated dashboard runtime loads a configured read-only data source when present and degrades safely when absent or invalid.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize docs and verification artifacts</name>
  <files>docs/dashboard.md, docs/render_dashboard.md, .planning/phases/09-render-dashboard/09-UAT.md, .planning/phases/09-render-dashboard/09-VERIFICATION.md</files>
  <action>Update dashboard and Render docs with the runtime source configuration, expected local/export JSON source behavior, missing-source `not_configured` behavior, and secret/value handling. Update `09-UAT.md` and `09-VERIFICATION.md` so GAP-09-01 is marked closed only if implementation tests pass. Keep all docs in English and list secret names only, never values.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_runtime_source.py tests/test_dashboard_render_config.py -q</automated>
    <automated>python -m pytest -q</automated>
  </verify>
  <done>Docs and Phase 9 verification artifacts show the runtime data-source gap as closed with command evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Render dashboard -> configured source path | Runtime reads externally supplied dashboard export data after auth. |
| Config file/env -> dashboard app | Source configuration can influence what file/export data is read. |
| Source payload -> dashboard pages | Parsed JSON can include unsafe diagnostics or malformed data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08-01 | Information Disclosure | Source config and diagnostics | mitigate | Store values outside repository; list names only; redact malformed-source errors and secret-like payload details. |
| T-09-08-02 | Tampering | Runtime source loader | mitigate | Read-only file/export loading only; no mutation endpoints, POSTs, deploy hooks, Object Store writes, or order controls. |
| T-09-08-03 | Spoofing | Fixture/export data | mitigate | Preserve fixture labels for test fixtures and source metadata for configured exports; do not label fixtures as live QuantConnect data. |
| T-09-08-04 | Denial of Service | Malformed source | mitigate | Malformed or missing source returns degraded dashboard state instead of crashing Streamlit. |
| T-09-08-05 | Repudiation | Gap closure evidence | mitigate | Update UAT/VERIFICATION with exact commands and keep not_configured behavior explicit for missing setup. |
</threat_model>

<verification>
- `python -m pytest tests/test_dashboard_runtime_source.py -q`
- `python -m pytest tests/test_dashboard_runtime_source.py tests/test_dashboard_data_contracts.py tests/test_dashboard_pages.py tests/test_dashboard_read_only.py tests/test_dashboard_render_config.py -q`
- `python -m pytest -q`
- `git status --short --branch`
</verification>

<success_criteria>
- GAP-09-01 is closed because authenticated runtime uses a configured source loader rather than a hard-coded not-configured snapshot.
- Missing dashboard source still returns explicit `not_configured` evidence.
- QC-05 and DASH-04 are no longer partial for the runtime dashboard source path.
- Dashboard remains read-only, secret-safe, deterministic offline-testable, and Render-compatible.
</success_criteria>

<output>
Create `.planning/phases/09-render-dashboard/09-08-SUMMARY.md` when done.
</output>
