---
phase: 09-render-dashboard
plan: "03"
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - dashboard/app.py
  - dashboard/pages/__init__.py
  - dashboard/pages/overview.py
  - dashboard/safety_view.py
  - tests/test_dashboard_pages.py
  - tests/test_dashboard_read_only.py
  - docs/dashboard.md
autonomous: true
requirements:
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-06
  - DASH-07
must_haves:
  truths:
    - "DASH-03/D-05: Authenticated users see an Overview-first page registry that names Overview, Positions, Trades, Signals, Backtests, Strategies, Risk, Notifications, Activity, and System Status."
    - "D-07: Overview summarizes paper-only warning, QuantConnect source state, Paper mode, portfolio availability, stale status, open positions count, recent signals/actions, and system warnings."
    - "DASH-04/D-14/D-15: Overview and registry helpers render visible typed empty/stale/error/not_configured states instead of silently hiding missing critical data."
    - "DASH-02/D-20/D-21/D-22: Registry and Overview contain no mutation controls, exports, acknowledgements, Paper mode changes, Telegram sends, or recovery approvals."
  artifacts:
    - path: dashboard/pages/__init__.py
      provides: "Page registry, display order, page metadata, and lazy render dispatch contracts"
    - path: dashboard/pages/overview.py
      provides: "Overview page pure view model/render helpers"
    - path: dashboard/app.py
      provides: "Streamlit shell wiring to page registry after auth"
    - path: dashboard/safety_view.py
      provides: "Shared disclaimer/source-state helpers used by pages"
    - path: tests/test_dashboard_pages.py
      provides: "Deterministic offline coverage for page registry and Overview behavior"
    - path: tests/test_dashboard_read_only.py
      provides: "Static/read-only checks for registry and Overview content"
    - path: docs/dashboard.md
      provides: "Dashboard page inventory, Overview behavior, and page-source policy"
  key_links:
    - from: dashboard/app.py
      to: dashboard/pages/__init__.py
      via: "Page registry consumed by the authenticated Streamlit navigation shell"
      pattern: "PAGE_REGISTRY|render_page"
    - from: dashboard/pages/overview.py
      to: dashboard/models.py
      via: "Typed DTO inputs and pure row/card helpers"
      pattern: "Dashboard.*|SourceMetadata"
    - from: dashboard/pages/overview.py
      to: dashboard/safety_view.py
      via: "Shared disclaimer and degraded-state helpers"
      pattern: "disclaimer|safety"
---

<objective>
Create the dashboard page registry and Overview page foundation over the 09-01 data contracts and 09-02 read-only shell.

Purpose: DASH-03 needs the full page inventory, while D-05 through D-07 require an Overview-first, compact operational entry point. This plan creates the registry and Overview foundation without taking ownership of every page module in one executor context.

Output: Page registry, Overview page helper, app registry wiring, deterministic registry/Overview tests, and dashboard page inventory documentation.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-render-dashboard/09-CONTEXT.md
@.planning/phases/09-render-dashboard/09-RESEARCH.md
@.planning/phases/09-render-dashboard/09-PATTERNS.md
@.planning/phases/09-render-dashboard/09-01-SUMMARY.md
@.planning/phases/09-render-dashboard/09-02-SUMMARY.md
@dashboard/app.py
@dashboard/models.py
@dashboard/data.py
@dashboard/safety_view.py
@docs/dashboard.md
@docs/safety.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add page registry and Overview tests</name>
  <files>tests/test_dashboard_pages.py, tests/test_dashboard_read_only.py</files>
  <behavior>
    - Test 1: The page registry exposes the exact display order: Overview, Positions, Trades, Signals, Backtests, Strategies, Risk, Notifications, Activity, System Status per D-05 and DASH-03.
    - Test 2: Registry metadata marks pages as observational/read-only and exposes no action kind beyond view/navigation/refresh per D-20 through D-22.
    - Test 3: Overview renders the paper-only disclaimer, QuantConnect source status, Paper mode, portfolio status, stale status, open positions count, recent signal/action counts, and system warnings per D-07.
    - Test 4: Overview shows `not_configured`, `not_available`, `stale`, and `error` states visibly and does not omit critical sections per D-14 and D-15.
  </behavior>
  <action>Create failing tests first for registry ordering, Overview behavior, degraded-state visibility, and read-only page metadata. Tests must instantiate typed 09-01 DTOs or local deterministic fixtures only; no Streamlit deployment, QuantConnect, Telegram, Render, internet, broker credentials, market access, or real secrets are allowed. Include negative assertions for order, cancel, modify, Paper mode switch, Telegram send, recovery approval, export, and acknowledge/mark-read wording per D-20 through D-22.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_pages.py tests/test_dashboard_read_only.py -q</automated>
  </verify>
  <done>Tests fail before implementation because page registry and Overview helpers do not yet satisfy DASH-03, D-05, D-07, and read-only page metadata expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement registry, Overview, and app wiring</name>
  <files>dashboard/app.py, dashboard/pages/__init__.py, dashboard/pages/overview.py, dashboard/safety_view.py, tests/test_dashboard_pages.py, tests/test_dashboard_read_only.py, docs/dashboard.md</files>
  <behavior>
    - Test 1: `PAGE_REGISTRY` or equivalent exposes all required page names in the D-05 order without importing missing future page modules during registry inspection.
    - Test 2: `render_page` or equivalent can render Overview from typed DTOs and can represent not-yet-implemented page modules as safe `not_available` states until later plans add them.
    - Test 3: `dashboard/app.py` uses the registry after the 09-02 auth gate and keeps Streamlit code thin.
    - Test 4: Overview and shared helpers contain the paper-only warning path and pass read-only static tests.
  </behavior>
  <action>Add `dashboard/pages/__init__.py` with page metadata, the exact D-05 page order, and lazy dispatch that does not force all page modules to exist before 09-04 and 09-05. Add `dashboard/pages/overview.py` as pure helpers over `dashboard.models` DTOs. Wire `dashboard/app.py` to consume the registry only after authentication from 09-02 succeeds. Overview must be compact and operational per D-06, show the high-priority state from D-07, preserve source/freshness/error context from D-03 and D-14 through D-15, and never calculate or invent portfolio values. Update `docs/dashboard.md` with the registry, Overview responsibilities, and statement that later page modules are implemented by 09-04 and 09-05.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_pages.py tests/test_dashboard_read_only.py tests/test_dashboard_auth.py tests/test_dashboard.py -q</automated>
  </verify>
  <done>Page registry exists, Overview is implemented and tested, app wiring uses the registry after auth, no mutation/export/acknowledgement controls exist, and documentation records the page inventory.</done>
</task>

</tasks>

<source_audit>
Plan-set audit: This plan covers the GOAL slice for dashboard navigation foundation and Overview. It covers REQ DASH-03 page inventory foundation, DASH-02 page read-only boundary, DASH-04 degraded state visibility, DASH-06 safe display expectations through the shared helpers, and DASH-07 registry/Overview tests. RESEARCH items for Streamlit tabs/navigation, thin page modules, fail-visible sections, and deterministic tests are covered. CONTEXT decisions D-05 through D-07, D-14 through D-15, and D-20 through D-22 are implemented here; D-01 through D-04 remain covered by 09-01, D-08 through D-10 and D-17 through D-19 are covered by 09-07, and D-11 through D-13 are covered by 09-02 and 09-06.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated Streamlit shell -> page registry | A selected page name is dispatched into page helper code. |
| Dashboard DTOs -> Overview helper | Degraded external data is transformed into visible overview cards/rows. |
| 09-04/09-05 page entries -> registry | Pages owned by 09-04 and 09-05 must not appear as healthy data before their modules exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Spoofing | dashboard/pages/overview.py | mitigate | Overview displays source/freshness/authority labels from DTO metadata per D-03 and D-07. |
| T-09-03-02 | Tampering/Elevation | dashboard/pages/__init__.py | mitigate | Registry metadata and read-only tests reject action surfaces beyond view/navigation/refresh per D-20/D-21/D-22. |
| T-09-03-03 | Repudiation | dashboard/pages/__init__.py | mitigate | Page entries owned by 09-04 and 09-05 render safe not_available states rather than pretending sections are complete. |
| T-09-03-04 | Information Disclosure | dashboard/safety_view.py | mitigate | Shared helpers render safe degraded-state text only and do not expose secret-bearing diagnostics. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package install or dependency change is planned in 09-03. If dependency changes are needed, stop and use the 09-06 package legitimacy checkpoint. |
</threat_model>

<verification>
Run page registry, read-only, auth, and existing dashboard tests:

```powershell
python -m pytest tests/test_dashboard_pages.py tests/test_dashboard_read_only.py tests/test_dashboard_auth.py tests/test_dashboard.py -q
```

Confirm no test requires QuantConnect, Telegram, Render, internet, broker credentials, market access, real secrets, or Streamlit deployment.
</verification>

<success_criteria>
Plan 09-03 is complete when the page registry and Overview page exist, Overview summarizes the highest-priority operating state, missing future pages are represented safely until implemented, no mutation/export/acknowledgement controls are present, and targeted tests pass offline.
</success_criteria>

<output>
Use atomic commits for each completed task following the execute-plan workflow. Create `.planning/phases/09-render-dashboard/09-03-SUMMARY.md` when done. The summary must list registry behavior, Overview helper behavior, tests run, read-only scan outcomes, and the handoff assumptions for 09-04 and 09-05.
</output>
