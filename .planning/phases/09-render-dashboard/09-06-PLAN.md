---
phase: 09-render-dashboard
plan: "06"
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - render.yaml
  - requirements.txt
  - pyproject.toml
  - .env.example
  - config/dashboard.yaml
  - tests/test_dashboard_render_config.py
  - docs/render_dashboard.md
  - docs/configuration.md
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-06
  - DASH-07
user_setup:
  - service: render
    why: "Host the read-only Streamlit dashboard and store runtime secrets/config outside repository files."
    env_vars:
      - name: DASHBOARD_PASSWORD
        source: "Render environment variable configured by the operator"
      - name: QUANTCONNECT_USER_ID
        source: "Render environment variable copied from approved QuantConnect secret source"
      - name: QUANTCONNECT_API_TOKEN
        source: "Render environment variable copied from approved QuantConnect secret source"
      - name: QUANTCONNECT_PROJECT_ID
        source: "Render environment variable for the Paper Trading project id"
      - name: QUANTCONNECT_LIVE_DEPLOY_ID
        source: "Render environment variable for the Paper deployment id"
      - name: TELEGRAM_BOT_TOKEN
        source: "Optional Render environment variable only if notification health uses configured Telegram status"
      - name: TELEGRAM_CHAT_ID
        source: "Optional Render environment variable only if notification health uses configured Telegram status"
must_haves:
  truths:
    - "DASH-01: Render configuration can run the Streamlit dashboard with a correct start command and Python >=3.11 setting."
    - "DASH-02/D-20/D-21: Render start/config paths expose the read-only dashboard only and do not add deploy hooks that mutate QuantConnect, Telegram, or Paper state."
    - "DASH-06/D-11/D-12/D-13: Render and dashboard config use env var names only and never commit dashboard passwords or service credentials."
    - "DASH-07: Static tests cover Render start command, Python version, env-var secrecy, and approved dependency declarations offline."
  artifacts:
    - path: render.yaml
      provides: "Render web service blueprint for Streamlit start command, Python version, health path, and non-secret env var names"
    - path: requirements.txt
      provides: "Approved Streamlit runtime dependency declaration after package checkpoint"
    - path: pyproject.toml
      provides: "Project dependency metadata aligned with approved Streamlit runtime dependency"
    - path: .env.example
      provides: "Non-secret dashboard/Render env var names for local setup"
    - path: config/dashboard.yaml
      provides: "Non-secret dashboard runtime config with read-only/auth/cache threshold references"
    - path: tests/test_dashboard_render_config.py
      provides: "Static Render/start command/env var/dependency tests"
    - path: docs/render_dashboard.md
      provides: "Render operator setup, env var, secret, and health-check documentation"
  key_links:
    - from: render.yaml
      to: dashboard/app.py
      via: "Start command"
      pattern: "streamlit run dashboard/app.py --server.address=0.0.0.0 --server.port=\\$PORT"
    - from: tests/test_dashboard_render_config.py
      to: requirements.txt
      via: "Runtime dependency declaration for Streamlit after package legitimacy checkpoint"
      pattern: "streamlit"
    - from: config/dashboard.yaml
      to: dashboard/config.py
      via: "Non-secret env var names and read-only invariants"
      pattern: "DASHBOARD_PASSWORD|read_only|paper_trading_only"
---

<objective>
Add Render deployment and dependency configuration for the password-protected read-only Streamlit dashboard, preserving the package-legitimacy checkpoint before dependency edits.

Purpose: DASH-01 requires Render deployability, while DASH-06 requires safe env var and secret handling. This plan is split away from cache/FX hardening so dependency and deployment changes stay below the checker scope threshold and keep the blocking package checkpoint explicit.

Output: Package checkpoint, Render blueprint/start command, conservative Streamlit dependency declaration, env-var examples, Render/static tests, and Render setup documentation.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-render-dashboard/09-CONTEXT.md
@.planning/phases/09-render-dashboard/09-RESEARCH.md
@.planning/phases/09-render-dashboard/09-PATTERNS.md
@.planning/phases/09-render-dashboard/09-02-SUMMARY.md
@dashboard/app.py
@dashboard/auth.py
@dashboard/config.py
@config/dashboard.yaml
@docs/configuration.md
@docs/safety.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify Streamlit package legitimacy before dependency declaration</name>
  <files>requirements.txt, pyproject.toml</files>
  <action>Pause before changing dependency declarations. Present the package legitimacy checkpoint to the user and require explicit approval before adding or pinning Streamlit. Do not modify `requirements.txt`, `pyproject.toml`, or any dependency file until the checkpoint is approved. If the user raises a package concern, resolve it before continuing to Task 2.</action>
  <what-built>No dependency file has been changed yet. This checkpoint exists because `09-RESEARCH.md` flagged `streamlit`, `PyYAML`, and `pytest` as `SUS`, and Render deployment needs a declared Streamlit runtime dependency.</what-built>
  <how-to-verify>
    1. Review the `09-RESEARCH.md` Package Legitimacy Audit.
    2. Verify the selected `streamlit` package/version on https://pypi.org/project/streamlit/ and confirm it is the intended official Streamlit package.
    3. Confirm no new auth package such as `streamlit[auth]`, `Authlib`, Render CLI, HTTP client library, or extra dependency is being added unless separately verified.
    4. Approve continuing with a conservative Streamlit runtime dependency declaration in `requirements.txt` and `pyproject.toml`.
  </how-to-verify>
  <verify>
    <automated>git diff -- requirements.txt pyproject.toml</automated>
  </verify>
  <done>Dependency files remain unchanged until approval; after approval, Task 2 may add only the verified Streamlit runtime dependency and no unverified package.</done>
  <resume-signal>Type "approved" to continue dependency/deployment work, or describe the package concern to resolve before implementation.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Render deployment configuration and dependency/static checks</name>
  <files>render.yaml, requirements.txt, pyproject.toml, .env.example, config/dashboard.yaml, tests/test_dashboard_render_config.py, docs/render_dashboard.md, docs/configuration.md</files>
  <behavior>
    - Test 1: `render.yaml` defines a Render Web Service start command equivalent to `streamlit run dashboard/app.py --server.address=0.0.0.0 --server.port=$PORT`.
    - Test 2: Render config sets Python >=3.11 through `PYTHON_VERSION` or equivalent supported mechanism and does not include secret values.
    - Test 3: Secret-bearing env vars use names/references only; Render Blueprint entries for secrets are not synced with repository values per D-13.
    - Test 4: Runtime dependencies include the approved Streamlit package declaration and do not add unverified packages.
    - Test 5: Docs and `.env.example` list env var names only and never include real secrets.
  </behavior>
  <action>After Task 1 approval, add `render.yaml` for a Python Render Web Service that starts Streamlit on `0.0.0.0` and `$PORT`, declares Python >=3.11, and uses env var names without committed values. Add the approved Streamlit runtime dependency to the project's dependency declarations while keeping existing `PyYAML` and pytest declarations conservative. Do not add `streamlit[auth]`, Authlib, Render CLI, requests/httpx, database clients, cache databases, or any package not separately verified. Update `.env.example`, `config/dashboard.yaml`, `docs/render_dashboard.md`, and `docs/configuration.md` with non-secret env var names, Render setup steps, health behavior, and the read-only dashboard boundary per D-11 through D-13 and D-20 through D-21.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_render_config.py tests/test_dashboard_auth.py tests/test_dashboard_read_only.py -q</automated>
  </verify>
  <done>Render config, dependency declarations, env examples, and docs are present, static tests pass, and no real Render/QuantConnect/Telegram/dashboard secrets are committed.</done>
</task>

</tasks>

<source_audit>
Plan-set audit: This plan covers the Render/dependency portion of GOAL Phase 9 and REQ DASH-01. It also covers DASH-02 through read-only deployment config, DASH-06 through env var/secret handling, and DASH-07 through static Render/dependency tests. RESEARCH items for Render Web Service start command, Render env vars, Render Python version, Streamlit package legitimacy, and no OIDC/auth dependency are covered. CONTEXT decisions D-11 through D-13 and D-20 through D-21 are implemented here; D-22 remains excluded from action surfaces.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Render runtime -> Streamlit app | Public HTTPS traffic reaches dashboard auth and read-only UI. |
| Render env vars -> app config | Runtime secrets/config enter process memory and must not be committed or rendered. |
| Package registry -> dependency declarations | Runtime packages enter the deployment supply chain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Tampering | render.yaml | mitigate | Start Streamlit read-only app only; no deploy hooks or commands that mutate QuantConnect/Telegram/Paper state. |
| T-09-06-02 | Information Disclosure | render.yaml/.env.example/docs | mitigate | Store env var names only; no committed values; docs instruct approved secret stores per D-13. |
| T-09-06-03 | Spoofing | config/dashboard.yaml | mitigate | Config names dashboard password env var and preserves auth gate/read-only invariants per D-11/D-12/D-20. |
| T-09-SC | Tampering | pip dependency changes | mitigate | Blocking human checkpoint verifies SUS package legitimacy before adding or pinning Streamlit; no unverified packages may be added. |
</threat_model>

<verification>
After the package legitimacy checkpoint is approved, run Render/dependency checks:

```powershell
python -m pytest tests/test_dashboard_render_config.py tests/test_dashboard_auth.py tests/test_dashboard_read_only.py -q
```

The checks must not require Render deployment, QuantConnect, Telegram, internet, credentials, market access, or real secrets.
</verification>

<success_criteria>
Plan 09-06 is complete when the package checkpoint is approved, Render can start the password-protected read-only Streamlit dashboard, dependency changes are limited to approved packages, env var examples/docs contain names only, and static tests pass offline.
</success_criteria>

<output>
Use atomic commits for each completed task following the execute-plan workflow. Create `.planning/phases/09-render-dashboard/09-06-SUMMARY.md` when done. The summary must include the package checkpoint outcome, dependency changes, Render configuration, tests run, and confirmation that no secret values or mutation controls were added.
</output>
