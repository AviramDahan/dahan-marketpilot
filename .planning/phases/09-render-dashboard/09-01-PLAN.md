---
phase: 09-render-dashboard
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/models.py
  - dashboard/data.py
  - dashboard/redaction.py
  - tests/test_dashboard_data_contracts.py
  - tests/test_dashboard_secret_masking.py
  - docs/dashboard.md
autonomous: true
requirements:
  - QC-05
  - DASH-04
  - DASH-06
  - DASH-07
user_setup:
  - service: quantconnect
    why: "Production dashboard reads QuantConnect Cloud API/Object Store only; tests use deterministic offline fixtures."
    env_vars:
      - name: QUANTCONNECT_USER_ID
        source: "QuantConnect account/API settings, stored outside repository files"
      - name: QUANTCONNECT_API_TOKEN
        source: "QuantConnect API token, stored only in approved secret stores"
      - name: QUANTCONNECT_PROJECT_ID
        source: "QuantConnect project identifier for the Paper Trading project"
      - name: QUANTCONNECT_LIVE_DEPLOY_ID
        source: "QuantConnect live deployment identifier for the running Paper deployment"
must_haves:
  truths:
    - "QC-05/D-04: QuantConnect Cloud API/Object Store dashboard data paths are documented from verified official sources before implementation code relies on them."
    - "DASH-04/D-01: Dashboard data contracts treat QuantConnect as authority and local fixtures/cache as display-only context."
    - "DASH-06/D-16: Dashboard errors and diagnostics redact secret-like values."
    - "DASH-07/D-03: Offline tests prove parsed dashboard DTOs include source, source timestamp, cache timestamp, freshness status, and authority."
  artifacts:
    - path: dashboard/models.py
      provides: "Frozen dashboard DTOs for source metadata, freshness, portfolio/position/order/signal/backtest/strategy/risk/notification/activity/system sections"
    - path: dashboard/data.py
      provides: "Read-only QuantConnect dashboard data interface, endpoint allowlist, Object Store export contract, fixture parsers, and typed degraded states"
    - path: dashboard/redaction.py
      provides: "Central redaction helpers for secret-like keys and values used by dashboard errors/statuses"
    - path: tests/test_dashboard_data_contracts.py
      provides: "Deterministic offline tests for API/Object Store fixture parsing and metadata authority labels"
    - path: tests/test_dashboard_secret_masking.py
      provides: "Deterministic offline tests for safe error/config/status redaction"
    - path: docs/dashboard.md
      provides: "Dashboard data authority, approved read paths, Object Store export keys, degraded states, and no-fake-data documentation"
  key_links:
    - from: dashboard/data.py
      to: marketpilot/quantconnect_paper.py
      via: "Reuse QuantConnectPaperSnapshot/status concepts"
      pattern: "QuantConnectPaper"
    - from: dashboard/data.py
      to: dashboard/models.py
      via: "Return typed DashboardSnapshot/section DTOs instead of raw external payloads"
      pattern: "Dashboard.*Snapshot|SourceMetadata"
    - from: dashboard/redaction.py
      to: marketpilot/telegram.py
      via: "Mirror to_safe_dict/repr=False and secret redaction conventions"
      pattern: "redact|to_safe_dict"
---

<objective>
Verify and codify the read-only QuantConnect data access/export boundary for Phase 9, then define dashboard data contracts that every later dashboard page can consume.

Purpose: QC-05, DASH-04, DASH-06, and DASH-07 require the dashboard to use verified QuantConnect-approved read/export paths, show degraded states honestly, and mask secrets. This plan creates the source-of-truth contract before app shell, pages, cache, and Render wiring consume it.

Output: Typed dashboard DTOs, read-only data adapter contracts, central redaction helpers, deterministic offline parser/redaction tests, and dashboard data-source documentation.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-render-dashboard/09-CONTEXT.md
@.planning/phases/09-render-dashboard/09-RESEARCH.md
@.planning/phases/09-render-dashboard/09-PATTERNS.md
@.planning/phases/08-quantconnect-paper-trading-and-telegram/08-VERIFICATION.md
@.planning/phases/08-quantconnect-paper-trading-and-telegram/08-01-SUMMARY.md
@.planning/phases/08-quantconnect-paper-trading-and-telegram/08-02-SUMMARY.md
@dashboard/models.py
@dashboard/safety_view.py
@tests/test_dashboard.py
@marketpilot/quantconnect_paper.py
@marketpilot/reconciliation.py
@marketpilot/telegram.py
@marketpilot/notification_events.py
@docs/paper_trading.md
@docs/safety.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock dashboard data contracts with failing tests</name>
  <files>tests/test_dashboard_data_contracts.py, tests/test_dashboard_secret_masking.py</files>
  <behavior>
    - Test 1: A deterministic QuantConnect portfolio fixture parses into a dashboard snapshot whose authoritative source is `quantconnect`, whose fixture label is explicit, and whose metadata includes source timestamp, cache timestamp, freshness status, and authority per D-01 and D-03.
    - Test 2: Missing QuantConnect prerequisites and missing Object Store exports parse into typed `not_configured` or `not_available` section states, not fake holdings, fake backtests, or optimistic healthy statuses per D-02 and D-04.
    - Test 3: Approved read paths are allowlisted for `/live/list`, `/live/portfolio/read`, `/live/orders/read`, `/live/insights/read`, `/live/logs/read`, `/object/list`, `/object/properties`, and `/object/get`; QuantConnect mutation paths are rejected per D-01 and D-21.
    - Test 4: Safe error/status dictionaries redact token, password, credential, api key, user id, account, secret, and chat-like values per D-13 and D-16.
  </behavior>
  <action>Create failing tests first for the dashboard source metadata, read-only endpoint allowlist, fixture parsing, typed degraded states, and redaction behavior. The tests must use only deterministic local fixture payloads and injected values; they must not require QuantConnect, Telegram, Render, internet, broker credentials, market data, or Streamlit runtime. Include negative assertions that fixture payloads cannot be labeled as live state per D-03 and cannot invent portfolio/backtest data when source data is absent per D-02 and D-04.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_data_contracts.py tests/test_dashboard_secret_masking.py -q</automated>
  </verify>
  <done>Tests fail before implementation for missing dashboard DTOs, read-only data contracts, endpoint allowlist enforcement, degraded states, and secret redaction coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typed QuantConnect dashboard DTOs and read-only data boundary</name>
  <files>dashboard/models.py, dashboard/data.py, dashboard/redaction.py, tests/test_dashboard_data_contracts.py, tests/test_dashboard_secret_masking.py</files>
  <behavior>
    - Test 1: Dashboard DTOs are immutable and expose safe `to_safe_dict()` output where secret-like fields can enter diagnostics.
    - Test 2: `DashboardSourceMetadata` or equivalent carries `source`, `source_timestamp`, `cache_timestamp`, `freshness_status`, `authority`, and optional reasons for every section.
    - Test 3: `DashboardDataClient` or equivalent can parse injected QuantConnect-like read payloads and Object Store dashboard JSON without making network calls during tests.
    - Test 4: Mutation endpoint constants or request operations are rejected by allowlist tests.
  </behavior>
  <action>Extend `dashboard/models.py` with frozen dataclasses/enums for source metadata, freshness/status codes, dashboard section errors, overview/portfolio/positions/trades/signals/backtests/strategies/risk/notifications/activity/system DTOs, and an aggregate dashboard snapshot. Add `dashboard/data.py` as a read-only interface around verified QuantConnect Cloud API read paths and Object Store dashboard-export paths from `09-RESEARCH.md`; do not implement QuantConnect mutation paths, order submission, Object Store upload/delete, project update, backtest creation, live commands, or Paper mode changes. Add `dashboard/redaction.py` using the existing Telegram redaction style so any config, auth, API, cache, Telegram, Render, or QuantConnect diagnostic can be rendered safely per D-13 and D-16. Keep production missing-data behavior typed as `not_configured`, `not_available`, `stale`, or `error` rather than fabricated values per D-04, D-14, and D-15.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_data_contracts.py tests/test_dashboard_secret_masking.py tests/test_dashboard.py -q</automated>
  </verify>
  <done>Data contracts are implemented, targeted dashboard tests pass, no dashboard code contains QuantConnect mutation endpoint usage, and missing source data remains fail-visible without fake portfolio/backtest values.</done>
</task>

<task type="auto">
  <name>Task 3: Document the approved dashboard data path and source audit</name>
  <files>docs/dashboard.md, tests/test_dashboard_data_contracts.py</files>
  <action>Create or update `docs/dashboard.md` with the Phase 9 dashboard data authority contract: QuantConnect is authoritative per D-01, Render/cache are display-only per D-02, all sections expose source/cache/freshness/authority metadata per D-03, verified read endpoints and Object Store export keys are the only approved production sources per QC-05 and D-04, and missing/unclear data renders typed degraded states rather than invented values. Document that deterministic fixtures are test-only and must be labeled as fixtures. Do not document real credential values. Add any lightweight documentation assertion needed to keep the approved data-source text present.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_data_contracts.py tests/test_dashboard_secret_masking.py -q</automated>
  </verify>
  <done>`docs/dashboard.md` names the read-only QuantConnect/API/Object Store paths, forbidden mutation paths, fixture labeling rule, degraded states, and secret masking boundary; tests remain deterministic offline.</done>
</task>

</tasks>

<source_audit>
Plan-set audit: GOAL Phase 9 is covered by 09-01 through 09-04. Requirements QC-05 and DASH-04 are covered here; DASH-01 and DASH-02 are covered by 09-02/09-04; DASH-03 is covered by 09-03; DASH-05 is covered by 09-04 and surfaced by 09-03; DASH-06 and DASH-07 are covered by all plans. Research items for QuantConnect read endpoints, Object Store exports, status codes, redaction, deterministic tests, and no dependency changes are covered here. CONTEXT decisions D-01 through D-04 and D-14 through D-16 are implemented here; D-05 through D-13 and D-17 through D-22 are covered by later plans.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QuantConnect API/Object Store -> dashboard parser | Untrusted external JSON enters typed DTO parsing. |
| Environment/config -> diagnostics | Secret-bearing values may enter runtime config or errors and must be redacted before display/logging. |
| Deterministic fixtures -> production UI | Test data must never be represented as live QuantConnect state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | dashboard/data.py | mitigate | Enforce read-only endpoint allowlist and reject QuantConnect mutation paths in tests. |
| T-09-01-02 | Spoofing | dashboard/models.py | mitigate | Require source, source timestamp, cache timestamp, freshness status, and authority labels on every section per D-03. |
| T-09-01-03 | Information Disclosure | dashboard/redaction.py | mitigate | Central redaction helper and safe dict tests for token/password/api key/user/account/credential values per D-13/D-16. |
| T-09-01-04 | Repudiation | docs/dashboard.md | mitigate | Document verified read/export sources and degraded states so operators can distinguish not_configured/not_available/stale/error states. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package install or dependency change is planned in 09-01. If introduced during execution, stop and apply the Package Legitimacy Gate from 09-RESEARCH.md before changing dependencies. |
</threat_model>

<verification>
Run targeted dashboard contract tests and existing dashboard safety tests:

```powershell
python -m pytest tests/test_dashboard_data_contracts.py tests/test_dashboard_secret_masking.py tests/test_dashboard.py -q
```

No test may require QuantConnect, Telegram, Render, internet, broker credentials, market access, or real secrets. Static review must confirm dashboard data code contains no order, cancel, modify, Paper mode switch, Telegram send, recovery approval, QuantConnect mutation, Object Store upload/delete, or live command control paths.
</verification>

<success_criteria>
Plan 09-01 is complete when the dashboard has typed read-only data/source contracts, source metadata and degraded states are test-covered, secret redaction is central and tested, QuantConnect read/export paths are documented from verified research, and all targeted tests pass offline.
</success_criteria>

<output>
Use atomic commits for each completed task following the execute-plan workflow. Create `.planning/phases/09-render-dashboard/09-01-SUMMARY.md` when done. The summary must list task commits, files changed, tests run, source-path decisions, any deviations, and confirmation that no real QuantConnect/Telegram/Render credentials or network access were required.
</output>
