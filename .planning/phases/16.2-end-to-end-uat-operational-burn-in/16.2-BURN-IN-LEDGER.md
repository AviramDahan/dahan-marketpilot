# Phase 16.2 Burn-In Ledger

**Status:** Not run
**Created:** 2026-06-17

This ledger records only sanitized observations. Do not paste raw credentials,
tokens, dashboard passwords, deploy hooks, chat IDs, or API responses containing
secrets.

## Prerequisites

- Phase 15 QuantConnect `/live/orders/read` order/fill authority passed for a
  Paper-only probe on 2026-06-17.
- Phase 16 scheduler implementation is complete.
- Phase 16.1 Render dashboard, scheduler worker, shared state, password auth,
  real dashboard data, and Telegram runtime delivery are verified.

These prerequisites do not automatically pass Phase 16.2 UAT rows. Phase 16.2
requires burn-in evidence captured during this phase.

## Session Evidence

| Session | Status | Window | Evidence | Cleanup |
|---|---|---|---|---|
| Session 1 | not_run | TBD | No observation recorded yet. | N/A |
| Session 2 | not_run | TBD | No observation recorded yet. | N/A |

## Partial Observations

- 2026-06-17T18:24Z: `scripts/phase16_2_observe_deployed_session.py`
  returned dashboard HTTP 200 for
  `https://dahan-marketpilot-dashboard.onrender.com`, but reported
  `REDIS_URL_missing_or_dashboard_payload_absent` and local heartbeat missing.
  This is partial service reachability evidence only; it does not count as a
  valid burn-in session.
- 2026-06-17T18:25Z: Render UI showed `dahan-marketpilot-dashboard`,
  `dahan-marketpilot-scheduler`, and `dahan-marketpilot-state` active. The
  scheduler worker deploy for commit `2eabbcd` was live and logs showed
  `python -m marketpilot.production_runner scheduler` started at 21:19 GMT+3.
  No heartbeat/shared-state content was externally verified yet.
- 2026-06-17T18:31Z: GitHub Actions Dashboard Health workflow
  `27711075662` passed against commit `ae95f29`. This proves read-only public
  dashboard reachability but not authenticated data freshness.
- 2026-06-17T18:33Z: GitHub Actions Heartbeat Monitor workflow
  `27711155724` failed due to missing Python dependency before reaching the
  heartbeat check. Fixed in commit `04f2204`.
- 2026-06-17T18:34Z: GitHub Actions Heartbeat Monitor workflow
  `27711215700` reached the real heartbeat check on commit `04f2204` and
  returned `{"status":"missing","reason":"heartbeat_missing"}`. This proves the
  monitor can detect a missing heartbeat, but it does not prove a fresh deployed
  heartbeat.
- 2026-06-17T18:36Z: Render Key Value service page showed the internal URL
  `[redacted-render-redis-url]` and external traffic blocked. GitHub Actions and
  local scripts cannot read Render shared state directly unless a safe external
  health endpoint or Render-side execution path is added.
- 2026-06-17T18:45Z: The deployed dashboard at
  `https://dahan-marketpilot-dashboard.onrender.com` loaded after
  operator-authorized password authentication. It displayed
  `SIMULATED PAPER TRADING ONLY - NOT FINANCIAL ADVICE`, Paper-only safety mode,
  read-only dashboard shell, source/freshness metadata, active auto-refresh,
  QuantConnect authoritative portfolio source, fresh portfolio status, last sync
  `14:45:00 ET`, last poll `14:45:07 ET`, and `Errors: 0`. The dashboard still
  reported `object_store_export_not_loaded`, so this is dashboard/data-source
  reachability evidence only and does not close heartbeat/shared-state, order
  authority, burn-in, or failure-handling UAT rows.
- 2026-06-17T18:55Z: After commit `ee8416c` deployed live on both Render worker
  and web services, the dashboard System Status tab displayed `System status:
  available` and `scheduler: info heartbeat attempted at
  2026-06-17T18:55:00.787866+00:00`. This proves the deployed worker published
  scheduler heartbeat through shared state and the dashboard loaded it after a
  redeploy. GitHub Actions missed-run monitoring remains pending because it
  still needs an externally readable deployed heartbeat source.
- 2026-06-17T19:36Z: GitHub Actions Heartbeat Monitor workflow run
  `27714718830` passed on commit `3d2ee9a` after reading the deployed read-only
  heartbeat health endpoint. The sanitized output reported `status=ok`,
  `age_seconds=106`, `latest_heartbeat_at=2026-06-17T19:35:00.771762+00:00`,
  `paper_trading_only=true`, `monitor_only=true`, `controls_scheduler=false`,
  `controls_orders=false`, and `worker_state=attempted`. This closes UAT-02
  heartbeat/missed-run monitor evidence without exposing Redis, credentials,
  QuantConnect secrets, Telegram secrets, dashboard passwords, or mutation
  controls.
- 2026-06-17T22:10Z: GitHub Actions Heartbeat Monitor workflow run
  `27722971339` passed on commit `124a696` after the regular market session.
  The deployed heartbeat endpoint truthfully reported `status=stale` and
  `reason=heartbeat_stale`, while the monitor annotated
  `market_window_status=closed` and `heartbeat_required_now=false`. This proves
  the missed-run monitor remains read-only and does not create false failures
  after market close; during market hours the same stale status remains a
  failure.
- 2026-06-17T22:27Z: `scripts/phase16_2_observe_deployed_session.py` read the
  deployed sanitized heartbeat endpoint directly with `--heartbeat-url`. The
  dashboard URL returned HTTP 200 and the heartbeat check reported
  `status=passed`, `reason=heartbeat_stale`, `market_window_status=closed`,
  `heartbeat_required_now=false`, `paper_trading_only=true`,
  `monitor_only=true`, and all scheduler/order/recovery controls false. The
  overall observer result correctly remained `blocked_external_not_verified`
  because the local process did not have Render shared-state access; this does
  not count as a valid burn-in session.
- 2026-06-18T06:04Z: Render SSH access was configured with a dedicated public
  key, then `scripts/phase16_2_resilience_checks.py all --store render
  --namespace marketpilot:v1.1:phase16.2-resilience` ran inside the deployed
  scheduler worker at `/opt/render/project/src` on commit `c76d8e5`. The
  sanitized result was `status=passed`, `store=render`,
  `production_scheduler_lock_touched=false`, and scheduler/order/recovery
  controls all false. Duplicate lock, stale-data sample, fake QuantConnect
  read failure, and fake Telegram rejection checks passed in the isolated
  namespace. This does not count as a market-session burn-in session or a real
  QuantConnect provider outage.

## External Probe Evidence

| Probe | Status | Evidence | Cleanup |
|---|---|---|---|
| Authoritative Paper order trace | not_run | No Phase 16.2 order/fill/rejection trace recorded yet. | N/A |
| Scheduler heartbeat and missed-run monitor | passed | GitHub Actions run `27714718830` passed on commit `3d2ee9a` against the deployed heartbeat health endpoint with sanitized `status=ok` output. GitHub Actions run `27722971339` passed on commit `124a696` after market close with sanitized `status=stale`, `market_window_status=closed`, and `heartbeat_required_now=false`, confirming no off-hours false missed-run failure. | N/A |
| Render restart/redeploy recovery | passed | Render worker and web services deployed commit `ee8416c`; post-redeploy dashboard System Status showed scheduler heartbeat `attempted` at `2026-06-17T18:55:00.787866+00:00`. | N/A |
| Duplicate-run prevention | passed | Render worker SSH run at 2026-06-18T06:04Z used Render Key Value namespace `marketpilot:v1.1:phase16.2-resilience`; first lock acquired, second lock rejected with `lock_already_held`, cleanup succeeded, and `production_scheduler_lock_touched=false`. | Isolated lock released. |
| Stale-data handling | passed | Deployed `/dashboard-state` endpoint returned sanitized `status=stale`, `reason=dashboard_state_stale`, `source=quantconnect`, `authority=authoritative`, `read_only_dashboard=true`, `paper_trading_only=true`, `monitor_only=true`, and all scheduler/order/recovery control flags false at 2026-06-17T22:35Z. The observer kept the session blocked, so this proves stale handling only. | N/A |
| Temporary QuantConnect failure handling | passed | Render worker SSH run at 2026-06-18T06:04Z exercised explicitly labeled fake QuantConnect read failure with `safety_decision=fail_closed`, `orders_attempted=false`, and `cleanup_success=true`. | N/A |
| Telegram delivery failure handling | passed | Render worker SSH run at 2026-06-18T06:04Z exercised controlled fake Telegram rejection with `delivery_status=rejected`, `controls_safety_logic=false`, and `delivery_required_for_safety=false`. | Production Telegram config was not mutated. |

## Prepared Market-Session Collection

- Off-hours prep added a read-only UAT-01 preflight. It checks environment
  presence without printing values, verifies the operator probe is disabled,
  inspects QuantConnect deployment state through read-only API calls, checks
  Telegram configuration without sending, and observes dashboard/shared
  state/heartbeat surfaces.
- Off-hours hardening added a stricter fill-authority gate. UAT-01 cannot pass
  on generic `filled` fields or positive fill quantity alone; it needs sanitized
  `/live/orders/read` metadata with matching correlation/tag/idempotency, order
  id, symbol, fill quantity, timestamp, and Paper-only confirmation.
- Off-hours hardening added a strict all-segment correlation requirement. The
  trace remains blocked if signal/probe, scoring, risk, QC authority, sync,
  dashboard, or Telegram evidence lacks the same non-empty correlation ID.
- Off-hours hardening added reconciliation-clean readiness metadata to the
  sanitized dashboard-state health path and preflight. The market gate requires
  `sync_status=success`, `reconciliation_clean=true`, `source=quantconnect`, a
  timezone-aware source timestamp, and fresh-enough data.
- Off-hours hardening changed open-order readiness from "zero open orders" to
  "no matching/duplicate/leftover/ambiguous probe orders." Legitimate unrelated
  protective or position-management Paper orders do not automatically block the
  preflight.
- Final duplicate-readiness correction allows same-symbol/same-side orders when
  they carry clearly different non-probe metadata. Same symbol/side blocks only
  with matching validation/probe metadata or no usable metadata.
- Off-hours prep added a one-shot market evidence collector. It requires one
  correlation ID and records missing segments as `not_run`; it does not invent
  signal, scoring, risk, order, fill, sync, dashboard, or Telegram evidence.
- UAT-01 requires an authoritative QuantConnect Paper fill. Submitted,
  rejected, or cancelled `/live/orders/read` evidence remains useful partial
  evidence but does not count as UAT-01 pass.
- Operator-probe inputs must be explicit and based on fresh market-session data:
  symbol, entry, stop, and target. Hardcoded defaults are no longer accepted by
  the production dependency builder.
- After any market-session attempt, restore
  `MARKETPILOT_RUNTIME_INPUT_KIND=none` and
  `MARKETPILOT_OPERATOR_PAPER_PROBE_ENABLED=false`, then confirm no unintended
  matching or ambiguous duplicate Paper orders remain.
