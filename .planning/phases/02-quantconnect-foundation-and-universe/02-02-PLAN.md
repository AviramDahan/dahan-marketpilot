---
phase: 02-quantconnect-foundation-and-universe
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - config/universe.yaml
  - marketpilot/universe.py
  - marketpilot/data_quality.py
  - marketpilot/qc_contracts.py
  - tests/test_universe.py
  - tests/test_data_quality.py
  - docs/universe.md
  - docs/configuration.md
  - docs/testing.md
autonomous: true
requirements:
  - QC-02
  - QC-03
  - UNI-01
  - UNI-02
  - UNI-03
  - UNI-04
must_haves:
  truths:
    - "D-05: Phase 2 builds full universe and data-quality foundation without strategy signals or scoring."
    - "D-06: Required universe criteria reject invalid, stale, missing, or insufficient data."
    - "D-07: The system never silently falls back to a tiny or hand-written list."
    - "D-14: Universe and data-quality contracts are documented in the same phase."
    - "D-15: Docs state which universe behavior is implemented locally and which QuantConnect APIs remain externally verified."
    - "Universe logic remains long-only US common equity oriented and source-of-truth aligned with QuantConnect."
  artifacts:
    - config/universe.yaml
    - marketpilot/universe.py
    - marketpilot/data_quality.py
    - marketpilot/qc_contracts.py
    - tests/test_universe.py
    - tests/test_data_quality.py
    - docs/universe.md
  key_links:
    - "marketpilot/universe.py consumes strict rules from config/universe.yaml and returns accepted/rejected records with reasons."
    - "marketpilot/qc_contracts.py records verified QuantConnect API names from docs/quantconnect_verification.md without requiring QuantConnect imports in local tests."
---

<objective>
Implement the dynamic universe and data-quality contract foundation.

Purpose: Later indicators and strategies need a strict, auditable universe boundary before any signal can be evaluated.
Output: Universe configuration, data-quality models, offline filters/tests, QuantConnect API contract notes, and synchronized docs.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-quantconnect-foundation-and-universe/02-CONTEXT.md
@.planning/phases/02-quantconnect-foundation-and-universe/02-RESEARCH.md
@.planning/phases/02-quantconnect-foundation-and-universe/02-01-SUMMARY.md
@docs/quantconnect_verification.md
@docs/configuration.md
@docs/testing.md
@marketpilot/safety.py
@marketpilot/configuration.py
@marketpilot/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add universe configuration and data-quality models</name>
  <files>config/universe.yaml, marketpilot/data_quality.py, docs/configuration.md</files>
  <read_first>.planning/phases/02-quantconnect-foundation-and-universe/02-CONTEXT.md, docs/configuration.md, marketpilot/safety.py</read_first>
  <action>Create `config/universe.yaml` with strict Phase 2 thresholds: `min_price_usd: 5`, `min_history_bars: 250`, `min_average_volume_20: 500000`, `min_average_dollar_volume_20: 20000000`, configurable `min_market_cap_usd`, common-equity-only mode, and exclusions for ETF, ADR, OTC, preferred shares, warrants, stale data, critical missing data, and unsupported securities. Create `marketpilot/data_quality.py` with sanitized dataclasses/enums for `DataQualityStatus`, `DataQualityIssue`, `UniverseCandidate`, `UniverseDecision`, and `UniverseSnapshot`. Update docs to describe the config keys.</action>
  <verify>python -m pytest tests/test_data_quality.py</verify>
  <acceptance_criteria>
    - `config/universe.yaml` contains every threshold and exclusion named above.
    - Data-quality issues include reason codes but do not expose secrets.
    - No strategy setup, signal, score, order, or portfolio state is introduced.
  </acceptance_criteria>
  <done>Universe configuration and data-quality vocabulary exist with strict safety boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Implement strict universe filtering and audit records</name>
  <files>marketpilot/universe.py, tests/test_universe.py, tests/test_data_quality.py</files>
  <read_first>config/universe.yaml, marketpilot/data_quality.py, docs/quantconnect_verification.md</read_first>
  <action>Implement pure-Python universe evaluation that accepts `UniverseCandidate` records and produces `UniverseSnapshot` with accepted symbols, rejected symbols, counts, additions/removals relative to a previous accepted set, exclusion reasons, sector distribution where sector is provided, update timestamp, and data-quality status. Reject candidates that fail required thresholds or exclusions. Do not use a hard-coded ticker list except test fixtures.</action>
  <verify>python -m pytest tests/test_universe.py tests/test_data_quality.py</verify>
  <acceptance_criteria>
    - Tests cover acceptance, price rejection, history rejection, volume/dollar-volume rejection, stale-data rejection, ETF/ADR/OTC/preferred/warrant exclusions, and critical missing data rejection.
    - Tests verify counts, additions, removals, reasons, and sector distribution.
    - Tests fail if the implementation silently truncates to a tiny list without recording the reason.
  </acceptance_criteria>
  <done>Universe selection produces auditable accepted/rejected records offline.</done>
</task>

<task type="auto">
  <name>Task 3: Document QuantConnect universe integration boundaries</name>
  <files>marketpilot/qc_contracts.py, docs/universe.md, docs/testing.md</files>
  <read_first>docs/quantconnect_verification.md, .planning/phases/02-quantconnect-foundation-and-universe/02-RESEARCH.md, marketpilot/universe.py</read_first>
  <action>Create `marketpilot/qc_contracts.py` containing constants or dataclasses that document verified current QuantConnect API names and forbidden legacy/deployment surfaces for this phase, such as `add_universe`, `Fundamental`, `add_equity`, `history`, and manual indicator cleanup. Create `docs/universe.md` explaining the intended current QuantConnect fundamental universe integration, strict rejection behavior, offline fixture tests, and deferred strategy/scoring boundaries. Update testing docs to list universe/data-quality tests.</action>
  <verify>Select-String -Path docs/universe.md -Pattern "add_universe", "Fundamental", "strict", "rejection", "no signals"; python -m pytest tests/test_universe.py tests/test_data_quality.py</verify>
  <acceptance_criteria>
    - Docs mention the current fundamental universe API and legacy API caution from research.
    - Docs state no strategy signals, scoring, orders, or Paper deployment are introduced.
    - `qc_contracts.py` does not import QuantConnect runtime modules for local tests.
  </acceptance_criteria>
  <done>Universe implementation boundaries are documented for later LEAN integration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_universe.py tests/test_data_quality.py` passes.
- [ ] `Select-String -Path docs/universe.md -Pattern "add_universe", "strict", "deferred", "strategy"` passes.
- [ ] No strategy setup, scoring, order, Paper deployment, fake universe result, or credential artifact is created.
</verification>

<success_criteria>

- Dynamic universe/data-quality foundation exists and is tested offline.
- Strict rejection and audit records cover Phase 2 requirements.
- QuantConnect universe API assumptions are documented but not faked.
- Future strategies can consume accepted universe records only after readiness checks.

</success_criteria>

<output>
After completion, create `.planning/phases/02-quantconnect-foundation-and-universe/02-02-SUMMARY.md`.
</output>
