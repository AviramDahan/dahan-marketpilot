---
phase: 02-quantconnect-foundation-and-universe
plan: "04"
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - config/regime.yaml
  - marketpilot/regime.py
  - tests/test_regime.py
  - docs/market_regime.md
  - docs/testing.md
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - QC-03
must_haves:
  truths:
    - "D-11: SPY/QQQ market regime is a future new-entry gate only and never liquidation or exit override logic."
    - "D-12: RISK_ON, NEUTRAL, RISK_OFF thresholds and transition behavior are configurable, documented, and unit-tested."
    - "D-13: No Telegram regime alerts are sent or implemented in Phase 2."
    - "D-14: Market regime contract and deferred notification boundaries are documented in the same phase."
    - "D-15: Docs state what was verified, what was not run, and which future phases consume regime state."
    - "QuantConnect remains authoritative for future paper/backtest state; regime tests use offline fixtures only."
  artifacts:
    - config/regime.yaml
    - marketpilot/regime.py
    - tests/test_regime.py
    - docs/market_regime.md
  key_links:
    - "marketpilot/regime.py consumes benchmark readiness concepts from marketpilot/symbol_data.py or marketpilot/indicators.py without creating orders."
    - "docs/market_regime.md links REG-03 to the explicit no-liquidation/no-exit-override behavior."
---

<objective>
Implement SPY/QQQ market regime foundation as a future entry gate only.

Purpose: Later setup phases need a tested market-regime state without allowing regime changes to liquidate or override exits.
Output: Regime configuration, pure regime classifier, tests, and documentation.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-quantconnect-foundation-and-universe/02-CONTEXT.md
@.planning/phases/02-quantconnect-foundation-and-universe/02-RESEARCH.md
@.planning/phases/02-quantconnect-foundation-and-universe/02-03-SUMMARY.md
@marketpilot/indicators.py
@marketpilot/symbol_data.py
@docs/indicators.md
@docs/safety.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add configurable market regime thresholds</name>
  <files>config/regime.yaml, docs/configuration.md</files>
  <read_first>.planning/phases/02-quantconnect-foundation-and-universe/02-CONTEXT.md, docs/configuration.md</read_first>
  <action>Create `config/regime.yaml` with benchmark symbols `SPY` and `QQQ`, required EMA20/EMA50/EMA200, slope windows, 20-day and 60-day returns, RISK_ON/RISK_OFF/NEUTRAL threshold fields, and explicit `entry_gate_only: true`, `liquidate_on_risk_off: false`, `override_exits: false`. Update configuration docs to describe these keys.</action>
  <verify>Select-String -Path config/regime.yaml -Pattern "SPY", "QQQ", "entry_gate_only: true", "liquidate_on_risk_off: false", "override_exits: false"</verify>
  <acceptance_criteria>
    - Regime config includes SPY/QQQ and required threshold keys.
    - Config explicitly disables liquidation and exit override behavior.
    - No order or Telegram configuration is added by this task.
  </acceptance_criteria>
  <done>Market regime configuration is explicit and safety-bounded.</done>
</task>

<task type="auto">
  <name>Task 2: Implement offline regime classifier and transition behavior</name>
  <files>marketpilot/regime.py, tests/test_regime.py</files>
  <read_first>config/regime.yaml, marketpilot/indicators.py, marketpilot/symbol_data.py, docs/safety.md</read_first>
  <action>Create `marketpilot/regime.py` with `MarketRegime` enum values `RISK_ON`, `NEUTRAL`, `RISK_OFF`, benchmark snapshot dataclasses, classification function, transition detection, unchanged-state suppression, and a future-entry permission result. Classification should use SPY/QQQ EMA200/EMA50/EMA20, slopes, 20-day return, and 60-day return inputs from offline fixture snapshots. It must not submit orders, liquidate, override exits, or send Telegram alerts.</action>
  <verify>python -m pytest tests/test_regime.py</verify>
  <acceptance_criteria>
    - Tests cover RISK_ON, NEUTRAL, RISK_OFF, transition detection, unchanged-state suppression, and unready/missing benchmark rejection.
    - Tests prove RISK_OFF blocks future new entries only.
    - Tests fail if liquidation, order, or Telegram send behavior appears.
  </acceptance_criteria>
  <done>Regime classifier works with offline fixtures and remains entry-gate only.</done>
</task>

<task type="auto">
  <name>Task 3: Document regime boundaries and verification</name>
  <files>docs/market_regime.md, docs/testing.md</files>
  <read_first>marketpilot/regime.py, tests/test_regime.py, config/regime.yaml, .planning/REQUIREMENTS.md</read_first>
  <action>Create `docs/market_regime.md` explaining SPY/QQQ inputs, thresholds, RISK_ON/NEUTRAL/RISK_OFF behavior, entry-gate-only semantics, no forced liquidation, no exit override, no Telegram delivery in Phase 2, and future consumers in strategy and notification phases. Update testing docs to list `tests/test_regime.py`.</action>
  <verify>Select-String -Path docs/market_regime.md -Pattern "RISK_ON", "NEUTRAL", "RISK_OFF", "entry gate", "no liquidation", "no Telegram"; Select-String -Path docs/testing.md -Pattern "test_regime.py"</verify>
  <acceptance_criteria>
    - Docs explicitly cover REG-03 no-liquidation behavior.
    - Docs state Telegram regime alerts are deferred.
    - Docs distinguish regime state from strategy signals and orders.
  </acceptance_criteria>
  <done>Market regime behavior and boundaries are documented for later phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_regime.py` passes.
- [ ] `python -m pytest` passes for all Phase 2 tests.
- [ ] `Select-String -Path marketpilot/regime.py, docs/market_regime.md -Pattern "Liquidate", "MarketOrder", "SetHoldings", "send_telegram"` returns no implementation of forbidden behavior.
</verification>

<success_criteria>

- SPY/QQQ market regime foundation exists and is tested.
- Regime thresholds are configurable and documented.
- RISK_OFF can block future new entries only.
- No liquidation, exit override, Telegram delivery, or order behavior is introduced.

</success_criteria>

<output>
After completion, create `.planning/phases/02-quantconnect-foundation-and-universe/02-04-SUMMARY.md`.
</output>
