---
phase: 02-quantconnect-foundation-and-universe
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/quantconnect_verification.md
  - docs/setup.md
  - docs/testing.md
  - docs/safety.md
  - lean/README.md
  - tests/test_quantconnect_verification_docs.py
  - tests/test_lean_static_safety.py
autonomous: true
requirements:
  - QC-02
  - QC-03
  - QC-04
user_setup:
  - "Optional external LEAN verification may require Docker, LEAN CLI, lean login, lean init, and QuantConnect organization access. Do not store credentials in the repository or chat."
must_haves:
  truths:
    - "D-01: Official QuantConnect/LEAN documentation and current API names are verified before later Phase 2 code depends on them."
    - "D-02: Automated verification remains offline-first and deterministic where possible."
    - "D-03: Missing LEAN CLI, Docker, or login is recorded as an external gate, not treated as success."
    - "D-04: Cloud API workflows are verified and documented only; no credentials, cloud backtest, or Paper deployment are required."
    - "D-14: New QuantConnect verification contracts are documented in the same phase."
    - "D-15: Docs distinguish verified sources, unrun checks, and user setup requirements."
    - "Live algorithm deployment and brokerage settings remain out of scope for Phase 2."
  artifacts:
    - docs/quantconnect_verification.md
    - lean/README.md
    - tests/test_quantconnect_verification_docs.py
  key_links:
    - "docs/quantconnect_verification.md links Phase 2 implementation to official QuantConnect docs and explains which commands are verification-only."
    - "tests/test_quantconnect_verification_docs.py protects the no-credentials and no-cloud-run boundaries."
---

<objective>
Create the Phase 2 QuantConnect/LEAN verification gate and documentation contract.

Purpose: Phase 2 must not build universe, indicator, or regime code on unverified QuantConnect API assumptions.
Output: Official-source verification docs, LEAN setup notes, and tests that enforce the verification/no-credentials/no-deployment boundary.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-quantconnect-foundation-and-universe/02-CONTEXT.md
@.planning/phases/02-quantconnect-foundation-and-universe/02-RESEARCH.md
@.planning/phases/01-foundation-and-safety/01-VERIFICATION.md
@docs/setup.md
@docs/testing.md
@docs/safety.md
@lean/main.py
@tests/test_lean_static_safety.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document verified QuantConnect and LEAN source contracts</name>
  <files>docs/quantconnect_verification.md, lean/README.md</files>
  <read_first>.planning/phases/02-quantconnect-foundation-and-universe/02-CONTEXT.md, .planning/phases/02-quantconnect-foundation-and-universe/02-RESEARCH.md, docs/setup.md, docs/testing.md, lean/main.py</read_first>
  <action>Create `docs/quantconnect_verification.md` that records the official QuantConnect/LEAN docs checked in research, the Phase 2 allowed API surface (`add_universe`, `add_equity`, `history`, indicator readiness, manual indicators/consolidator cleanup for dynamic universes, `lean build`, Cloud API authentication and compile endpoints), and the forbidden surface (live deployment, brokerage settings, cloud backtest execution, Paper deployment, credentials). Create `lean/README.md` explaining the current minimal shell, optional `lean build` gate, and that missing LEAN CLI/Docker/login must be reported as not run.</action>
  <verify>Select-String -Path docs/quantconnect_verification.md -Pattern "add_universe", "lean build", "Cloud API", "credentials", "not run"; Select-String -Path lean/README.md -Pattern "lean build", "no order", "not run"</verify>
  <acceptance_criteria>
    - `docs/quantconnect_verification.md` cites official source URLs from `02-RESEARCH.md`.
    - The docs explicitly say no credentials, cloud backtest, Paper deployment, or live deployment are required in Phase 2.
    - `lean/README.md` documents optional external LEAN verification and no fake success.
  </acceptance_criteria>
  <done>Phase 2 has a clear QuantConnect verification gate for downstream implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize setup, testing, and safety docs</name>
  <files>docs/setup.md, docs/testing.md, docs/safety.md</files>
  <read_first>docs/setup.md, docs/testing.md, docs/safety.md, docs/quantconnect_verification.md</read_first>
  <action>Update existing docs to reference `docs/quantconnect_verification.md`. `docs/setup.md` must keep LEAN CLI/Docker/login as external user setup. `docs/testing.md` must distinguish offline pytest checks from optional LEAN compile. `docs/safety.md` must reiterate that Cloud API verification does not authorize credentials, broker settings, Paper deployment, or live deployment.</action>
  <verify>Select-String -Path docs/setup.md, docs/testing.md, docs/safety.md -Pattern "quantconnect_verification.md", "LEAN", "credentials"</verify>
  <acceptance_criteria>
    - Existing docs link to the new verification doc.
    - Docs do not ask the user to paste credentials into chat or files.
    - Docs distinguish executed checks from external checks.
  </acceptance_criteria>
  <done>Repository docs are synchronized with the Phase 2 verification gate.</done>
</task>

<task type="auto">
  <name>Task 3: Add static tests for verification documentation and LEAN safety</name>
  <files>tests/test_quantconnect_verification_docs.py, tests/test_lean_static_safety.py</files>
  <read_first>docs/quantconnect_verification.md, lean/README.md, tests/test_lean_static_safety.py</read_first>
  <action>Add tests that verify the QuantConnect verification docs contain official source links, required no-credential/no-cloud-execution statements, `lean build` as optional external verification, and no claim that LEAN compile was run. Extend LEAN static safety tests if needed to keep `lean/main.py` free of order/live deployment APIs.</action>
  <verify>python -m pytest tests/test_quantconnect_verification_docs.py tests/test_lean_static_safety.py</verify>
  <acceptance_criteria>
    - Tests fail if docs imply credentials are required in the repository.
    - Tests fail if docs claim an unexecuted LEAN compile succeeded.
    - Tests continue to fail on forbidden order/live API calls in `lean/main.py`.
  </acceptance_criteria>
  <done>Verification docs and LEAN safety boundaries are protected by tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_quantconnect_verification_docs.py tests/test_lean_static_safety.py` passes.
- [ ] `Select-String -Path docs/quantconnect_verification.md -Pattern "add_universe", "history", "indicator readiness", "lean build", "Cloud API"` passes.
- [ ] No credentials, cloud backtest result, Paper Trading result, or live deployment artifact is created.
</verification>

<success_criteria>

- Official QuantConnect/LEAN source contracts are documented.
- External LEAN and Cloud API verification gates are explicit.
- No secret-bearing or live-deployment path is introduced.
- Docs and tests distinguish executed checks from unexecuted external checks.

</success_criteria>

<output>
After completion, create `.planning/phases/02-quantconnect-foundation-and-universe/02-01-SUMMARY.md`.
</output>
