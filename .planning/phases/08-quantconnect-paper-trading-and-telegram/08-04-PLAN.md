---
phase: 08-quantconnect-paper-trading-and-telegram
plan: "04"
type: execute
wave: 4
depends_on:
  - "08-03"
files_modified:
  - marketpilot/notification_events.py
  - marketpilot/telegram.py
  - tests/test_telegram_alert_coverage.py
  - tests/test_regime_telegram_alerts.py
  - tests/test_daily_summary_notifications.py
  - tests/test_telegram_failure_isolation.py
  - tests/test_notification_dedup_rate_limit.py
  - docs/notification_events.md
  - docs/telegram.md
  - docs/safety.md
autonomous: true
requirements:
  - REG-04
  - TEL-03
  - TEL-04
  - TEL-06
must_haves:
  truths:
    - "D-21 and TEL-03: configured BUY candidate, WATCH, Paper BUY, Paper SELL, submitted-order, partial-fill, full-fill, stop, target, partial-close, full-close, rejected-order, canceled-order, regime-change, system, error, start/restart, and daily-summary alerts are covered."
    - "D-22 and REG-04: regime alerts emit only on actual regime transitions and duplicate unchanged-state alerts are suppressed."
    - "D-23 and TEL-03: daily summaries are modeled as scheduled or end-of-day notification artifacts with active Paper mode, new signals, entries, exits, open positions, rejected actions, and system warnings."
    - "D-24 and TEL-06: historical backtests remain real-Telegram-disabled by default, and Phase 7 backtest preview events remain fake-collector-only unless an operator explicitly requests a separate preview pathway outside normal backtest execution."
    - "D-18 and TEL-04: disabled notifications, missing token, missing chat ID, quota/rate limiting, Telegram rejection, and network/API failure stay typed and non-authoritative across every alert family."
    - "D-19 and TEL-04: duplicate suppression uses stable event type plus correlation ID keys, rate limiting is conservative and local, and duplicate/rate-limit behavior is tested without real Telegram calls."
  artifacts:
    - path: "marketpilot/notification_events.py"
      provides: "Expanded alert taxonomy and factories for Phase 8 Telegram coverage, regime transitions, system/error/start/restart, and daily summaries"
      exports: ["NotificationEventType", "NotificationDomainEvent", "event_for_regime_transition", "event_for_daily_summary"]
    - path: "tests/test_telegram_alert_coverage.py"
      provides: "TEL-03 full alert family coverage tests"
    - path: "tests/test_regime_telegram_alerts.py"
      provides: "REG-04 transition-only regime alert and unchanged-state suppression tests"
    - path: "tests/test_daily_summary_notifications.py"
      provides: "D-23 daily summary artifact tests"
    - path: "tests/test_telegram_failure_isolation.py"
      provides: "TEL-04 and TEL-06 failure, disabled, quota, duplicate, and rate-limit behavior tests"
  key_links:
    - from: "marketpilot/notification_events.py"
      to: "marketpilot/telegram.py"
      via: "All alert events format through the Telegram adapter without changing trading control flow"
      pattern: "NotificationDomainEvent|TelegramDeliveryResult"
    - from: "marketpilot/notification_events.py"
      to: "marketpilot/regime.py"
      via: "Regime transition alerts are generated only when previous and current regime states differ"
      pattern: "regime|transition"
    - from: "tests/test_telegram_alert_coverage.py"
      to: "docs/notification_events.md"
      via: "Docs enumerate all required alert families and safety behavior"
      pattern: "BUY candidate|WATCH|daily summary|regime"
---

<objective>
Complete Telegram alert coverage, regime transition suppression, daily summary
events, duplicate suppression, rate limiting, and failure-isolation tests.

Purpose: satisfy REG-04, TEL-03, TEL-04, and TEL-06 across the complete Phase 8
alert taxonomy while keeping real Telegram disabled in normal historical
backtests.

Output: expanded `marketpilot/notification_events.py`, Telegram formatting and
delivery tests, alert coverage tests, regime transition tests, daily summary
tests, failure-isolation tests, and synchronized docs. This plan implements
D-21 through D-24 and re-verifies D-18 and D-19 across all alert families.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-quantconnect-paper-trading-and-telegram/08-CONTEXT.md
@.planning/phases/08-quantconnect-paper-trading-and-telegram/08-RESEARCH.md
@docs/safety.md
@docs/notification_events.md
@docs/activation_gates.md
@marketpilot/notification_events.py
@marketpilot/telegram.py
@marketpilot/regime.py
@marketpilot/order_lifecycle.py
@marketpilot/exits.py
@marketpilot/reconciliation.py
</context>

<interfaces>
- Alert coverage remains transport-neutral in `NotificationDomainEvent`; real
  Telegram delivery is handled only by `marketpilot.telegram`.
- Regime transition helpers should accept previous and current regime states and
  return no event for unchanged state.
- Daily summary helpers produce scheduled/end-of-day artifacts as sanitized
  domain events, not fake portfolio authority.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand alert taxonomy and coverage tests</name>
  <files>marketpilot/notification_events.py, tests/test_telegram_alert_coverage.py, docs/notification_events.md</files>
  <behavior>
    - Test 1: D-21 every required alert family has a stable event type or factory: BUY candidate, WATCH, Paper BUY, Paper SELL, submitted-order, partial-fill, full-fill, stop, target, partial-close, full-close, rejected-order, canceled-order, regime-change, system, error, start/restart, and daily-summary.
    - Test 2: TEL-03 alert payloads are sanitized and include enough context for Telegram formatting without containing secrets or profitability guarantees.
    - Test 3: existing Phase 6 event types remain backward-compatible.
  </behavior>
  <action>Create tests first. Extend `NotificationEventType` and factory helpers to cover all D-21 alert families while preserving existing Phase 6 event names and fake collector behavior. Keep factories transport-neutral and sanitized. Include paper mode, activation state, order/fill state, stop/target context, and system severity where relevant, but never include secrets, credentials, fake performance, or profitability claims. Update `docs/notification_events.md` with the complete taxonomy.</action>
  <verify>
    <automated>python -m pytest tests/test_telegram_alert_coverage.py tests/test_notification_events.py -q</automated>
  </verify>
  <done>All TEL-03 alert families have stable transport-neutral event coverage without breaking Phase 6 notification contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regime transition and daily summary events</name>
  <files>marketpilot/notification_events.py, tests/test_regime_telegram_alerts.py, tests/test_daily_summary_notifications.py, docs/telegram.md, docs/notification_events.md</files>
  <behavior>
    - Test 1: D-22 and REG-04 regime alerts emit only when previous and current state differ.
    - Test 2: repeated unchanged regime state produces no new regime alert and can still be deduplicated by event type plus correlation ID.
    - Test 3: D-23 daily summaries include active Paper mode, new signals, entries, exits, open positions, rejected actions, and system warnings.
    - Test 4: D-24 normal historical backtests remain real-Telegram-disabled and Phase 7 preview events remain fake-collector-only.
  </behavior>
  <action>Add pure helpers for regime transition events and daily summary events. Regime helpers must require previous and current states and return no event for unchanged state. Daily summary events must be scheduled/end-of-day artifacts with sanitized payload fields and explicit source labels; they must not invent portfolio values or replace QuantConnect as source of truth. Add tests proving backtest preview events stay fake-collector-only and do not route to real Telegram in normal backtest execution.</action>
  <verify>
    <automated>python -m pytest tests/test_regime_telegram_alerts.py tests/test_daily_summary_notifications.py tests/test_backtest_notification_preview.py -q</automated>
  </verify>
  <done>REG-04 transition-only behavior, D-23 daily summaries, and D-24 historical-backtest safety are implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-test duplicate suppression, rate limits, and failure isolation across all alerts</name>
  <files>marketpilot/telegram.py, marketpilot/notification_events.py, tests/test_telegram_failure_isolation.py, tests/test_notification_dedup_rate_limit.py, docs/telegram.md, docs/safety.md</files>
  <behavior>
    - Test 1: D-18 disabled/missing-token/missing-chat-ID/quota/rate-limit/API-failure/rejection outcomes are typed for all representative alert families.
    - Test 2: D-19 duplicate suppression uses event type plus correlation ID and local rate limiting without paid broadcast behavior.
    - Test 3: TEL-06 Telegram delivery failure does not stop trading logic, order lifecycle, protective exits, reconciliation, or daily summary creation.
  </behavior>
  <action>Add cross-alert failure-isolation tests that use the Telegram fake/injected HTTP boundary from Plan 08-03. Test representative signal, order, fill, protective recovery, regime, system, error, start/restart, and daily-summary events. Confirm dedup and rate-limit outcomes only affect delivery attempts and never mutate Paper mode, order lifecycle, exit, reconciliation, or protective recovery decisions. Update docs with failure semantics and quota/rate-limit behavior.</action>
  <verify>
    <automated>python -m pytest tests/test_telegram_alert_coverage.py tests/test_regime_telegram_alerts.py tests/test_daily_summary_notifications.py tests/test_telegram_failure_isolation.py tests/test_notification_dedup_rate_limit.py -q</automated>
    <automated>python -m pytest -q</automated>
  </verify>
  <done>Telegram alert coverage, duplicate suppression, rate limiting, and failure behavior are verified across the Phase 8 event surface.</done>
</task>

</tasks>

<source_audit>
| Source | Items Covered |
|--------|---------------|
| GOAL | Telegram signal, paper activity, regime, system, error, start/restart, and daily summary alert behavior is complete and safe. |
| REQ | REG-04, TEL-03, TEL-04, and TEL-06 are covered. |
| RESEARCH | Extends `notification_events.py`, uses `telegram.py`, preserves fake collector behavior for tests, and keeps real Telegram disabled in normal backtests. |
| CONTEXT | D-21, D-22, D-23, D-24 are implemented; D-18 and D-19 are re-verified across all alert families. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trading/reconciliation events -> notification taxonomy | Safety-domain observations become alert events but cannot control trading. |
| Regime state -> Telegram alert event | Regime state transitions produce alerts only when the state changes. |
| Alert queue -> Telegram delivery | Deduplication, rate limiting, and failures affect only delivery attempts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-17 | Tampering | Alert taxonomy | mitigate | Tests enumerate required D-21 alert families and preserve existing Phase 6 event compatibility. |
| T-08-18 | Denial of Service | Duplicate/rate-limit layer | mitigate | Dedup/rate limit is local, conservative, and cannot mutate safety-domain decisions. |
| T-08-19 | Repudiation | Regime transition alerts | mitigate | Transition events include previous state, current state, timestamp, and correlation ID; unchanged states emit no alert. |
| T-08-20 | Information Disclosure | Telegram formatting | mitigate | Sanitized payloads exclude secrets, credentials, and profitability claims across all alert families. |
| T-08-21 | Elevation of Privilege | Backtest preview routing | mitigate | Normal historical backtests remain real-Telegram-disabled; preview events remain fake-collector-only. |
| T-08-SC | Tampering | package installs | accept | No npm, pip, or cargo install tasks exist in this plan; use existing dependencies and Python standard library only. |
</threat_model>

<verification>
- `python -m pytest tests/test_telegram_alert_coverage.py tests/test_regime_telegram_alerts.py tests/test_daily_summary_notifications.py tests/test_telegram_failure_isolation.py tests/test_notification_dedup_rate_limit.py -q`
- `python -m pytest -q`
- Confirm D-21 alert names and D-22 regime transition behavior are documented in `docs/notification_events.md` and `docs/telegram.md`.
</verification>

<success_criteria>
- Every TEL-03 alert family has a stable transport-neutral event and Telegram formatting coverage.
- REG-04 regime transition alerts emit only on actual state changes.
- Daily summaries are modeled without inventing portfolio data or replacing QuantConnect authority.
- Duplicate suppression, rate limiting, disabled/missing-secret behavior, delivery failure, and backtest preview safety are tested offline.
</success_criteria>

<output>
Create `.planning/phases/08-quantconnect-paper-trading-and-telegram/08-04-SUMMARY.md` when done.
</output>

