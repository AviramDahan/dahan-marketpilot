---
phase: 08-quantconnect-paper-trading-and-telegram
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - marketpilot/quantconnect_paper.py
  - marketpilot/reconciliation.py
  - marketpilot/recovery.py
  - marketpilot/exits.py
  - marketpilot/notification_events.py
  - tests/test_reconciliation.py
  - tests/test_quantconnect_restart_recovery.py
  - tests/test_protective_recovery.py
  - docs/recovery.md
  - docs/paper_trading.md
  - docs/notification_events.md
  - docs/safety.md
autonomous: true
requirements:
  - TEL-02
  - TEL-06
must_haves:
  truths:
    - "D-11 and TEL-02: QuantConnect remains authoritative for Paper cash, equity, holdings, orders, fills, deployment status, algorithm status, and Paper performance; local records are audit mirror and recovery context only."
    - "D-12 and TEL-02: reconciliation compares QuantConnect live state to local lifecycle/audit records; mismatches block new entries, preserve exit and protective recovery obligations, emit high-severity system events, and require explicit recovery handling."
    - "D-13 and TEL-02: QuantConnect order IDs and fill data become authoritative after submission, while local idempotency keys still prevent duplicate local intent generation before submission."
    - "D-14 and TEL-02: restart recovery rebuilds active position and order context from QuantConnect first, then attaches local audit history; QuantConnect unavailability never makes local state complete authority."
    - "D-15, TEL-02, and TEL-06: if a filled Paper position lacks required stop, target, or protective state, new entries are blocked and alerts may be emitted, but Telegram failure must not block protective recovery logic."
  artifacts:
    - path: "marketpilot/reconciliation.py"
      provides: "QuantConnect snapshot versus local lifecycle/audit comparison, mismatch classification, and new-entry block decisions"
      exports: ["ReconciliationDecision", "ReconciliationMismatch", "reconcile_quantconnect_state"]
    - path: "marketpilot/quantconnect_paper.py"
      provides: "Authoritative QuantConnect Paper snapshot contracts for deployment, portfolio, holdings, orders, fills, and algorithm status"
      exports: ["QuantConnectPaperSnapshot", "QuantConnectPaperOrder", "QuantConnectPaperFill"]
    - path: "marketpilot/recovery.py"
      provides: "QuantConnect-first restart recovery composition with local audit history attached only as context"
    - path: "tests/test_reconciliation.py"
      provides: "TEL-02 mismatch, authoritative-source, idempotency, and high-severity event tests"
    - path: "tests/test_protective_recovery.py"
      provides: "TEL-02 and TEL-06 protective recovery tests proving Telegram failure independence"
  key_links:
    - from: "marketpilot/reconciliation.py"
      to: "marketpilot/order_lifecycle.py"
      via: "OrderIntent idempotency and lifecycle states become local audit mirror after QuantConnect IDs/fills arrive"
      pattern: "OrderIntent|OrderLifecycleEvent|make_order_idempotency_key"
    - from: "marketpilot/reconciliation.py"
      to: "marketpilot/audit_journal.py"
      via: "Append-only audit records attach context without becoming authoritative portfolio state"
      pattern: "AuditJournalRecord|AppendOnlyJsonlAuditJournal"
    - from: "marketpilot/reconciliation.py"
      to: "marketpilot/exits.py"
      via: "Filled positions without protective state preserve stop/target/exit obligations and block new entries"
      pattern: "ExitPlan|obligations_active|stop|target"
---

<objective>
Create QuantConnect Paper state reconciliation, restart recovery, and
protective-order recovery contracts around QuantConnect as source of truth.

Purpose: satisfy TEL-02 and TEL-06 by turning Phase 6 local order, exit,
recovery, audit, and notification-domain models into a safe QuantConnect-aligned
recovery layer.

Output: `marketpilot/reconciliation.py`, extensions to
`marketpilot/quantconnect_paper.py`, `marketpilot/recovery.py`,
`marketpilot/exits.py`, notification-domain system events, tests, and docs.
This plan implements D-11 through D-15.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-quantconnect-paper-trading-and-telegram/08-CONTEXT.md
@.planning/phases/08-quantconnect-paper-trading-and-telegram/08-RESEARCH.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-CONTEXT.md
@.planning/phases/07-backtesting-and-validation/07-CONTEXT.md
@docs/safety.md
@docs/recovery.md
@docs/notification_events.md
@marketpilot/quantconnect_paper.py
@marketpilot/recovery.py
@marketpilot/risk.py
@marketpilot/order_lifecycle.py
@marketpilot/exits.py
@marketpilot/audit_journal.py
@marketpilot/notification_events.py
</context>

<interfaces>
- QuantConnect snapshots are typed input contracts; they may be fixtures in
  tests only when clearly labeled as deterministic test fixtures.
- Reconciliation decisions must expose `block_new_entries`, `preserve_exits`,
  `authoritative_source`, mismatch reasons, severity, and correlation ID.
- Protective recovery may emit `NotificationDomainEvent` objects but must not
  depend on a successful delivery result.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add QuantConnect snapshot and reconciliation contracts</name>
  <files>marketpilot/quantconnect_paper.py, marketpilot/reconciliation.py, tests/test_reconciliation.py, docs/paper_trading.md</files>
  <behavior>
    - Test 1: D-11 QuantConnect snapshot fields are authoritative for cash, equity, holdings, orders, fills, deployment status, algorithm status, and Paper performance.
    - Test 2: D-12 mismatched local lifecycle or audit state blocks new entries, preserves exits, emits a high-severity system event, and requires explicit recovery handling.
    - Test 3: D-13 QuantConnect order IDs and fill data override local mirror fields after submission, while local idempotency keys still prevent duplicate intent generation before submission.
  </behavior>
  <action>Create tests first. Extend `marketpilot.quantconnect_paper` with immutable snapshot/order/fill/status contracts using explicit fixture labels for deterministic tests. Add `marketpilot.reconciliation` with mismatch enums and a pure `reconcile_quantconnect_state` function that compares QuantConnect snapshots to local order lifecycle and audit context. The function must never overwrite QuantConnect state, must never fabricate missing QuantConnect fields, and must return an explicit new-entry block on mismatch while preserving exit obligations.</action>
  <verify>
    <automated>python -m pytest tests/test_reconciliation.py -q</automated>
  </verify>
  <done>QuantConnect Paper snapshots drive reconciliation decisions, and local lifecycle/audit data remains mirror context only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add QuantConnect-first restart recovery</name>
  <files>marketpilot/recovery.py, marketpilot/reconciliation.py, tests/test_quantconnect_restart_recovery.py, docs/recovery.md, docs/safety.md</files>
  <behavior>
    - Test 1: D-14 restart recovery rebuilds active position and order context from QuantConnect snapshot first.
    - Test 2: D-14 local audit history attaches after QuantConnect reconstruction and cannot become authoritative when QuantConnect is unavailable.
    - Test 3: unavailable QuantConnect input returns not_configured or not_run style recovery status and blocks new entries instead of pretending local state is complete.
  </behavior>
  <action>Extend restart recovery to compose the Phase 6 `RecoveryDecision` pattern with the new QuantConnect snapshot contract. Add a deterministic `recover_from_quantconnect_snapshot` style function that returns reconstructed context, attached local audit records, authoritative-source metadata, and recovery-required reasons. Keep JSONL audit history append-only and contextual. Document that QuantConnect outage or missing credentials prevents authoritative rebuild and blocks new entries until operator recovery.</action>
  <verify>
    <automated>python -m pytest tests/test_quantconnect_restart_recovery.py -q</automated>
  </verify>
  <done>Restart recovery is QuantConnect-first and fail-closed when QuantConnect state is unavailable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add protective-order recovery and Telegram-failure isolation</name>
  <files>marketpilot/exits.py, marketpilot/reconciliation.py, marketpilot/notification_events.py, tests/test_protective_recovery.py, docs/recovery.md, docs/notification_events.md</files>
  <behavior>
    - Test 1: D-15 a filled Paper position with missing stop, target, or protective state blocks new entries and marks protective recovery required.
    - Test 2: D-15 system or Telegram-domain alert events may be created for protective recovery, but delivery failure does not change the recovery decision.
    - Test 3: TEL-06 protective exit logic continues even when a fake notification collector simulates delivery failure.
  </behavior>
  <action>Add pure protective recovery helpers that inspect filled QuantConnect positions/orders against required `ExitPlan` obligations. If stop/target/protective state is absent, return a recovery decision with `block_new_entries: true`, `protective_recovery_required: true`, and a high-severity sanitized notification-domain event. Do not call Telegram, do not require Telegram configuration, and do not let fake collector failure alter the recovery decision. Update recovery and notification docs with the safety-critical behavior.</action>
  <verify>
    <automated>python -m pytest tests/test_protective_recovery.py tests/test_reconciliation.py tests/test_quantconnect_restart_recovery.py -q</automated>
    <automated>python -m pytest -q</automated>
  </verify>
  <done>Protective-order recovery is safety-critical, blocks new entries on missing protection, and is independent from notification delivery success.</done>
</task>

</tasks>

<source_audit>
| Source | Items Covered |
|--------|---------------|
| GOAL | QuantConnect Paper Trading state, order reconciliation, restart recovery, and protective-order recovery remain source-of-truth aligned. |
| REQ | TEL-02 and TEL-06 are covered. |
| RESEARCH | Uses `quantconnect_paper.py`, `reconciliation.py`, existing Phase 6 recovery/order/exit/audit contracts, and fake notification behavior. |
| CONTEXT | D-11, D-12, D-13, D-14, and D-15 are implemented by this plan. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QuantConnect snapshot -> local reconciliation | External authoritative Paper state crosses into local recovery and audit decisions. |
| Local audit mirror -> recovery context | Local records are useful context but must not replace QuantConnect authority. |
| Protective recovery -> notification events | Safety-critical recovery may emit alerts, but notification delivery is non-authoritative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | Reconciliation | mitigate | QuantConnect wins for holdings, cash, orders, fills, deployment, algorithm status, and Paper performance. |
| T-08-08 | Elevation of Privilege | Restart recovery | mitigate | QuantConnect unavailable or mismatched state blocks new entries and marks recovery required. |
| T-08-09 | Repudiation | Reconciliation mismatch events | mitigate | Emit high-severity system events with mismatch reasons and correlation IDs. |
| T-08-10 | Information Disclosure | Snapshot/audit payloads | mitigate | Sanitize event payloads and never store credentials or secret-bearing API responses. |
| T-08-11 | Denial of Service | Telegram failure during protective recovery | mitigate | Tests prove notification failure cannot stop protective recovery or exit obligations. |
| T-08-SC | Tampering | package installs | accept | No npm, pip, or cargo install tasks exist in this plan; use existing dependencies and Python standard library only. |
</threat_model>

<verification>
- `python -m pytest tests/test_reconciliation.py tests/test_quantconnect_restart_recovery.py tests/test_protective_recovery.py -q`
- `python -m pytest -q`
- Confirm no test invokes QuantConnect, Telegram, Render, broker credentials, internet, or real market access.
</verification>

<success_criteria>
- QuantConnect snapshots are the only authoritative Paper state inputs.
- Reconciliation mismatch blocks new entries and preserves exit/protective recovery obligations.
- Restart recovery uses QuantConnect first and attaches local audit history only as context.
- Protective recovery does not depend on Telegram delivery success.
</success_criteria>

<output>
Create `.planning/phases/08-quantconnect-paper-trading-and-telegram/08-02-SUMMARY.md` when done.
</output>

