---
phase: 14-data-sync-dashboard-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 14-02
files_modified:
  - dashboard/pages/overview.py
autonomous: true
requirements:
  - DASH-05
  - SAFE-04
must_haves:
  truths:
    - "Dashboard displays color-coded freshness banner (green/yellow/red) with last sync time in ET"
    - "Dashboard displays portfolio summary metrics: Cash, Equity, Unrealized P&L"
    - "Dashboard displays holdings table with symbol, quantity, avg_price, market_price, P&L%"
    - "Dashboard displays sync status section: last poll time, error count"
    - "All displayed timestamps are converted from UTC to ET at the display boundary"
    - "Empty/unavailable data shows informational message instead of fabricated values"
    - "QC is explicitly labeled as authoritative source in the UI"
  artifacts:
    - path: "dashboard/pages/overview.py"
      provides: "Extended overview with freshness banner, portfolio metrics, holdings table, sync status"
      contains: "render_freshness_banner"
  key_links:
    - from: "dashboard/pages/overview.py"
      to: "dashboard/models.py"
      via: "DashboardFreshnessStatus enum for banner color"
      pattern: "DashboardFreshnessStatus"
    - from: "dashboard/pages/overview.py"
      to: "dashboard/data.py"
      via: "DashboardSnapshot containing sync data"
      pattern: "DashboardSnapshot"
---

<objective>
Extend the dashboard Portfolio/Overview page with freshness-aware portfolio display per D-12/D-13. Add: (1) color-coded freshness banner with timestamps in ET, (2) portfolio summary metric cards, (3) holdings table, and (4) sync status section. All timestamps convert from UTC to ET at the display boundary per SAFE-04.

Purpose: Users see current QC portfolio state with explicit freshness indication — green for fresh, yellow for stale warning, red for error state. The dashboard never fabricates data and clearly labels QC as authoritative.

Output: Extended `dashboard/pages/overview.py` with sync portfolio display elements.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-data-sync-dashboard-integration/14-CONTEXT.md
@.planning/phases/14-data-sync-dashboard-integration/14-RESEARCH.md
@.planning/phases/14-data-sync-dashboard-integration/14-PATTERNS.md
@.planning/phases/14-data-sync-dashboard-integration/14-02-SUMMARY.md
@dashboard/pages/overview.py
@dashboard/models.py
@dashboard/data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add freshness banner and portfolio metrics to overview</name>
  <files>dashboard/pages/overview.py</files>
  <read_first>
    - dashboard/pages/overview.py (full file — current build_overview function, OverviewView dataclass, import patterns)
    - dashboard/models.py (DashboardFreshnessStatus members including ERROR, DashboardSnapshot structure, DashboardPortfolioSection, DashboardSourceMetadata)
    - dashboard/data.py (DashboardSnapshot returned fields — portfolio section items, source_metadata fields)
  </read_first>
  <action>
Extend `dashboard/pages/overview.py` to include sync portfolio display elements per D-13. Add these pure-function view builders following the existing `build_overview` pattern (frozen dataclass view models + pure builder functions):

1. **Import `zoneinfo`** — add `from zoneinfo import ZoneInfo` and `ET = ZoneInfo("America/New_York")` as module-level constant for UTC→ET conversion at display boundary.

2. **Create `SyncPortfolioView` frozen dataclass** with fields:
   - `freshness_label: str` — e.g. "✅ Portfolio data fresh — last update: 14:30:02 ET"
   - `freshness_level: str` — "fresh" | "stale" | "error" | "unavailable"
   - `cash: str | None` — formatted currency string or None
   - `equity: str | None` — formatted currency string or None
   - `unrealized_pnl: str | None` — formatted P&L string or None
   - `holdings: tuple[HoldingRow, ...] | None` — None if no data
   - `sync_status_label: str` — e.g. "Last sync: 14:30:02 ET | Errors: 0"
   - `authority_label: str` — "Source: QuantConnect (authoritative)"

3. **Create `HoldingRow` frozen dataclass** with fields: `symbol: str`, `quantity: str`, `avg_price: str`, `market_price: str`, `pnl_pct: str`

4. **Create `build_sync_portfolio_view(snapshot: DashboardSnapshot) -> SyncPortfolioView`** function:
   - If `snapshot.source_metadata.freshness_status == UNKNOWN` or portfolio section status is NOT_AVAILABLE/NOT_CONFIGURED → return view with `freshness_level="unavailable"`, `freshness_label="📊 No sync data available — run python -m marketpilot sync to start"`, all metrics None
   - Convert `source_metadata.source_timestamp` from UTC to ET using `.astimezone(ET).strftime("%H:%M:%S ET")` (D-11/SAFE-04)
   - Map freshness status to banner:
     - FRESH → `freshness_level="fresh"`, label: "✅ Portfolio data fresh — last update: {et_time}"
     - STALE → `freshness_level="stale"`, label: "⚠️ Portfolio data stale (>10 min) — last update: {et_time}"
     - ERROR → `freshness_level="error"`, label: "🔴 Portfolio data error (>30 min) — last update: {et_time}"
   - Extract cash, equity, unrealized_pnl from snapshot.portfolio section data (format as USD strings)
   - Build HoldingRow tuples from portfolio holdings items
   - Build sync_status_label from source_metadata timestamps (converted to ET)
   - Set `authority_label = "Source: QuantConnect (authoritative)"` per D-15

5. **Update `build_overview()`** — add a call to `build_sync_portfolio_view(snapshot)` and include the sync portfolio view in OverviewView (either extend OverviewView with a `sync_portfolio: SyncPortfolioView | None` field, or compose the lines). Follow the existing pattern — if OverviewView uses `lines: tuple[str, ...]`, append the freshness and sync status as additional lines. If extending the dataclass is cleaner, add the field with `None` default.

Key constraints:
- All timezone conversion (UTC→ET) happens HERE in the display layer, nowhere else (SAFE-04/D-11)
- Never fabricate values — if data is None/missing, show explicit "not available" message (D-14)
- QC authority label is always visible (D-15)
- Pure functions only — no side effects, no file I/O in this module
  </action>
  <verify>
    <automated>python -c "from dashboard.pages.overview import build_sync_portfolio_view, SyncPortfolioView, HoldingRow; print('imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - SyncPortfolioView and HoldingRow are frozen dataclasses exported from dashboard/pages/overview.py
    - build_sync_portfolio_view returns a view with freshness_level matching snapshot freshness status
    - Timestamps in freshness_label and sync_status_label are in ET format (not UTC)
    - freshness_level is "unavailable" when no sync data exists (not an error or crash)
    - authority_label contains "QuantConnect" and "authoritative" per D-15
    - Holdings rows include symbol, quantity, avg_price, market_price, pnl_pct fields
    - No file I/O or imports from marketpilot.sync in this module
    - zoneinfo.ZoneInfo("America/New_York") used for UTC→ET conversion
    - build_overview still works unchanged for existing tests
  </acceptance_criteria>
  <done>Dashboard overview produces freshness banner view (green/yellow/red), portfolio metric strings, holdings rows, and sync status label — all with ET timestamps and QC authority.</done>
</task>

<task type="auto">
  <name>Task 2: Wire sync portfolio view into existing overview output</name>
  <files>dashboard/pages/overview.py</files>
  <read_first>
    - dashboard/pages/overview.py (after Task 1 changes — see updated OverviewView and build_overview)
    - dashboard/models.py (DashboardSnapshot fields that carry portfolio data from sync_jsonl loader)
  </read_first>
  <action>
Ensure `build_overview()` incorporates the sync portfolio view into its output so the Streamlit rendering layer can display it.

If Task 1 extended `OverviewView` with a `sync_portfolio: SyncPortfolioView | None` field:
- In `build_overview()`, call `sync_view = build_sync_portfolio_view(snapshot)`
- Pass `sync_portfolio=sync_view` to OverviewView constructor
- Default to None if build_sync_portfolio_view raises (defensive — shouldn't happen with pure function but adds robustness)

If Task 1 appended to `lines: tuple[str, ...]`:
- Add sync portfolio lines at the TOP of the lines tuple (D-13 says display elements go at top of portfolio page):
  - freshness_label
  - Cash/Equity/P&L metrics as formatted strings
  - Holdings summary (count and top symbols)
  - sync_status_label
  - authority_label
- These lines appear BEFORE the existing overview lines (disclaimer, QC source, paper mode, etc.)

Verify that `build_overview(snapshot)` still returns valid output for snapshots without sync data (freshness_status=UNKNOWN) — the sync_portfolio_view handles this by showing "unavailable" state.

The existing `_system_warnings()` function should remain unchanged.
  </action>
  <verify>
    <automated>python -c "
from dashboard.pages.overview import build_overview, OverviewView
from dashboard.models import DashboardSnapshot, DashboardSourceMetadata, DashboardFreshnessStatus, DashboardAuthority, DashboardPortfolioSection, DashboardCollectionSection, DashboardSectionStatus
from datetime import datetime, timezone
meta = DashboardSourceMetadata(source='test', source_timestamp=datetime.now(timezone.utc), cache_timestamp=datetime.now(timezone.utc), freshness_status=DashboardFreshnessStatus.UNKNOWN, authority=DashboardAuthority.AUTHORITATIVE)
# Minimal snapshot construction test
print('build_overview wiring OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - build_overview() incorporates sync portfolio view for snapshots with sync data
    - build_overview() handles snapshots without sync data gracefully (freshness_level="unavailable")
    - Sync portfolio display elements appear at top of overview (per D-13 ordering)
    - Existing overview lines (disclaimer, paper mode, etc.) still present after sync elements
    - _system_warnings() function unchanged
    - No breaking changes to existing tests that call build_overview()
  </acceptance_criteria>
  <done>build_overview integrates sync portfolio view: freshness banner, metrics, holdings summary, and sync status are included in the overview output for Streamlit rendering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DashboardSnapshot → display layer | Snapshot data rendered to user — must never fabricate |
| UTC timestamps → ET display strings | Timezone conversion boundary (SAFE-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | Information Disclosure | Portfolio values in UI | accept | Dashboard is authenticated (auth_enabled=True in config); portfolio is paper-only |
| T-14-11 | Spoofing | Fabricated display values | mitigate | Pure function returns None/unavailable for missing data; never generates fake numbers |
| T-14-12 | Tampering | Incorrect timezone display | mitigate | All ET conversion uses zoneinfo.ZoneInfo("America/New_York") — DST-aware stdlib |
</threat_model>

<verification>
- `python -c "from dashboard.pages.overview import build_sync_portfolio_view, SyncPortfolioView, HoldingRow"` imports OK
- build_overview still produces valid OverviewView for existing test fixtures
- All existing tests pass: `pytest --tb=short -q`
- Freshness banner text includes "ET" timezone marker
</verification>

<success_criteria>
- Dashboard overview page displays freshness-aware sync portfolio information
- Three freshness states visually distinguishable (fresh/stale/error)
- All timestamps shown in ET (converted from UTC at display boundary only)
- Holdings table data available as structured view for Streamlit rendering
- QC authority always visible to user
- No data fabrication — missing sync data shows explicit informational message
</success_criteria>

## Artifacts this phase produces

### New symbols created by this plan:
- **Dataclass:** `dashboard.pages.overview.SyncPortfolioView` — frozen view model for sync portfolio display
- **Dataclass:** `dashboard.pages.overview.HoldingRow` — frozen view model for individual holding
- **Function:** `dashboard.pages.overview.build_sync_portfolio_view()` — builds sync portfolio view from DashboardSnapshot
- **Constant:** `dashboard.pages.overview.ET` — ZoneInfo("America/New_York") for display conversion
- **Field:** `OverviewView.sync_portfolio` (or additional lines) — sync portfolio integrated into overview output

<output>
Create `.planning/phases/14-data-sync-dashboard-integration/14-03-SUMMARY.md` when done
</output>
