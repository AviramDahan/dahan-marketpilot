---
phase: 14-data-sync-dashboard-integration
plan: 04
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - tests/test_sync.py
  - tests/test_dashboard_sync_loader.py
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03
  - SYNC-04
  - SYNC-05
  - SYNC-06
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - SAFE-04
must_haves:
  truths:
    - "Test suite covers sync_portfolio happy path (poll → reconcile → persist → return)"
    - "Test suite covers atomic JSONL write and last-line read"
    - "Test suite covers generation counter monotonic increment"
    - "Test suite covers threshold-based alert emission (ORDER mismatch always, CASH only >1%)"
    - "Test suite covers PAPER_TRADING_ONLY enforcement"
    - "Test suite covers 3-state freshness evaluation (FRESH/STALE/ERROR boundaries)"
    - "Test suite covers sync_jsonl loader degraded states (missing file, empty file, corrupt JSON)"
    - "All 454+ existing tests still pass unchanged"
  artifacts:
    - path: "tests/test_sync.py"
      provides: "Sync module unit tests"
      min_lines: 80
    - path: "tests/test_dashboard_sync_loader.py"
      provides: "Dashboard sync_jsonl loader tests"
      min_lines: 60
  key_links:
    - from: "tests/test_sync.py"
      to: "marketpilot/sync.py"
      via: "imports sync_portfolio, atomic_jsonl_append, read_last_sync_record"
      pattern: "from marketpilot\\.sync import"
    - from: "tests/test_dashboard_sync_loader.py"
      to: "dashboard/data.py"
      via: "imports load_dashboard_snapshot"
      pattern: "from dashboard\\.data import"
---

<objective>
Create comprehensive test suites for the sync module and dashboard sync_jsonl loader. Tests verify all requirement behaviors: atomic JSONL I/O, generation counters, threshold-based alerting, 3-state freshness evaluation, degraded states, and PAPER_TRADING_ONLY enforcement.

Purpose: Validate that all SYNC-* and DASH-* requirements are satisfied with automated tests. These tests serve as regression guards for the sync pipeline and dashboard data layer.

Output: `tests/test_sync.py` + `tests/test_dashboard_sync_loader.py`
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-data-sync-dashboard-integration/14-CONTEXT.md
@.planning/phases/14-data-sync-dashboard-integration/14-RESEARCH.md
@.planning/phases/14-data-sync-dashboard-integration/14-PATTERNS.md
@.planning/phases/14-data-sync-dashboard-integration/14-01-SUMMARY.md
@.planning/phases/14-data-sync-dashboard-integration/14-02-SUMMARY.md
@marketpilot/sync.py
@dashboard/data.py
@tests/test_audit_journal.py
@tests/test_reconciliation.py
@tests/test_qc_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sync module test suite</name>
  <files>tests/test_sync.py</files>
  <read_first>
    - marketpilot/sync.py (full file — all functions, SyncRecord, SyncResult, sync_portfolio signature)
    - tests/test_audit_journal.py (JSONL test patterns — tmp_path usage, record assertion style)
    - tests/test_reconciliation.py (snapshot fixture pattern — _snapshot() builder, assertion patterns)
    - tests/test_qc_api.py (mocked client pattern — patch.object approach)
    - marketpilot/quantconnect_paper.py (QuantConnectPaperSnapshot constructor, QuantConnectHolding, QuantConnectPaperPerformance fields)
    - marketpilot/reconciliation.py (ReconciliationDecision structure, ReconciliationMismatch, ReconciliationMismatchType)
  </read_first>
  <action>
Create `tests/test_sync.py` with these test cases using pytest and tmp_path fixture:

**Fixtures:**
- `_test_snapshot() -> QuantConnectPaperSnapshot` — deterministic test fixture with captured_at=datetime(2026, 6, 16, 14, 30, tzinfo=timezone.utc), cash=Decimal("100000"), portfolio_equity=Decimal("105000"), holdings with AAPL (50 shares, avg 175, market 182), deployment_status=RUNNING, algorithm_status=RUNNING, performance with unrealized_profit=Decimal("350")
- `_clean_decision(correlation_id: str) -> ReconciliationDecision` — no mismatches, authoritative_source="quantconnect", block_new_entries=False, preserve_exits=True, requires_explicit_recovery=False, mismatches=()
- `_mismatch_decision(correlation_id: str, mismatch_type: ReconciliationMismatchType) -> ReconciliationDecision` — one mismatch of given type

**Test cases (each as a separate test function):**

1. `test_atomic_jsonl_append_creates_file(tmp_path)` — append to non-existent file, verify file created with 1 line, parse with json.loads
2. `test_atomic_jsonl_append_appends_lines(tmp_path)` — append 3 records, verify file has 3 lines
3. `test_atomic_jsonl_append_no_partial_on_existing(tmp_path)` — write initial content, append, verify all previous content intact plus new line
4. `test_read_last_sync_record_missing_file(tmp_path)` — returns None for non-existent path
5. `test_read_last_sync_record_empty_file(tmp_path)` — write empty file, returns None
6. `test_read_last_sync_record_returns_last_line(tmp_path)` — write 3 records with different generations, verify returns generation=3 record
7. `test_next_generation_starts_at_one(tmp_path)` — empty/missing file → generation 1
8. `test_next_generation_increments(tmp_path)` — existing file with generation 5 → returns 6
9. `test_sync_portfolio_rejects_paper_trading_false(tmp_path)` — patch `PAPER_TRADING_ONLY` to False, verify RuntimeError raised
10. `test_sync_portfolio_success_happy_path(tmp_path)` — mock QCApiClient.read_live_algorithm to return _test_snapshot, mock reconcile_quantconnect_state to return _clean_decision; verify JSONL file gets one record with sync_status="success", reconciliation_clean=True, generation=1
11. `test_sync_portfolio_api_error_persists_error_record(tmp_path)` — mock QCApiClient.read_live_algorithm to raise QCApiError; verify JSONL gets error record with sync_status="api_error"
12. `test_sync_portfolio_emits_alert_on_order_mismatch(tmp_path)` — mock reconcile to return decision with ORDER_ID mismatch; verify SyncResult.alert_emitted=True and event_for_system_incident was called
13. `test_sync_portfolio_no_alert_on_small_cash_mismatch(tmp_path)` — CASH mismatch with delta < 1% of equity → alert_emitted=False
14. `test_sync_portfolio_alerts_on_large_cash_mismatch(tmp_path)` — CASH mismatch with delta > 1% of equity → alert_emitted=True
15. `test_sync_portfolio_never_auto_corrects(tmp_path)` — verify sync_portfolio returns result but does NOT modify snapshot or call any correction function (SYNC-04)
16. `test_sync_record_timestamps_are_utc(tmp_path)` — verify all datetime fields in persisted JSONL have "+00:00" suffix (SAFE-04)

Use `unittest.mock.patch` for mocking QCApiClient and reconcile_quantconnect_state. Use `tmp_path / "portfolio_sync.jsonl"` for JSONL path.
  </action>
  <verify>
    <automated>pytest tests/test_sync.py -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_sync.py contains at least 14 test functions
    - All tests pass with pytest
    - Tests cover: atomic write, last-line read, generation counter, PAPER_TRADING_ONLY enforcement, happy path sync, API error, threshold alerting (order always, cash >1%), no auto-correct, UTC timestamps
    - Tests use tmp_path for file isolation (no test pollution)
    - Tests mock QCApiClient and reconcile_quantconnect_state (no real API calls)
    - No new test dependencies beyond pytest and unittest.mock
  </acceptance_criteria>
  <done>Sync module has comprehensive test coverage: JSONL I/O, generation counter, threshold logic, alert emission, safety enforcement, UTC compliance all verified.</done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard sync_jsonl loader test suite</name>
  <files>tests/test_dashboard_sync_loader.py</files>
  <read_first>
    - dashboard/data.py (full file — _load_sync_jsonl_snapshot, load_dashboard_snapshot dispatch)
    - dashboard/models.py (DashboardFreshnessStatus, DashboardSnapshot, DashboardSectionStatus)
    - dashboard/config.py (DashboardConfig constructor with data_source_kind, stale_warning_seconds, stale_error_seconds)
    - tests/test_sync.py (fixture patterns from Task 1 — reuse snapshot format)
  </read_first>
  <action>
Create `tests/test_dashboard_sync_loader.py` with test cases for the sync_jsonl dashboard loader:

**Helper:**
- `_write_sync_record(path: Path, record: dict) -> None` — writes a single JSON line to the given path (for setting up test JSONL files)
- `_sample_record(source_timestamp: datetime, generation: int = 1) -> dict` — returns a valid JSONL record dict matching the schema from RESEARCH.md (generation, source_timestamp ISO, captured_at ISO, sync_status="success", reconciliation_clean=True, portfolio dict with cash/equity/holdings/unrealized_profit, orders_count, fills_count, deployment_status, algorithm_status, error_detail=None)

**Test cases:**

1. `test_sync_jsonl_dispatch(tmp_path)` — create DashboardConfig with data_source_kind="sync_jsonl" and data_source_path pointing to a valid JSONL file; call load_dashboard_snapshot; verify it returns a DashboardSnapshot (not error/not_configured)
2. `test_sync_jsonl_missing_file(tmp_path)` — point to non-existent path; verify snapshot has status NOT_AVAILABLE and reason contains "no_sync_data"
3. `test_sync_jsonl_empty_file(tmp_path)` — create empty file; verify returns NOT_AVAILABLE state
4. `test_sync_jsonl_corrupt_json(tmp_path)` — write invalid JSON line; verify returns ERROR status (not crash)
5. `test_freshness_fresh(tmp_path)` — write record with source_timestamp = 5 minutes ago; verify freshness_status == FRESH
6. `test_freshness_stale(tmp_path)` — write record with source_timestamp = 15 minutes ago; verify freshness_status == STALE
7. `test_freshness_error(tmp_path)` — write record with source_timestamp = 45 minutes ago; verify freshness_status == ERROR
8. `test_freshness_boundary_10min(tmp_path)` — write record with source_timestamp = exactly 600 seconds ago; verify FRESH (≤ threshold)
9. `test_freshness_boundary_30min(tmp_path)` — write record with source_timestamp = exactly 1800 seconds ago; verify STALE (≤ threshold)
10. `test_authority_is_authoritative(tmp_path)` — verify DashboardSourceMetadata.authority == DashboardAuthority.AUTHORITATIVE
11. `test_no_fabrication_on_missing_data(tmp_path)` — missing file → portfolio section has no items, no fabricated values
12. `test_source_timestamp_parsed_as_utc(tmp_path)` — write record with UTC ISO timestamp; verify source_metadata.source_timestamp has tzinfo=timezone.utc

Use `datetime.now(timezone.utc)` for all test timestamps. Pass `now=` parameter to `load_dashboard_snapshot` to control wall-clock for deterministic freshness assertions.
  </action>
  <verify>
    <automated>pytest tests/test_dashboard_sync_loader.py -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_dashboard_sync_loader.py contains at least 10 test functions
    - All tests pass with pytest
    - Tests cover: dispatch to sync_jsonl, missing/empty/corrupt file handling, 3-state freshness (FRESH/STALE/ERROR) with boundary values, authority=AUTHORITATIVE, no data fabrication, UTC timestamp parsing
    - Tests use tmp_path for file isolation
    - Tests pass `now=` parameter to control freshness evaluation deterministically
    - No imports from marketpilot.sync (dashboard tests verify dashboard behavior independently)
  </acceptance_criteria>
  <done>Dashboard sync_jsonl loader has full test coverage: dispatch, degraded states, 3-state freshness with boundary tests, authority, no-fabrication, and UTC compliance.</done>
</task>

<task type="auto">
  <name>Task 3: Verify all existing tests pass (regression check)</name>
  <files>tests/</files>
  <read_first>
    - tests/test_sync.py (verify new test file exists and passes)
    - tests/test_dashboard_sync_loader.py (verify new test file exists and passes)
  </read_first>
  <action>
Run the full test suite to verify no regressions. Per D-20, all 454+ existing tests must pass unchanged.

Execute: `pytest --tb=short -q`

If any existing tests fail:
- Identify if the failure is due to the new ERROR enum member in DashboardFreshnessStatus (could affect tests that enumerate freshness states)
- Fix by updating only the failing assertion if it's a benign enum extension issue
- Do NOT modify existing test logic or behavior

If all pass, record the total test count in the summary.
  </action>
  <verify>
    <automated>pytest --tb=short -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pytest` exits with code 0
    - All existing tests (454+) pass unchanged
    - New test files (test_sync.py, test_dashboard_sync_loader.py) also pass
    - Total test count is at least 454 + new tests (≥ 480)
    - No warnings about deprecated imports or broken fixtures
  </acceptance_criteria>
  <done>Full test suite passes with zero regressions. New Phase 14 tests integrated and passing alongside all existing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → assertions | Test data must not accidentally validate incorrect behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | Tampering | Test isolation | mitigate | All tests use tmp_path; no shared state between tests; no real API calls |
| T-14-14 | Repudiation | Test coverage gaps | mitigate | Tests explicitly cover each requirement boundary (FRESH/STALE/ERROR thresholds, 1% equity threshold, ORDER always-alert) |
</threat_model>

<verification>
- `pytest tests/test_sync.py tests/test_dashboard_sync_loader.py -v` shows all tests passing
- `pytest --tb=short -q` full suite passes with 0 failures
- Test count ≥ 480 (454 existing + new)
</verification>

<success_criteria>
- Sync module has unit tests covering all SYNC-* requirement behaviors
- Dashboard loader has unit tests covering all DASH-* requirement behaviors
- All existing tests pass unchanged (SAFE-03/D-20 compliance)
- Tests are deterministic (controlled timestamps, mocked APIs, tmp_path isolation)
- No new test dependencies beyond pytest and unittest.mock
</success_criteria>

## Artifacts this phase produces

### New symbols created by this plan:
- **File:** `tests/test_sync.py` — sync module test suite (≥14 test functions)
- **File:** `tests/test_dashboard_sync_loader.py` — dashboard sync loader test suite (≥10 test functions)
- **Test fixtures:** `_test_snapshot()`, `_clean_decision()`, `_mismatch_decision()` in test_sync.py
- **Test helpers:** `_write_sync_record()`, `_sample_record()` in test_dashboard_sync_loader.py

<output>
Create `.planning/phases/14-data-sync-dashboard-integration/14-04-SUMMARY.md` when done
</output>
