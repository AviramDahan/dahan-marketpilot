---
phase: 13-qc-api-client-and-safety-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - requirements-dev.txt
  - pyproject.toml
  - .pre-commit-config.yaml
  - .secrets.baseline
  - .gitignore
autonomous: true
requirements: [API-04, SAFE-01]
must_haves:
  truths:
    - "tenacity>=9.0.0 is declared in requirements.txt and pyproject.toml dependencies"
    - "detect-secrets and pre-commit are declared in requirements-dev.txt and pyproject.toml dev extras"
    - "pre-commit hook rejects any commit setting PAPER_TRADING_ONLY = False"
    - "detect-secrets baseline file exists and pre-commit hook references it"
    - ".env is in .gitignore"
  artifacts:
    - path: "requirements.txt"
      provides: "tenacity runtime dependency"
      contains: "tenacity>=9.0.0"
    - path: "requirements-dev.txt"
      provides: "detect-secrets and pre-commit dev dependencies"
      contains: "detect-secrets>=1.5.0"
    - path: ".pre-commit-config.yaml"
      provides: "detect-secrets hook + PAPER_TRADING_ONLY guard hook"
      contains: "detect-secrets"
    - path: ".secrets.baseline"
      provides: "detect-secrets baseline for repo scanning"
  key_links:
    - from: ".pre-commit-config.yaml"
      to: ".secrets.baseline"
      via: "args reference"
      pattern: "--baseline.*\\.secrets\\.baseline"
---

<objective>
Install runtime and dev dependencies for QC API client phase; configure pre-commit hooks for credential leakage prevention (detect-secrets) and PAPER_TRADING_ONLY tampering prevention.

Purpose: Foundation dependencies and safety hooks must exist before the API client module is built, ensuring no credential can leak and no safety constant can be disabled via commit.
Output: Updated dependency files, .pre-commit-config.yaml, .secrets.baseline, updated .gitignore
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-qc-api-client-and-safety-foundation/13-RESEARCH.md
@.planning/phases/13-qc-api-client-and-safety-foundation/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tenacity to runtime deps; detect-secrets and pre-commit to dev deps</name>
  <files>requirements.txt, requirements-dev.txt, pyproject.toml</files>
  <read_first>
    - requirements.txt (current dependency list and format)
    - requirements-dev.txt (current dev deps and format)
    - pyproject.toml (current dependencies and optional-dependencies sections)
  </read_first>
  <action>
Add `tenacity>=9.0.0` to `requirements.txt` (alphabetical position among existing deps). Add to `pyproject.toml` `[project].dependencies` array.

Add `detect-secrets>=1.5.0` and `pre-commit>=4.0.0` to `requirements-dev.txt`. Add to `pyproject.toml` `[project.optional-dependencies].dev` array.

Run `pip install tenacity>=9.0.0 detect-secrets>=1.5.0 pre-commit>=4.0.0` to install locally.

Verify with `pip show tenacity detect-secrets pre-commit` that all three are installed.

Per D-12: These are new modules; existing 433 tests remain unaffected since nothing imports tenacity yet.
  </action>
  <verify>
    <automated>pip show tenacity | grep -i "version" ; pip show detect-secrets | grep -i "version" ; pip show pre-commit | grep -i "version" ; grep "tenacity" requirements.txt ; grep "detect-secrets" requirements-dev.txt</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains line matching `tenacity>=9.0.0`
    - requirements-dev.txt contains lines matching `detect-secrets>=1.5.0` and `pre-commit>=4.0.0`
    - pyproject.toml [project].dependencies contains `"tenacity>=9.0.0"`
    - pyproject.toml [project.optional-dependencies].dev contains `"detect-secrets>=1.5.0"` and `"pre-commit>=4.0.0"`
    - `pip show tenacity` returns version >= 9.0.0
    - `pip show detect-secrets` returns version >= 1.5.0
    - Existing test suite still passes: `pytest -q --co` shows 433+ tests collected
  </acceptance_criteria>
  <done>All three packages installed and declared in both requirements files and pyproject.toml</done>
</task>

<task type="auto">
  <name>Task 2: Create .pre-commit-config.yaml with detect-secrets and PAPER_TRADING_ONLY guard hooks</name>
  <files>.pre-commit-config.yaml, .secrets.baseline, .gitignore</files>
  <read_first>
    - .gitignore (to verify .env is listed; add if missing)
    - marketpilot/constants.py (confirm PAPER_TRADING_ONLY = True exists)
  </read_first>
  <action>
Create `.pre-commit-config.yaml` with two hook repos:

1. Yelp/detect-secrets rev v1.5.0 — hook id `detect-secrets`, args `['--baseline', '.secrets.baseline']`, exclude pattern `\.secrets\.baseline$`
2. Local repo — hook id `paper-trading-only-guard`, name "Reject PAPER_TRADING_ONLY = False", entry uses bash grep command: `bash -c 'if grep -rn "PAPER_TRADING_ONLY\s*=\s*False" --include="*.py" .; then echo "ERROR: PAPER_TRADING_ONLY must never be set to False"; exit 1; fi'`, language system, types [python], pass_filenames false.

Run `detect-secrets scan > .secrets.baseline` to generate the baseline file.

Run `pre-commit install` to activate hooks in the local git repo.

Ensure `.env` is in `.gitignore`. Add it if not already present.

Per D-06 and D-10: These hooks enforce SAFE-01 (constant tampering prevention) and API-04 (credential leakage prevention at commit time).
  </action>
  <verify>
    <automated>pre-commit run --all-files 2>&1 | head -20 ; test -f .secrets.baseline && echo "baseline exists" ; grep "\.env" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `.pre-commit-config.yaml` exists with `detect-secrets` hook referencing `.secrets.baseline`
    - `.pre-commit-config.yaml` has local hook `paper-trading-only-guard` that greps for `PAPER_TRADING_ONLY\s*=\s*False`
    - `.secrets.baseline` file exists (generated by detect-secrets scan)
    - `pre-commit run --all-files` passes (exit code 0) on clean repo state
    - `.gitignore` contains `.env` entry
    - Creating a test file with `PAPER_TRADING_ONLY = False` would be rejected by the hook
  </acceptance_criteria>
  <done>Pre-commit hooks installed and functional; detect-secrets baseline generated; .env protected in .gitignore</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → git repo | Credentials in .env must never reach committed files |
| Code change → constants.py | PAPER_TRADING_ONLY constant must not be tampered with |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | PAPER_TRADING_ONLY constant | mitigate | pre-commit grep hook rejects any commit setting value to False |
| T-13-02 | Information Disclosure | .env credentials | mitigate | detect-secrets pre-commit hook + .gitignore entry for .env |
| T-13-SC | Tampering | pip installs (tenacity, detect-secrets, pre-commit) | mitigate | Package Legitimacy Audit passed (all OK in RESEARCH.md); well-known packages with 2M-40M weekly downloads |
</threat_model>

<verification>
- `pre-commit run --all-files` exits 0
- `grep "tenacity" requirements.txt` returns a match
- `grep "detect-secrets" requirements-dev.txt` returns a match
- `.secrets.baseline` file is non-empty JSON
- Full test suite unaffected: `pytest -q --co` collects 433+ tests
</verification>

<success_criteria>
- All dependency files updated with correct version pins
- Pre-commit hooks active and passing on clean repo
- PAPER_TRADING_ONLY guard hook would block a commit introducing `= False`
- detect-secrets baseline file generated
- No existing tests broken
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

### New Files
- `.pre-commit-config.yaml` — pre-commit hook configuration (detect-secrets + PAPER_TRADING_ONLY guard)
- `.secrets.baseline` — detect-secrets baseline JSON

### Modified Files
- `requirements.txt` — added `tenacity>=9.0.0`
- `requirements-dev.txt` — added `detect-secrets>=1.5.0`, `pre-commit>=4.0.0`
- `pyproject.toml` — added tenacity to dependencies; detect-secrets and pre-commit to dev extras
- `.gitignore` — ensured `.env` entry exists

### Symbols / Identifiers
- Pre-commit hook ID: `detect-secrets`
- Pre-commit hook ID: `paper-trading-only-guard`
</artifacts_produced>

<output>
Create `.planning/phases/13-qc-api-client-and-safety-foundation/13-01-SUMMARY.md` when done
</output>
