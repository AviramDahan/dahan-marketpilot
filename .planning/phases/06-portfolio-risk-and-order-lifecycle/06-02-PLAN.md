---
phase: 06-portfolio-risk-and-order-lifecycle
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - marketpilot/order_lifecycle.py
  - tests/test_order_lifecycle_contract.py
  - tests/test_order_lifecycle_transitions.py
  - tests/test_order_idempotency.py
  - tests/test_order_lifecycle_safety.py
  - docs/order_lifecycle.md
  - docs/safety.md
  - docs/testing.md
autonomous: true
requirements:
  - SCO-04
  - RISK-03
  - RISK-04
must_haves:
  truths:
    - "D-09 and RISK-04: lifecycle states cover planned, submitted, partially_filled, filled, rejected, canceled, protective_orders_pending, open, partially_closed, and closed."
    - "D-10: QuantConnect is source of truth for actual order/fill state; local models are audit mirrors, intent records, and recovery context only."
    - "D-11 and RISK-03: duplicate prevention uses an idempotency key from symbol + strategy_mode + primary_setup + signal_time + portfolio_epoch."
    - "D-12: Phase 6 creates models, intents, transitions, and events only; actual QuantConnect Paper order submission remains Phase 8."
    - "Lifecycle transitions must preserve SCO-04 audit evidence and must not submit, cancel, replace, or modify real or Paper orders."
  artifacts:
    - path: "marketpilot/order_lifecycle.py"
      provides: "Order intent models, lifecycle states, transition validation, lifecycle events, and idempotency key builder"
      exports: ["OrderIntent", "OrderLifecycleState", "OrderLifecycleEvent", "transition_order_state", "make_order_idempotency_key"]
    - path: "tests/test_order_lifecycle_contract.py"
      provides: "Lifecycle state and intent shape tests"
    - path: "tests/test_order_lifecycle_transitions.py"
      provides: "Allowed and forbidden transition tests"
    - path: "tests/test_order_idempotency.py"
      provides: "Duplicate-order prevention key tests"
    - path: "tests/test_order_lifecycle_safety.py"
      provides: "Static safety tests"
  key_links:
    - from: "marketpilot/order_lifecycle.py"
      to: "marketpilot.risk.RiskDecision"
      via: "accepted risk decision input"
      pattern: "RiskDecision"
---

<objective>
Create order intent and lifecycle domain models with duplicate-order prevention.

Purpose: satisfy the lifecycle portions of RISK-03 and RISK-04 while preserving
QuantConnect as the future source of truth and keeping Phase 6 free of actual
Paper order submission.

Output: `marketpilot/order_lifecycle.py`, lifecycle tests, idempotency tests, and
docs. This plan implements D-09 through D-12.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-CONTEXT.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-RESEARCH.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-PATTERNS.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-01-PLAN.md
@marketpilot/ranking.py
@marketpilot/scoring.py
@docs/scoring.md
@docs/safety.md
</context>

<interfaces>
- Consume accepted `RiskDecision` outputs from Plan 06-01 once available.
- Lifecycle objects may include future QuantConnect order IDs as optional
  mirrored fields, but those IDs cannot be fabricated as authoritative state.
- Idempotency key input must include symbol, strategy mode, primary setup,
  signal time, and portfolio epoch.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add order intent contracts and lifecycle state enum</name>
  <files>marketpilot/order_lifecycle.py, tests/test_order_lifecycle_contract.py</files>
  <action>Create tests first. Add frozen dataclasses/enums for `OrderIntent`, `OrderLifecycleState`, `OrderLifecycleEvent`, and rejection/cancel/close reason values. Include the exact states from D-09. `OrderIntent` must include candidate/risk identifiers, symbol, primary setup, strategy mode, signal time, portfolio epoch, requested quantity, entry assumption, stop/target placeholders, and audit metadata. It must not contain a method that submits an order.</action>
  <verify>
    <automated>python -m pytest tests/test_order_lifecycle_contract.py -q</automated>
  </verify>
  <acceptance_criteria>Lifecycle contracts exist, include D-09 states, and clearly mark local models as audit/intention records.</acceptance_criteria>
  <done>Order intent and lifecycle contracts are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement transition validation and lifecycle events</name>
  <files>marketpilot/order_lifecycle.py, tests/test_order_lifecycle_transitions.py</files>
  <action>Implement `transition_order_state()` or equivalent pure function. Test allowed transitions for planned to submitted/rejected/canceled, submitted to partially_filled/filled/rejected/canceled, filled to protective_orders_pending/open, open to partially_closed/closed, partially_closed to closed, and recovery-related restoration transitions. Test forbidden transitions such as closed back to open, rejected to filled, canceled to filled, and submitted without intent identity. Every transition returns an event with previous state, next state, timestamp, reason, source, and sanitized payload.</action>
  <verify>
    <automated>python -m pytest tests/test_order_lifecycle_transitions.py -q</automated>
  </verify>
  <acceptance_criteria>RISK-04 lifecycle states and transition rules are deterministic and auditable.</acceptance_criteria>
  <done>Lifecycle transition validation is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Add idempotency and safety documentation</name>
  <files>marketpilot/order_lifecycle.py, tests/test_order_idempotency.py, tests/test_order_lifecycle_safety.py, docs/order_lifecycle.md, docs/testing.md, docs/safety.md</files>
  <action>Implement `make_order_idempotency_key(symbol, strategy_mode, primary_setup, signal_time, portfolio_epoch)` using normalized stable components. Tests must prove the same logical signal produces the same key, different signal time/setup/mode/epoch produces a different key, and case/whitespace normalization is stable. Add safety tests scanning for forbidden order submission APIs, broker adapters, Telegram delivery, live deployment, and fake portfolio mutation. Document lifecycle, idempotency, QuantConnect authority, and Phase 8 deferral.</action>
  <verify>
    <automated>python -m pytest tests/test_order_lifecycle_contract.py tests/test_order_lifecycle_transitions.py tests/test_order_idempotency.py tests/test_order_lifecycle_safety.py -q</automated>
    <automated>python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>Duplicate-order prevention is deterministic and Phase 6 lifecycle code remains intent/events only.</acceptance_criteria>
  <done>Order lifecycle models are ready for exit and persistence plans.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Spoofing | Idempotency key | mitigate | Normalize all key parts and test same/different logical signal behavior. |
| T-06-06 | Tampering | Lifecycle transition | mitigate | Pure transition function rejects impossible state changes. |
| T-06-07 | Elevation of Privilege | Order intent | mitigate | Static tests prove no submit/cancel/replace APIs, broker adapters, or Paper deployment. |
| T-06-08 | Repudiation | Lifecycle event | mitigate | Preserve previous/next state, reason, source, timestamp, and sanitized payload. |
</threat_model>

<verification>
- `python -m pytest tests/test_order_lifecycle_contract.py tests/test_order_lifecycle_transitions.py tests/test_order_idempotency.py tests/test_order_lifecycle_safety.py -q`
- `python -m pytest -q`
</verification>

<success_criteria>
- RISK-03 duplicate prevention is implemented.
- RISK-04 lifecycle states and transitions are implemented.
- SCO-04 lifecycle event evidence is available for persistence.
- D-09 through D-12 are visible in code, tests, and docs.
</success_criteria>

<output>
Create `.planning/phases/06-portfolio-risk-and-order-lifecycle/06-02-SUMMARY.md` when done.
</output>

