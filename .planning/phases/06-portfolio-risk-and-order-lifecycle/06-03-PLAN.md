---
phase: 06-portfolio-risk-and-order-lifecycle
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - marketpilot/exits.py
  - config/exits.yaml
  - tests/test_exit_contract.py
  - tests/test_stops_targets.py
  - tests/test_partial_trailing_holding_period.py
  - tests/test_exit_regime_authority.py
  - tests/test_exit_safety.py
  - docs/exits.md
  - docs/configuration.md
  - docs/testing.md
  - docs/safety.md
autonomous: true
requirements:
  - SCO-04
  - RISK-03
  - RISK-06
must_haves:
  truths:
    - "D-13 and RISK-03: initial stops derive from setup evidence such as structural invalidation, swing low, or breakout level, with an ATR sanity cap."
    - "D-14 and RISK-03: initial target is at least 2R and target price is computed from risk per share."
    - "D-15 and RISK-03: partial exits are modeled/configurable as events only, such as partial at 1R or 2R; execution remains later."
    - "D-16 and RISK-03: trailing stop is modeled/configurable but disabled by default until validation."
    - "D-17 and RISK-06: exit obligations remain authoritative for existing positions even if market regime later turns RISK_OFF."
    - "Exit models must emit audit evidence for SCO-04 and must not submit orders, mutate QuantConnect state, or send notifications."
  artifacts:
    - path: "marketpilot/exits.py"
      provides: "Stop, target, partial exit, trailing exit, max holding period, and exit obligation models"
      exports: ["ExitPlan", "StopModel", "TargetModel", "PartialExitRule", "TrailingStopPolicy", "HoldingPeriodPolicy", "build_exit_plan"]
    - path: "config/exits.yaml"
      provides: "Exit defaults and disabled unsafe behavior flags"
    - path: "tests/test_stops_targets.py"
      provides: "Structural stop, ATR cap, 2R target, and invalid exit rejection tests"
    - path: "tests/test_exit_regime_authority.py"
      provides: "RISK_OFF does not remove existing exit obligations tests"
---

<objective>
Create stop, target, partial-exit, trailing-exit, and holding-period models.

Purpose: satisfy RISK-03 and RISK-06 by creating authoritative exit obligations
for existing positions while keeping all behavior modeled/events-only until
later QuantConnect Paper execution.

Output: `config/exits.yaml`, `marketpilot/exits.py`, exit tests, and exit docs.
This plan implements D-13 through D-17.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-CONTEXT.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-RESEARCH.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-PATTERNS.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-01-PLAN.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-02-PLAN.md
@marketpilot/order_lifecycle.py
@marketpilot/risk.py
@marketpilot/setups/base.py
@docs/safety.md
</context>

<interfaces>
- Exit plans attach to order intents and open position mirrors, not real
  QuantConnect order tickets.
- Stop/target calculations must use explicit entry price and stop evidence.
- Regime input can block new entries but cannot cancel existing exit
  obligations.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add exit config and contracts</name>
  <files>config/exits.yaml, marketpilot/exits.py, tests/test_exit_contract.py, docs/configuration.md</files>
  <action>Create contract tests first. Add `config/exits.yaml` with `paper_trading_only: true`, structural stop policy, ATR sanity cap, minimum target R multiple 2.0, partial exit rules modeled but execution disabled, trailing stop disabled by default, max holding period policy, and disabled unsafe behavior flags. Add frozen dataclasses/enums for `ExitPlan`, `StopModel`, `TargetModel`, `PartialExitRule`, `TrailingStopPolicy`, and `HoldingPeriodPolicy`.</action>
  <verify>
    <automated>python -m pytest tests/test_exit_contract.py -q</automated>
  </verify>
  <acceptance_criteria>Exit config and models exist and prove trailing exits are disabled by default.</acceptance_criteria>
  <done>Exit contracts are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement initial stops and targets</name>
  <files>marketpilot/exits.py, tests/test_stops_targets.py</files>
  <action>Implement `build_exit_plan()` or equivalent helper that chooses initial stop from setup evidence keys such as structural invalidation, swing low, breakout level, or explicit stop evidence. Apply ATR sanity cap and reject invalid stop distance. Compute target price from risk per share and minimum 2R. Tests must cover Trend Pullback-style swing-low stop, Volume Breakout-style breakout-level stop, invalid stop rejection, ATR cap rejection, and 2R target calculation.</action>
  <verify>
    <automated>python -m pytest tests/test_stops_targets.py -q</automated>
  </verify>
  <acceptance_criteria>Stops and targets are deterministic, auditable, and fail closed on invalid data.</acceptance_criteria>
  <done>Initial stop and target modeling is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Add partial exits, trailing policy, holding period, regime authority, and docs</name>
  <files>marketpilot/exits.py, tests/test_partial_trailing_holding_period.py, tests/test_exit_regime_authority.py, tests/test_exit_safety.py, docs/exits.md, docs/testing.md, docs/safety.md</files>
  <action>Model partial exits as planned domain events only. Add configurable partial rules such as partial at 1R or 2R, but do not execute them. Add trailing stop policy object disabled by default. Add maximum holding period model with audit event output. Add tests proving `RISK_OFF` does not remove existing exit obligations and only affects new entries. Add safety scans and docs.</action>
  <verify>
    <automated>python -m pytest tests/test_exit_contract.py tests/test_stops_targets.py tests/test_partial_trailing_holding_period.py tests/test_exit_regime_authority.py tests/test_exit_safety.py -q</automated>
    <automated>python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>RISK-03 and RISK-06 exit obligations are modeled and tested without execution behavior.</acceptance_criteria>
  <done>Exit modeling is ready for persistence and notification-domain events.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | Stop evidence | mitigate | Missing/invalid/inverted stop evidence rejects fail-closed. |
| T-06-10 | Information Disclosure | Exit payloads | mitigate | Events must use sanitized payloads and no secrets. |
| T-06-11 | Elevation of Privilege | Exit model | mitigate | Static tests prove no order submission, cancellation, or Telegram delivery. |
| T-06-12 | Denial of Service | Regime change | mitigate | Tests prove RISK_OFF cannot erase existing exit obligations. |
</threat_model>

<verification>
- `python -m pytest tests/test_exit_contract.py tests/test_stops_targets.py tests/test_partial_trailing_holding_period.py tests/test_exit_regime_authority.py tests/test_exit_safety.py -q`
- `python -m pytest -q`
</verification>

<success_criteria>
- RISK-03 stop, target, partial, trailing, and holding-period modeling is covered.
- RISK-06 regime-independent existing-position exits are covered.
- D-13 through D-17 are visible in code, tests, and docs.
</success_criteria>

<output>
Create `.planning/phases/06-portfolio-risk-and-order-lifecycle/06-03-SUMMARY.md` when done.
</output>

