---
phase: 06-portfolio-risk-and-order-lifecycle
plan: "01"
type: execute
wave: 1
depends_on:
  - "05-03"
files_modified:
  - marketpilot/risk.py
  - config/risk.yaml
  - tests/test_risk_contract.py
  - tests/test_position_sizing.py
  - tests/test_portfolio_constraints.py
  - tests/test_risk_safety.py
  - docs/risk_management.md
  - docs/configuration.md
  - docs/testing.md
  - docs/safety.md
autonomous: true
requirements:
  - SCO-04
  - RISK-01
  - RISK-02
must_haves:
  truths:
    - "D-01 and RISK-01: default per-trade risk budget is 1% of simulated equity."
    - "D-02 and RISK-01: default maximum open positions is 10."
    - "D-03 and RISK-01: default maximum sector exposure is 30% of simulated equity."
    - "D-04 and RISK-01: default maximum new entries per Paper trading day is 3."
    - "D-05 and RISK-02: position sizing uses risk_amount / stop_distance."
    - "D-06 and RISK-02: missing, zero, negative, non-finite, or invalid stop distance rejects fail-closed."
    - "D-07 and RISK-02: default maximum allocation per position is 15% of simulated equity."
    - "D-08 and RISK-02: insufficient cash may reduce quantity only if minimum reward/risk and minimum quantity remain valid; otherwise reject."
    - "Phase 6 consumes `RankedCandidate` as audit evidence only and must not treat BUY_CANDIDATE, WATCH, or score values as order instructions."
    - "Risk decisions must be auditable for SCO-04 and must not submit orders, mutate QuantConnect state, send Telegram messages, or fabricate portfolio values."
  artifacts:
    - path: "marketpilot/risk.py"
      provides: "Risk config, portfolio snapshot, risk decision, rejection reasons, position sizing, and portfolio constraint validation"
      exports: ["RiskConfig", "PortfolioSnapshot", "PositionSizingDecision", "RiskDecision", "evaluate_portfolio_risk", "load_risk_config"]
    - path: "config/risk.yaml"
      provides: "Paper-only risk defaults and disabled unsafe behavior flags"
      contains: "per_trade_risk_pct: 1.0, max_open_positions: 10, max_sector_exposure_pct: 30.0, max_new_entries_per_day: 3, max_position_allocation_pct: 15.0"
    - path: "tests/test_risk_contract.py"
      provides: "Risk config and contract safety tests"
    - path: "tests/test_position_sizing.py"
      provides: "Risk amount, stop distance, allocation, cash, reward/risk, and quantity tests"
    - path: "tests/test_portfolio_constraints.py"
      provides: "Position count, sector exposure, daily entry, and cash constraint tests"
    - path: "tests/test_risk_safety.py"
      provides: "Static scan proving no order submission, Telegram delivery, broker adapter, live behavior, or fake portfolio authority"
  key_links:
    - from: "marketpilot/risk.py"
      to: "marketpilot.ranking.RankedCandidate"
      via: "candidate evidence input"
      pattern: "RankedCandidate"
    - from: "marketpilot/risk.py"
      to: "config/risk.yaml"
      via: "load_risk_config"
      pattern: "yaml.safe_load"
---

<objective>
Create portfolio constraints and position sizing as paper-only domain decisions.

Purpose: satisfy RISK-01, RISK-02, and the decision-recording portion of SCO-04
by producing explicit risk decisions from ranked audit candidates, stop/target
inputs, cash/allocation/sector/count state, and daily-entry state without order
submission or portfolio mutation.

Output: `config/risk.yaml`, `marketpilot/risk.py`, risk tests, and risk docs.
This plan implements D-01 through D-08.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-CONTEXT.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-RESEARCH.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-PATTERNS.md
@.planning/phases/05-relative-strength-and-unified-scoring/05-CONTEXT.md
@.planning/phases/05-relative-strength-and-unified-scoring/05-03-SUMMARY.md
@marketpilot/models.py
@marketpilot/ranking.py
@marketpilot/scoring.py
@docs/scoring.md
@docs/safety.md
</context>

<interfaces>
- Input candidate boundary is `marketpilot.ranking.RankedCandidate`.
- `RankedCandidate.classification` and score values are evidence only.
- Position sizing requires explicit entry price, stop price or stop distance,
  reward/risk input, simulated equity, available cash, current open positions,
  sector exposure, and daily entry count.
- Use exact Decimal arithmetic where money/risk amounts are represented.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add risk config, contracts, and fail-closed config tests</name>
  <files>config/risk.yaml, marketpilot/risk.py, tests/test_risk_contract.py, docs/configuration.md</files>
  <action>Create `tests/test_risk_contract.py` first. Add `config/risk.yaml` with `paper_trading_only: true`, D-01 through D-08 defaults, minimum reward/risk, minimum quantity, and disabled behavior flags for order submission, broker adapters, Telegram delivery, live deployment, fake portfolio, leverage, margin, short selling, and real-money paths. Add `RiskConfig`, `RiskRejectionReason`, `PortfolioSnapshot`, and `load_risk_config()` in `marketpilot/risk.py`. The loader must use `yaml.safe_load`, require mapping data, validate positive thresholds, validate percentages are within safe bounds, and fail closed on unsafe disabled behavior flags.</action>
  <verify>
    <automated>python --version</automated>
    <automated>python -m pytest tests/test_risk_contract.py -q</automated>
  </verify>
  <acceptance_criteria>Risk config loads safely, defaults match D-01 through D-08, and unsafe config fails closed.</acceptance_criteria>
  <done>Risk config and contract tests are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement risk-based position sizing</name>
  <files>marketpilot/risk.py, tests/test_position_sizing.py</files>
  <action>Create sizing tests before implementation. Implement `calculate_position_size()` or equivalent decision helper that calculates risk amount as simulated equity times 1%, computes quantity from `risk_amount / stop_distance`, caps by 15% allocation, caps by available cash, and rejects zero or invalid quantity. Missing, zero, negative, non-finite, or inverted stop distance must reject fail-closed. If cash is insufficient, reduced quantity is allowed only when minimum reward/risk and minimum quantity remain valid; otherwise reject. Do not use score-scaled sizing.</action>
  <verify>
    <automated>python -m pytest tests/test_position_sizing.py -q</automated>
  </verify>
  <acceptance_criteria>Sizing tests cover valid sizing, invalid stop distance, allocation cap, cash cap, insufficient cash rejection, minimum quantity, and minimum reward/risk.</acceptance_criteria>
  <done>Risk-based position sizing is deterministic and fail-closed.</done>
</task>

<task type="auto">
  <name>Task 3: Implement portfolio constraints and audit-safe decisions</name>
  <files>marketpilot/risk.py, tests/test_portfolio_constraints.py, tests/test_risk_safety.py, docs/risk_management.md, docs/testing.md, docs/safety.md</files>
  <action>Implement `evaluate_portfolio_risk()` or equivalent function returning a frozen `RiskDecision` with accepted/rejected status, calculated quantity, risk amount, allocation amount, rejection reasons, candidate identifiers, and audit evidence. Enforce max open positions 10, max sector exposure 30%, max new entries per day 3, cash constraints, allocation cap, and per-trade risk. Add tests proving classifications are not instructions and risk decisions do not submit orders. Add docs for risk management, configuration, testing, and safety.</action>
  <verify>
    <automated>python -m pytest tests/test_risk_contract.py tests/test_position_sizing.py tests/test_portfolio_constraints.py tests/test_risk_safety.py -q</automated>
    <automated>python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>RISK-01 and RISK-02 are covered by code, tests, and docs; SCO-04 risk decision evidence is present; no order submission or fake portfolio authority exists.</acceptance_criteria>
  <done>Portfolio risk and sizing are ready for order-intent lifecycle planning.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `config/risk.yaml` | mitigate | Loader rejects unsafe paper settings, invalid percentages, and disabled behavior flags that are not false. |
| T-06-02 | Spoofing | `RankedCandidate` input | mitigate | Treat candidate classification as audit evidence only; require explicit risk/stop/cash/allocation constraints. |
| T-06-03 | Elevation of Privilege | Risk decision output | mitigate | Static tests prove no order submission, broker adapters, Telegram delivery, Paper deployment, live behavior, leverage, margin, or fake portfolio authority. |
| T-06-04 | Repudiation | Risk rejection | mitigate | Preserve rejection reasons, numeric values, config version placeholders, and timestamps for audit. |
</threat_model>

<verification>
- `python --version`
- `python -m pytest tests/test_risk_contract.py tests/test_position_sizing.py tests/test_portfolio_constraints.py tests/test_risk_safety.py -q`
- `python -m pytest -q`
</verification>

<success_criteria>
- RISK-01 and RISK-02 are implemented and tested.
- SCO-04 has risk decision evidence ready for audit journaling.
- D-01 through D-08 are visible in code, tests, and docs.
- No order submission, Paper deployment, Telegram delivery, broker integration, leverage, margin, or fake portfolio behavior is introduced.
</success_criteria>

<output>
Create `.planning/phases/06-portfolio-risk-and-order-lifecycle/06-01-SUMMARY.md` when done.
</output>

