---
phase: 06-portfolio-risk-and-order-lifecycle
plan: "05"
type: execute
wave: 5
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - marketpilot/notification_events.py
  - tests/test_notification_events.py
  - tests/test_notification_fake_transport.py
  - tests/test_notification_dedup_rate_limit.py
  - tests/test_notification_safety.py
  - docs/notification_events.md
  - docs/testing.md
  - docs/safety.md
autonomous: true
requirements:
  - SCO-04
  - RISK-07
must_haves:
  truths:
    - "D-22 and RISK-07: Phase 6 emits typed events for risk rejection, sizing decision, order intent, lifecycle transition, stop/target update, partial close, full close, and recovery mismatch."
    - "D-23: notification events are typed domain dataclasses with stable event_type and sanitized payloads; they are not Telegram message strings."
    - "D-24 and RISK-07: future transport delivery failures must never block risk logic, order safety, protective exits, or lifecycle progression."
    - "D-25: Phase 6 includes fake transport/collector for deterministic tests only; no real Telegram, tokens, chat IDs, network calls, or delivery behavior."
    - "Notification-domain events must be journal-friendly for SCO-04 and transport-neutral for Phase 8."
  artifacts:
    - path: "marketpilot/notification_events.py"
      provides: "Typed notification-domain events, fake collector, deduplication, and rate limiting"
      exports: ["NotificationDomainEvent", "FakeNotificationCollector", "NotificationDeduplicator", "NotificationRateLimiter"]
    - path: "tests/test_notification_events.py"
      provides: "Typed event and payload sanitization tests"
    - path: "tests/test_notification_fake_transport.py"
      provides: "Fake collector and delivery-failure isolation tests"
    - path: "tests/test_notification_dedup_rate_limit.py"
      provides: "Deduplication and rate-limit tests"
---

<objective>
Create transport-neutral notification-domain events and deterministic fake
transport tests.

Purpose: satisfy RISK-07 and the alert portion of SCO-04 while preserving the
rule that notifications can observe safety decisions but never control them.

Output: `marketpilot/notification_events.py`, notification-domain tests, and
docs. This plan implements D-22 through D-25.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-CONTEXT.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-RESEARCH.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-PATTERNS.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-01-PLAN.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-02-PLAN.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-03-PLAN.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-04-PLAN.md
@docs/safety.md
</context>

<interfaces>
- Events should be creatable from risk decisions, order lifecycle events, exit
  updates, audit journal records, and recovery mismatches.
- Events must expose stable `event_type`, timestamp, severity/category,
  correlation ID, and sanitized payload.
- Fake collector stores events in memory for tests and can simulate delivery
  failure without raising into risk/order safety logic.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add typed notification-domain events</name>
  <files>marketpilot/notification_events.py, tests/test_notification_events.py</files>
  <action>Create tests first. Implement `NotificationDomainEvent` and event factory helpers for risk rejection, sizing decision, order intent, lifecycle transition, stop/target update, partial close, full close, and recovery mismatch. Payloads must be sanitized and must not contain secrets, tokens, chat IDs, credentials, or raw exception strings that could leak sensitive data. Tests must prove stable `event_type` values.</action>
  <verify>
    <automated>python -m pytest tests/test_notification_events.py -q</automated>
  </verify>
  <acceptance_criteria>D-22 and D-23 typed events exist and are transport-neutral.</acceptance_criteria>
  <done>Notification-domain event contracts are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Add fake collector and delivery-failure isolation</name>
  <files>marketpilot/notification_events.py, tests/test_notification_fake_transport.py</files>
  <action>Implement `FakeNotificationCollector` or equivalent deterministic test transport. It should collect events, optionally simulate failure, and expose failure status without throwing into risk/order/exit safety flows. Tests must prove simulated transport failure does not block or mutate upstream safety decisions.</action>
  <verify>
    <automated>python -m pytest tests/test_notification_fake_transport.py -q</automated>
  </verify>
  <acceptance_criteria>D-24 and D-25 failure isolation is testable without real Telegram.</acceptance_criteria>
  <done>Fake notification transport is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Add deduplication, rate limiting, safety scans, and docs</name>
  <files>marketpilot/notification_events.py, tests/test_notification_dedup_rate_limit.py, tests/test_notification_safety.py, docs/notification_events.md, docs/testing.md, docs/safety.md</files>
  <action>Add pure in-memory deduplication and rate-limit helpers for domain events. They must be deterministic, testable, and transport-neutral. Add tests proving duplicate event suppression and rate-limit decisions are audit-safe and do not control risk/order safety logic. Add static safety tests proving no Telegram API calls, tokens, chat IDs, network calls, credentials, order submission, or Paper deployment. Document Phase 8 handoff.</action>
  <verify>
    <automated>python -m pytest tests/test_notification_events.py tests/test_notification_fake_transport.py tests/test_notification_dedup_rate_limit.py tests/test_notification_safety.py -q</automated>
    <automated>python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>RISK-07 notification-domain behavior is implemented and ready for later Telegram delivery integration.</acceptance_criteria>
  <done>Phase 6 notification-domain events are complete.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Information Disclosure | Event payload | mitigate | Sanitize payloads and test no secrets/tokens/chat IDs leak. |
| T-06-18 | Denial of Service | Notification transport | mitigate | Fake failure never blocks risk/order/exit safety decisions. |
| T-06-19 | Tampering | Dedup/rate limiting | mitigate | Dedup/rate limit only affects notification emission, not safety logic. |
| T-06-20 | Elevation of Privilege | Notification module | mitigate | Static tests prove no Telegram delivery, network calls, or order control. |
</threat_model>

<verification>
- `python -m pytest tests/test_notification_events.py tests/test_notification_fake_transport.py tests/test_notification_dedup_rate_limit.py tests/test_notification_safety.py -q`
- `python -m pytest -q`
</verification>

<success_criteria>
- RISK-07 typed notification-domain events are implemented and tested.
- SCO-04 alert/audit event coverage exists.
- D-22 through D-25 are visible in code, tests, and docs.
- Real Telegram delivery remains deferred to Phase 8.
</success_criteria>

<output>
Create `.planning/phases/06-portfolio-risk-and-order-lifecycle/06-05-SUMMARY.md` when done.
</output>

