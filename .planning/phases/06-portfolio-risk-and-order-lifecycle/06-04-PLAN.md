---
phase: 06-portfolio-risk-and-order-lifecycle
plan: "04"
type: execute
wave: 4
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - marketpilot/audit_journal.py
  - marketpilot/recovery.py
  - tests/test_audit_journal.py
  - tests/test_restart_recovery.py
  - tests/test_split_delisting_placeholders.py
  - tests/test_persistence_safety.py
  - docs/audit_journal.md
  - docs/recovery.md
  - docs/safety.md
  - docs/testing.md
autonomous: true
requirements:
  - SCO-04
  - RISK-04
  - RISK-05
must_haves:
  truths:
    - "D-18 and SCO-04: persist an append-only audit journal of intents, decisions, lifecycle events, config versions, strategy versions, and recovery context."
    - "D-19: audit journal format is JSONL append-only."
    - "D-20 and RISK-04: on restart mismatch, QuantConnect wins; local state is marked mismatched and a recovery event is emitted."
    - "D-21 and RISK-05: split and delisting handling is modeled through safe placeholders, rejection, and recovery states until QuantConnect integration is verified."
    - "Persistence must not become an authoritative portfolio store and must not fabricate QuantConnect cash, holdings, orders, fills, or Paper state."
  artifacts:
    - path: "marketpilot/audit_journal.py"
      provides: "Append-only JSONL audit record contracts and writer/reader helpers"
      exports: ["AuditJournalRecord", "AppendOnlyJsonlAuditJournal"]
    - path: "marketpilot/recovery.py"
      provides: "Restart mismatch and corporate-action placeholder recovery contracts"
      exports: ["RecoveryDecision", "RecoveryMismatch", "CorporateActionPlaceholder"]
    - path: "tests/test_restart_recovery.py"
      provides: "QuantConnect-wins mismatch tests"
    - path: "tests/test_split_delisting_placeholders.py"
      provides: "Safe split/delisting placeholder tests"
---

<objective>
Create append-only persistence, restart recovery contracts, and safe corporate
action placeholders.

Purpose: satisfy SCO-04, RISK-04 restoration expectations, and RISK-05 by
recording Phase 6 decisions/events and modeling safe recovery behavior while
keeping QuantConnect authoritative.

Output: `marketpilot/audit_journal.py`, `marketpilot/recovery.py`, persistence
tests, and recovery docs. This plan implements D-18 through D-21.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-CONTEXT.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-RESEARCH.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-PATTERNS.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-01-PLAN.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-02-PLAN.md
@.planning/phases/06-portfolio-risk-and-order-lifecycle/06-03-PLAN.md
@docs/safety.md
</context>

<interfaces>
- Accept risk decisions, order lifecycle events, and exit events as audit input.
- Journal records are append-only JSONL and include event type, timestamp,
  correlation IDs, config/version hashes or placeholders, and sanitized payload.
- Recovery compares local mirror snapshots to future QuantConnect snapshots; in
  conflicts, QuantConnect wins.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add JSONL audit journal contracts and tests</name>
  <files>marketpilot/audit_journal.py, tests/test_audit_journal.py, docs/audit_journal.md</files>
  <action>Create tests first. Implement `AuditJournalRecord` and `AppendOnlyJsonlAuditJournal` with append-only write behavior, JSON serialization, deterministic read-back, schema validation, timestamp/correlation fields, sanitized payloads, config version placeholder, and strategy version placeholder. Tests must prove records append in order, invalid records fail closed, secrets are sanitized, and existing files are not rewritten destructively.</action>
  <verify>
    <automated>python -m pytest tests/test_audit_journal.py -q</automated>
  </verify>
  <acceptance_criteria>JSONL append-only journal exists and preserves SCO-04 audit records without becoming a portfolio store.</acceptance_criteria>
  <done>Audit journal contracts are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Add restart recovery and QuantConnect-wins mismatch handling</name>
  <files>marketpilot/recovery.py, tests/test_restart_recovery.py, docs/recovery.md</files>
  <action>Implement recovery contracts for local mirror state, future QuantConnect snapshot input, mismatch classification, and recovery event output. Tests must prove QuantConnect values win for order state, fills, holdings, cash, and open positions; local state is marked mismatched; and a recovery event is emitted for the audit journal. Do not call QuantConnect APIs in Phase 6.</action>
  <verify>
    <automated>python -m pytest tests/test_restart_recovery.py -q</automated>
  </verify>
  <acceptance_criteria>Restart restoration behavior is modeled and tested with QuantConnect authority preserved.</acceptance_criteria>
  <done>Recovery mismatch handling is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Add split/delisting placeholders, safety tests, and docs</name>
  <files>marketpilot/recovery.py, tests/test_split_delisting_placeholders.py, tests/test_persistence_safety.py, docs/recovery.md, docs/testing.md, docs/safety.md</files>
  <action>Add safe placeholder types for split, delisting, and unsupported corporate action events. They should produce rejection/recovery states and audit events, not full execution behavior. Add safety scans proving no fake portfolio, no QuantConnect state fabrication, no credential use, no real API calls, and no destructive persistence. Document deferral to QuantConnect verification.</action>
  <verify>
    <automated>python -m pytest tests/test_audit_journal.py tests/test_restart_recovery.py tests/test_split_delisting_placeholders.py tests/test_persistence_safety.py -q</automated>
    <automated>python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>SCO-04, RISK-04, and RISK-05 persistence/recovery behavior is deterministic, safe, and documented.</acceptance_criteria>
  <done>Persistence and recovery are ready for notification-domain events and later Phase 8 integration.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Repudiation | Audit journal | mitigate | Append-only JSONL preserves event type, timestamp, correlation IDs, and sanitized payload. |
| T-06-14 | Tampering | Journal file | mitigate | Tests prove append-only behavior and validation of malformed records. |
| T-06-15 | Spoofing | Local mirror state | mitigate | Recovery logic marks mismatches and lets QuantConnect win. |
| T-06-16 | Elevation of Privilege | Persistence | mitigate | Static tests prove local files are not authoritative portfolio state and no QuantConnect values are fabricated. |
</threat_model>

<verification>
- `python -m pytest tests/test_audit_journal.py tests/test_restart_recovery.py tests/test_split_delisting_placeholders.py tests/test_persistence_safety.py -q`
- `python -m pytest -q`
</verification>

<success_criteria>
- SCO-04 audit journal coverage is implemented.
- RISK-04 restart restoration is modeled with QuantConnect authority.
- RISK-05 split/delisting safe placeholders are implemented.
- D-18 through D-21 are visible in code, tests, and docs.
</success_criteria>

<output>
Create `.planning/phases/06-portfolio-risk-and-order-lifecycle/06-04-SUMMARY.md` when done.
</output>

