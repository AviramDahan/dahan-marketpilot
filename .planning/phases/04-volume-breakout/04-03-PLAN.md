---
phase: 04-volume-breakout
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - marketpilot/setups/volume_breakout.py
  - tests/test_volume_breakout_explanations.py
  - tests/test_volume_breakout_safety.py
  - docs/volume_breakout.md
  - docs/testing.md
  - docs/safety.md
autonomous: true
requirements:
  - SET-04
user_setup: []
must_haves:
  truths:
    - "D-13: Every Volume Breakout result exposes numeric evidence for resistance, lookback, buffer, close, volume ratio, EMA20 extension, ATR%, reward/risk proxy, and regime."
    - "D-14: Volume Breakout returns valid/rejected status, evidence, explanations, and rejection reasons only; it does not implement full MarketPilot Score or BUY/WATCH/AVOID classification."
    - "D-15: Production setup files contain no orders, sizing, portfolio state, backtest result creation, Telegram delivery, live/Paper deployment, credentials, or fake performance behavior."
    - "D-16: Documentation and tests describe Volume Breakout as a module parallel to Trend Pullback, not a broad setup-framework refactor."
  artifacts:
    - path: "tests/test_volume_breakout_explanations.py"
      provides: "Evidence completeness and explanation tests"
      contains: "test_valid_result_includes_breakout_evidence_components"
    - path: "tests/test_volume_breakout_safety.py"
      provides: "Forbidden behavior and setup-result-only tests"
      contains: "test_volume_breakout_production_files_contain_no_forbidden_behavior"
    - path: "docs/volume_breakout.md"
      provides: "Volume Breakout contract, rules, evidence, and boundaries"
      contains: "There is no BUY, WATCH, or AVOID output."
    - path: "docs/testing.md"
      provides: "Phase 4 deterministic test documentation"
      contains: "Phase 4 Volume Breakout"
    - path: "docs/safety.md"
      provides: "Safety documentation synchronized with setup-only behavior"
      contains: "Volume Breakout"
  key_links:
    - from: "tests/test_volume_breakout_explanations.py"
      to: "marketpilot/setups/volume_breakout.py"
      via: "evaluate_volume_breakout evidence assertions"
      pattern: "evaluate_volume_breakout"
    - from: "tests/test_volume_breakout_safety.py"
      to: "marketpilot/setups/volume_breakout.py"
      via: "static forbidden-behavior scan"
      pattern: "PRODUCTION_FILES"
    - from: "docs/volume_breakout.md"
      to: "config/volume_breakout.yaml"
      via: "documented default configuration"
      pattern: "config/volume_breakout.yaml"
---

<objective>
Complete Volume Breakout explanation, evidence-component, documentation, and safety coverage without adding full scoring or classifications.

Purpose: Phase 4 needs future-consumable evidence components for Phase 5, but D-14 explicitly forbids adding MarketPilot Score, confidence, ranking, or BUY/WATCH/AVOID classifications here. This plan makes SET-04 outputs auditable and documented while preserving D-15 safety boundaries.
Output: explanation/evidence completeness tests, safety tests, `docs/volume_breakout.md`, and synchronized testing/safety documentation.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-volume-breakout/04-CONTEXT.md
@.planning/phases/04-volume-breakout/04-RESEARCH.md
@.planning/phases/04-volume-breakout/04-02-SUMMARY.md
@marketpilot/setups/base.py
@marketpilot/setups/volume_breakout.py
@tests/test_trend_pullback_explanations.py
@tests/test_trend_pullback_safety.py
@docs/trend_pullback.md
@docs/testing.md
@docs/safety.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add evidence completeness and explanation tests</name>
  <files>marketpilot/setups/volume_breakout.py, tests/test_volume_breakout_explanations.py</files>
  <read_first>marketpilot/setups/volume_breakout.py, tests/test_trend_pullback_explanations.py, .planning/phases/04-volume-breakout/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: A valid result includes evidence names `resistance_lookback_bars`, `prior_resistance`, `breakout_buffer_pct`, `buffered_resistance`, `breakout_close`, `volume_ratio`, `ema20_extension_pct`, `atr_pct`, `average_dollar_volume`, `reward_risk_proxy`, and `regime` per D-13.
    - Test 2: A rejected result includes rejection explanations and evidence with `passed is False` for the failed gate per D-13/D-14.
    - Test 3: Result evidence names do not include `total_score`, `confidence`, `ranking`, `classification`, `BUY`, `WATCH`, or `AVOID` per D-14/D-15.
  </behavior>
  <action>Add `tests/test_volume_breakout_explanations.py` by mirroring the Trend Pullback explanation tests while using Volume Breakout fixtures from detection tests. If existing explanations are too sparse, update `_explain()` or equivalent result-building logic in `marketpilot/setups/volume_breakout.py` so valid results state they are valid on completed daily-bar breakout evidence and rejected results include `Rejected: {reason.value}.` lines. Treat "scoring components" only as evidence names that Phase 5 may consume later; do not add `total_score`, confidence, rank, classification, score thresholds, score weights, BUY/WATCH/AVOID labels, or candidate ranking.</action>
  <verify>
    <automated>python -m pytest tests/test_volume_breakout_explanations.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Evidence completeness covers every D-13 required numeric field.
    - Rejected explanations include the actual rejection reason value.
    - Tests prove no full score, confidence, ranking, or classification field exists.
  </acceptance_criteria>
  <done>Volume Breakout evidence is complete enough for audit and later scoring consumption without implementing Phase 5 scoring behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add safety tests and synchronize documentation</name>
  <files>tests/test_volume_breakout_safety.py, docs/volume_breakout.md, docs/testing.md, docs/safety.md</files>
  <read_first>tests/test_trend_pullback_safety.py, docs/trend_pullback.md, docs/testing.md, docs/safety.md, .planning/phases/04-volume-breakout/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Static scan of `marketpilot/setups/base.py` and `marketpilot/setups/volume_breakout.py` rejects forbidden strings for BUY/WATCH/AVOID classifications, order APIs, Telegram, fake backtests, Paper/Live deployment, credentials, tokens, and passwords per D-15.
    - Test 2: A valid Volume Breakout result has no `order`, `quantity`, `portfolio_weight`, `telegram_message`, `backtest_result`, `classification`, or `total_score` attributes per D-14/D-15.
    - Test 3: Documentation states current-bar exclusion, close-based breakout confirmation, volume confirmation, SET-04 hard gates, evidence fields, and deferred boundaries without performance claims or fake results.
  </behavior>
  <action>Create `tests/test_volume_breakout_safety.py` using the Trend Pullback safety pattern and include `marketpilot/setups/base.py` plus `marketpilot/setups/volume_breakout.py` in `PRODUCTION_FILES`. Create `docs/volume_breakout.md` in English with sections for contract, default config, result vocabulary, evidence and explanations, fixture design, detection rules, rejection rules, deferred boundaries, and handoff notes. Update `docs/testing.md` to list Phase 4 Volume Breakout tests and offline constraints. Update `docs/safety.md` to state that Volume Breakout remains setup evidence only and must not contain orders, sizing, portfolio state, backtest results, Telegram delivery, live/Paper deployment, credentials, fake performance, or profitability claims. Do not document any fake backtest result, generated performance number, same-close fill assumption, real order path, or credential example.</action>
  <verify>
    <automated>python -m pytest tests/test_volume_breakout_explanations.py tests/test_volume_breakout_safety.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `docs/volume_breakout.md` contains the sentence `There is no BUY, WATCH, or AVOID output.`
    - `docs/testing.md` lists the five Volume Breakout test files.
    - `docs/safety.md` mentions Volume Breakout setup-only behavior and preserves the existing paper-only disclaimer.
  </acceptance_criteria>
  <done>Volume Breakout has complete explanation, safety, and documentation coverage while staying inside Phase 4 boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluator result -> future scoring | Numeric evidence will later be consumed by Phase 5 and must not masquerade as a final score here. |
| docs/tests -> user expectations | Documentation and tests must not imply profitability, same-close fills, live deployment, or trading authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Repudiation | `SetupResult.explanation` | mitigate | Tests require readable valid/rejected explanations tied to exact rejection reasons. |
| T-04-08 | Information Disclosure | docs and tests | mitigate | Safety tests and docs forbid credential/token/password strings and external service setup. |
| T-04-09 | Elevation of Privilege | future-consumable evidence | mitigate | Tests assert no total score, confidence, ranking, classification, BUY/WATCH/AVOID, orders, sizing, portfolio state, backtests, Telegram, or deployment behavior. |
| T-04-SC | Tampering | package installs | mitigate | No package install is planned; if an executor introduces npm/pip/cargo installs, stop for package legitimacy review first. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_volume_breakout_contract.py tests/test_volume_breakout_detection.py tests/test_volume_breakout_rejections.py tests/test_volume_breakout_explanations.py tests/test_volume_breakout_safety.py -x`
- [ ] `python -m pytest`
- [ ] Static safety scan in `tests/test_volume_breakout_safety.py` passes.
- [ ] Documentation contains no fake performance, fake portfolio values, profitability claims, credential examples, order instructions, Telegram delivery behavior, or live/Paper deployment instructions.
</verification>

<success_criteria>
- D-13 evidence completeness is verified for valid and rejected results.
- D-14 is enforced by tests: no MarketPilot Score, classification, confidence, or ranking behavior.
- D-15 is enforced by static and behavioral safety tests.
- D-16 is reflected in docs and tests as a Volume Breakout module parallel to Trend Pullback.
- SET-04 is documented and test-covered without adding out-of-scope trading behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-volume-breakout/04-03-SUMMARY.md`.
</output>
