---
phase: 04-volume-breakout
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - marketpilot/setups/base.py
  - marketpilot/setups/volume_breakout.py
  - config/volume_breakout.yaml
  - tests/test_volume_breakout_contract.py
autonomous: true
requirements:
  - SET-03
user_setup: []
must_haves:
  truths:
    - "SET-03/D-01/D-02: Prior resistance uses a configurable completed daily-bar lookback and excludes the current bar."
    - "D-03/D-04: The contract distinguishes close-above-buffered-resistance breakout validity from intraday high movement."
    - "D-15/D-16: Volume Breakout has its own module/config/test artifacts parallel to Trend Pullback and contains no order, portfolio, classification, backtest, Telegram, or deployment behavior."
  artifacts:
    - path: "marketpilot/setups/base.py"
      provides: "Shared rejection vocabulary for Volume Breakout gates"
      contains: "BREAKOUT_NOT_CONFIRMED"
    - path: "marketpilot/setups/volume_breakout.py"
      provides: "Volume Breakout input/config contract and current-bar-excluded resistance helper"
      contains: "SETUP_NAME = \"volume_breakout\""
    - path: "config/volume_breakout.yaml"
      provides: "Fail-closed Volume Breakout defaults"
      contains: "lookback_bars: 20"
    - path: "tests/test_volume_breakout_contract.py"
      provides: "Contract and current-bar exclusion tests"
      contains: "test_prior_resistance_excludes_current_bar_high"
  key_links:
    - from: "tests/test_volume_breakout_contract.py"
      to: "marketpilot/setups/volume_breakout.py"
      via: "contract imports"
      pattern: "from marketpilot\\.setups\\.volume_breakout import"
    - from: "marketpilot/setups/volume_breakout.py"
      to: "config/volume_breakout.yaml"
      via: "DEFAULT_CONFIG_PATH and safe YAML loader"
      pattern: "volume_breakout\\.yaml"
---

<objective>
Create the Volume Breakout contract, configuration, rejection vocabulary, and prior-resistance helper required before full detection.

Purpose: SET-03 depends on an explicit no-look-ahead contract: prior resistance must be calculated from previous completed bars only, with D-01 current-bar exclusion, D-02 default 20 completed bars, D-03 close-based validity, and D-04 configurable breakout buffer locked before any evaluator can be trusted.
Output: `config/volume_breakout.yaml`, `marketpilot/setups/volume_breakout.py`, expanded `SetupRejectionReason` values, and contract tests that establish the D-15/D-16 evidence-only module boundary.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-volume-breakout/04-CONTEXT.md
@.planning/phases/04-volume-breakout/04-RESEARCH.md
@marketpilot/setups/base.py
@marketpilot/setups/trend_pullback.py
@config/trend_pullback.yaml
@tests/test_trend_pullback_contract.py
@tests/test_trend_pullback_safety.py
@docs/testing.md
@docs/safety.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Volume Breakout config and rejection vocabulary</name>
  <files>marketpilot/setups/base.py, marketpilot/setups/volume_breakout.py, config/volume_breakout.yaml, tests/test_volume_breakout_contract.py</files>
  <read_first>marketpilot/setups/base.py, config/trend_pullback.yaml, tests/test_trend_pullback_contract.py, .planning/phases/04-volume-breakout/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `load_volume_breakout_config()` returns `paper_trading_only: true`, `timing_mode: completed_daily_bar`, `resistance.lookback_bars == 20`, and `volume.min_volume_ratio == 1.5` per D-02/D-05/D-06.
    - Test 2: The config fails closed if `paper_trading_only` is not true or `disabled_behaviors.intrabar_validity` is not false per D-15.
    - Test 3: `SetupRejectionReason` includes Volume Breakout reasons for invalid prior resistance, unconfirmed breakout close, weak volume confirmation, excessive EMA20 extension, insufficient dollar volume, earnings-risk conflict, and portfolio conflict per D-01 through D-12.
  </behavior>
  <action>Create `config/volume_breakout.yaml` by mirroring the Trend Pullback fail-closed structure, not by refactoring `trend_pullback.py`. Use top-level key `volume_breakout`. Include `paper_trading_only: true`, `timing_mode: completed_daily_bar`, `resistance.lookback_bars: 20`, `resistance.breakout_buffer_pct: 0.25`, `resistance.require_close_above_buffered_resistance: true`, `volume.average_volume_period: 20`, `volume.min_volume_ratio: 1.5`, `volume.min_dollar_volume: 20000000`, `risk.max_atr_pct: 8.0`, `risk.max_ema20_extension_pct: 10.0`, `risk.min_reward_risk_proxy: 1.5`, `risk.reward_risk_epsilon: 0.01`, `deferred_gates.earnings_risk_source_verified: false`, `deferred_gates.portfolio_conflict_check_available: false`, and disabled behavior flags for intrabar validity, order creation, portfolio sizing, BUY/WATCH/AVOID classifications, backtest result creation, Telegram delivery, Paper deployment, and Live deployment. Create the initial `marketpilot/setups/volume_breakout.py` skeleton with `SETUP_NAME = "volume_breakout"`, `DEFAULT_CONFIG_PATH`, and `load_volume_breakout_config(path=DEFAULT_CONFIG_PATH)` using `yaml.safe_load` and the same fail-closed checks the tests assert: require `paper_trading_only is True`, `timing_mode == "completed_daily_bar"`, and `disabled_behaviors.intrabar_validity is False`. Do not add `CompletedDailyBar`, `VolumeBreakoutInput`, `calculate_prior_resistance()`, `contract_result()`, or evaluator logic in Task 1; Task 2 extends this skeleton. Extend `SetupRejectionReason` with exact enum members `INVALID_PRIOR_RESISTANCE`, `BREAKOUT_NOT_CONFIRMED`, `VOLUME_CONFIRMATION_WEAK`, `EMA20_EXTENSION_EXCESSIVE`, `INSUFFICIENT_DOLLAR_VOLUME`, `EARNINGS_RISK_CONFLICT`, and `PORTFOLIO_CONFLICT`. Keep existing Trend Pullback enum values unchanged.</action>
  <verify>
    <automated>python -m pytest tests/test_volume_breakout_contract.py::test_volume_breakout_config_contains_safety_bounded_defaults tests/test_volume_breakout_contract.py::test_rejection_reason_contract_covers_volume_breakout_gates -x</automated>
  </verify>
  <acceptance_criteria>
    - `marketpilot/setups/volume_breakout.py` contains `SETUP_NAME = "volume_breakout"`, `DEFAULT_CONFIG_PATH`, and `def load_volume_breakout_config`.
    - `config/volume_breakout.yaml` contains `lookback_bars: 20`, `min_volume_ratio: 1.5`, and `buy_watch_avoid_classifications: false`.
    - `marketpilot/setups/base.py` contains the seven exact new enum names listed in the task action.
    - No existing Trend Pullback enum value or test import is removed.
  </acceptance_criteria>
  <done>Volume Breakout config defaults and rejection vocabulary are present, tested, and bounded by completed-daily-bar safety settings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Volume Breakout contract and prior-resistance helper</name>
  <files>marketpilot/setups/volume_breakout.py, tests/test_volume_breakout_contract.py</files>
  <read_first>marketpilot/setups/volume_breakout.py, marketpilot/setups/trend_pullback.py, tests/test_trend_pullback_contract.py, .planning/phases/04-volume-breakout/04-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `contract_result("msft", signal_time)` returns a rejected `SetupResult` with `setup_name == "volume_breakout"`, uppercase symbol, completed daily-bar timing, and no trade fields per D-14/D-15.
    - Test 2: `calculate_prior_resistance()` returns the highest high from the previous 20 completed bars and ignores a much higher current-bar high per D-01/D-02.
    - Test 3: `calculate_prior_resistance()` rejects insufficient history, incomplete bars, non-positive lookback, and invalid high values instead of inventing resistance per SET-03.
  </behavior>
  <action>Extend the Task 1 `marketpilot/setups/volume_breakout.py` skeleton as a parallel module per D-16. Preserve `SETUP_NAME`, `DEFAULT_CONFIG_PATH`, and `load_volume_breakout_config()`. Add a local frozen `CompletedDailyBar` dataclass matching the Trend Pullback bar fields, a frozen `VolumeBreakoutInput` dataclass with `symbol_data`, `regime`, `bars`, `indicators`, `average_volume`, `average_dollar_volume`, `atr_pct`, `projected_target`, `earnings_source_verified: bool = False`, `earnings_risk_conflict: bool = False`, and `portfolio_conflict: bool | None = None`, plus `REQUIRED_INDICATORS = ("EMA20", "ATR14")`. `projected_target` is setup evidence only for the D-10 reward/risk proxy calculation in Plan 04-02; it must not create order target, stop, order, sizing, or lifecycle behavior. Add `calculate_prior_resistance(bars, lookback_bars)` and `contract_result(symbol, signal_time)`. `calculate_prior_resistance` must select only `bars[-lookback_bars - 1:-1]`, require all included bars and the signal bar to be complete, require finite positive highs, and raise `ValueError` for invalid inputs. Do not implement the full evaluator in this plan; this plan establishes the contract and helper needed by Plan 04-02.</action>
  <verify>
    <automated>python -m pytest tests/test_volume_breakout_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `marketpilot/setups/volume_breakout.py` contains `calculate_prior_resistance` and the slice excludes `bars[-1]`.
    - `tests/test_volume_breakout_contract.py` contains `test_prior_resistance_excludes_current_bar_high`.
    - Contract results use `SetupResult`, `SetupTiming`, `SetupStatus.REJECTED`, and `SetupRejectionReason.INCOMPLETE_COMPLETED_BAR_DATA`.
  </acceptance_criteria>
  <done>The Volume Breakout contract can be imported, its config loads safely, and current-bar-excluded resistance behavior is proven by deterministic tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file -> setup module | YAML values influence setup-gate thresholds and must fail closed. |
| completed bars -> resistance helper | Fixture or runtime bar data crosses into no-look-ahead resistance calculation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `config/volume_breakout.yaml` | mitigate | Fail closed when `paper_trading_only` is not true or intrabar validity is enabled. |
| T-04-02 | Tampering | `calculate_prior_resistance` | mitigate | Use explicit prior-bar slice ending before `bars[-1]` and test a high current bar is ignored. |
| T-04-03 | Elevation of Privilege | `marketpilot/setups/volume_breakout.py` | mitigate | Contract tests and disabled config flags forbid orders, sizing, classifications, backtests, Telegram, and deployment behavior per D-15. |
| T-04-SC | Tampering | package installs | mitigate | No package install is planned; if an executor introduces npm/pip/cargo installs, stop for package legitimacy review first. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_volume_breakout_contract.py -x`
- [ ] `python -m pytest tests/test_trend_pullback_contract.py tests/test_trend_pullback_safety.py -x`
- [ ] `python -m pytest`
- [ ] Confirm `marketpilot/setups/volume_breakout.py` contains no `BUY`, `WATCH`, `AVOID`, order APIs, portfolio sizing, backtest result creation, Telegram delivery, Paper deployment, Live deployment, credential, token, or password behavior.
</verification>

<success_criteria>
- SET-03 has a tested contract for current-bar-excluded prior resistance.
- D-01 through D-04 are reflected in config and contract tests.
- D-15 and D-16 boundaries are enforced by module shape and forbidden-behavior checks.
- Trend Pullback tests still pass after shared rejection vocabulary changes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-volume-breakout/04-01-SUMMARY.md`.
</output>
