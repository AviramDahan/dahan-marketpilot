---
phase: 04-volume-breakout
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - marketpilot/setups/volume_breakout.py
  - tests/test_volume_breakout_detection.py
  - tests/test_volume_breakout_rejections.py
autonomous: true
requirements:
  - SET-03
  - SET-04
user_setup: []
must_haves:
  truths:
    - "SET-03/D-01/D-02: Full detection uses prior resistance from previous completed bars only."
    - "D-03/D-04: A valid breakout requires the completed daily close to exceed buffered prior resistance; intraday high alone is rejected."
    - "SET-04/D-05/D-06/D-07/D-08/D-09: Valid results require volume confirmation, acceptable EMA20 extension, acceptable ATR, sufficient dollar volume, valid reward/risk, and non-RISK_OFF regime."
    - "D-10/D-11/D-12: The evaluator calculates reward/risk proxy from `projected_target`, `latest.close`, and `prior_resistance`; earnings and portfolio fields remain setup evidence or explicit input gates only; this plan creates no orders, real stops, order targets, sizing, portfolio state, or invented earnings data."
    - "D-13/D-14/D-15: Results include numeric evidence, valid/rejected status, explanations, and rejection reasons only."
  artifacts:
    - path: "marketpilot/setups/volume_breakout.py"
      provides: "Volume Breakout evaluator with detection, rejections, and evidence"
      contains: "def evaluate_volume_breakout"
    - path: "tests/test_volume_breakout_detection.py"
      provides: "Breakout trigger, current-bar exclusion, and volume confirmation tests"
      contains: "test_detects_valid_volume_breakout_on_completed_close_and_volume"
    - path: "tests/test_volume_breakout_rejections.py"
      provides: "SET-04 hard-gate rejection tests"
      contains: "test_calculates_reward_risk_proxy_from_projected_target_and_resistance"
  key_links:
    - from: "marketpilot/setups/volume_breakout.py"
      to: "marketpilot/setups/base.py"
      via: "SetupResult, NumericEvidence, SetupRejectionReason"
      pattern: "SetupResult"
    - from: "marketpilot/setups/volume_breakout.py"
      to: "marketpilot/symbol_data.py"
      via: "SymbolData.future_signal_ready(REQUIRED_INDICATORS)"
      pattern: "future_signal_ready"
    - from: "tests/test_volume_breakout_detection.py"
      to: "marketpilot/setups/volume_breakout.py"
      via: "evaluate_volume_breakout fixtures"
      pattern: "evaluate_volume_breakout"
---

<objective>
Implement Volume Breakout detection, rejection, and evidence generation on top of the Plan 04-01 contract.

Purpose: SET-03 and SET-04 require a no-look-ahead evaluator that confirms breakouts only after the completed daily close, then rejects weak or unsafe candidates using volume, ATR, EMA20 extension, dollar volume, evaluator-calculated reward/risk proxy, readiness, and regime gates.
Output: `evaluate_volume_breakout()` plus deterministic detection and rejection tests that preserve D-10 through D-15 boundaries.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-volume-breakout/04-CONTEXT.md
@.planning/phases/04-volume-breakout/04-RESEARCH.md
@.planning/phases/04-volume-breakout/04-01-SUMMARY.md
@marketpilot/setups/base.py
@marketpilot/setups/volume_breakout.py
@marketpilot/setups/trend_pullback.py
@tests/test_trend_pullback_detection.py
@tests/test_trend_pullback_rejections.py
@docs/trend_pullback.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement close-confirmed breakout detection and volume confirmation</name>
  <files>marketpilot/setups/volume_breakout.py, tests/test_volume_breakout_detection.py</files>
  <read_first>marketpilot/setups/volume_breakout.py, tests/test_trend_pullback_detection.py, .planning/phases/04-volume-breakout/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: A fixture with 20 prior completed highs near 100, a completed signal close above `prior_resistance * (1 + breakout_buffer_pct / 100)`, and volume at least 1.5x average volume returns `SetupStatus.VALID` per D-01 through D-06.
    - Test 2: A fixture with signal-bar high above resistance but close below buffered resistance returns `SetupStatus.REJECTED` with `SetupRejectionReason.BREAKOUT_NOT_CONFIRMED` per D-03/D-04.
    - Test 3: A fixture with signal-bar high far above all prior highs still records prior resistance from prior bars only per SET-03/D-01.
    - Test 4: A fixture with weak breakout-bar volume returns `SetupRejectionReason.VOLUME_CONFIRMATION_WEAK` per D-05/D-06.
  </behavior>
  <action>Implement `evaluate_volume_breakout(setup_input, config=None)` in `marketpilot/setups/volume_breakout.py`. Load config via `load_volume_breakout_config()`, use `calculate_prior_resistance()` for D-01 current-bar exclusion, compute `buffered_resistance = prior_resistance * (1 + breakout_buffer_pct / 100)`, and require `latest.close > buffered_resistance` when `require_close_above_buffered_resistance` is true. Compute `volume_ratio = latest.volume / average_volume` only when average volume is finite and positive; otherwise use a failing evidence value and reject. Add `NumericEvidence` entries for `resistance_lookback_bars`, `prior_resistance`, `breakout_buffer_pct`, `buffered_resistance`, `breakout_close`, `average_volume`, `volume_average_period`, and `volume_ratio`. Preserve completed daily-bar timing and use `SetupResult` only. Do not add trade instructions, fills, same-close execution assumptions, scoring, classifications, orders, sizing, portfolio state, backtest results, Telegram, or deployment behavior.</action>
  <verify>
    <automated>python -m pytest tests/test_volume_breakout_detection.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_volume_breakout_detection.py` proves a current-bar high does not affect prior resistance.
    - `evaluate_volume_breakout()` rejects high-only breakouts when the completed close does not clear buffered resistance.
    - Valid detection evidence includes `prior_resistance`, `buffered_resistance`, `breakout_close`, and `volume_ratio`.
  </acceptance_criteria>
  <done>Volume Breakout detects only completed-close breakouts with 1.5x default volume confirmation and tested current-bar exclusion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SET-04 rejections and numeric evidence</name>
  <files>marketpilot/setups/volume_breakout.py, tests/test_volume_breakout_rejections.py</files>
  <read_first>marketpilot/setups/volume_breakout.py, tests/test_trend_pullback_rejections.py, marketpilot/symbol_data.py, marketpilot/regime.py, .planning/phases/04-volume-breakout/04-RESEARCH.md</read_first>
  <behavior>
    - Test 1: RISK_OFF or `future_entries_allowed == False` rejects with `SetupRejectionReason.RISK_OFF` per D-08.
    - Test 2: rejected symbol data, missing indicators, invalid indicator values, insufficient history, or incomplete bars reject with the existing readiness/incomplete-data reasons.
    - Test 3: ATR above `risk.max_atr_pct`, EMA20 extension above `risk.max_ema20_extension_pct`, or average dollar volume below `volume.min_dollar_volume` reject with the matching reasons per D-07/D-09/SET-04.
    - Test 4: The evaluator calculates `reward_risk_proxy` from `latest.close`, `prior_resistance`, and explicit setup-evidence input `projected_target`; with `latest.close=105`, `prior_resistance=100`, `projected_target=115`, and `epsilon=0.01`, evidence records `risk_per_share_proxy=5`, `reward_per_share_proxy=10`, and `reward_risk_proxy=2.0`; reducing `projected_target` to 107 records `reward_risk_proxy=0.4` and rejects below `risk.min_reward_risk_proxy` per D-10/SET-04.
    - Test 5: `VolumeBreakoutInput` does not accept a precomputed `reward_risk_proxy` field after this plan; tests construct fixtures with `projected_target` and assert the evaluator produces the proxy evidence itself per D-10.
    - Test 6: `earnings_source_verified == False` records evidence and does not reject by itself; a verified explicit earnings conflict rejects without inventing earnings data per D-11.
    - Test 7: `portfolio_conflict is True` rejects with `SetupRejectionReason.PORTFOLIO_CONFLICT`, while `portfolio_conflict is None` records availability evidence and does not calculate real portfolio constraints per D-12.
  </behavior>
  <action>Extend `evaluate_volume_breakout()` to mirror the Trend Pullback evidence-first rejection pattern. Validate `SymbolData.future_signal_ready(REQUIRED_INDICATORS)`, each required `IndicatorResult`, all bars, and finite positive numeric inputs. Ensure `VolumeBreakoutInput` uses `projected_target: float` for D-10 proxy math and does not accept any precomputed reward/risk proxy input. `projected_target` is setup evidence for proxy math only and must not be documented, named, or used as an order target, profit target, stop, lifecycle target, or trade instruction. Apply the regime gate with `MarketRegime.RISK_OFF` and `RegimeResult.future_entries_allowed`. Compute `ema20_extension_pct = ((latest.close - EMA20) / EMA20) * 100`, failing when EMA20 is invalid or extension exceeds config. Compute reward/risk after `prior_resistance` is known: `epsilon = max(config.risk.reward_risk_epsilon, 0.01)` if config provides a positive value, otherwise `0.01`; `risk_per_share_proxy = max(latest.close - prior_resistance, epsilon)`; `reward_per_share_proxy = max(setup_input.projected_target - latest.close, 0)`; `reward_risk_proxy = reward_per_share_proxy / risk_per_share_proxy`. Add `NumericEvidence` for `ema20_extension_pct`, `atr_pct`, `average_dollar_volume`, `projected_target`, `risk_per_share_proxy`, `reward_per_share_proxy`, `reward_risk_proxy`, `regime`, `earnings_source_verified`, `earnings_risk_conflict`, and `portfolio_conflict`. Reject below `risk.min_reward_risk_proxy` using the calculated proxy, not a precomputed input. Reject only from explicit inputs and verified local values; do not invent an earnings calendar, do not calculate real portfolio constraints, and do not create stop, target, order, or lifecycle behavior. Deduplicate rejection reasons while preserving first-seen order and use `SetupStatus.REJECTED` whenever any reason exists.</action>
  <verify>
    <automated>python -m pytest tests/test_volume_breakout_detection.py tests/test_volume_breakout_rejections.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Rejected results retain evidence for the gate that failed.
    - `tests/test_volume_breakout_rejections.py` contains `test_calculates_reward_risk_proxy_from_projected_target_and_resistance` and asserts the exact `2.0` and `0.4` proxy outcomes described in the behavior block.
    - Fixture construction uses `projected_target`; production and tests do not pass `reward_risk_proxy` as a setup input.
    - `earnings_source_verified: false` does not become a rejection without a verified explicit conflict.
    - Portfolio conflict behavior is driven only by explicit input and does not inspect account, holdings, orders, cash, sector exposure, or position sizing.
  </acceptance_criteria>
  <done>SET-04 hard gates and D-10 through D-12 deferred-boundary rules are implemented with complete numeric evidence, including evaluator-calculated reward/risk proxy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| setup input -> evaluator | Untrusted numeric fixture/runtime values can be missing, stale, NaN, infinite, or semantically unsafe. |
| regime/readiness -> setup validity | Existing Phase 2 readiness and regime contracts determine whether future entries are allowed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | `evaluate_volume_breakout` | mitigate | Reject incomplete bars, invalid resistance, invalid indicators, NaN/infinite values, and current-bar look-ahead. |
| T-04-05 | Repudiation | `SetupResult.evidence` | mitigate | Record failed and passed numeric gates so every rejection reason is auditable. |
| T-04-06 | Elevation of Privilege | Volume Breakout result | mitigate | Return setup evidence only and avoid orders, sizing, classifications, backtests, Telegram, and deployment behavior per D-15. |
| T-04-SC | Tampering | package installs | mitigate | No package install is planned; if an executor introduces npm/pip/cargo installs, stop for package legitimacy review first. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_volume_breakout_detection.py tests/test_volume_breakout_rejections.py -x`
- [ ] `python -m pytest tests/test_volume_breakout_rejections.py::test_calculates_reward_risk_proxy_from_projected_target_and_resistance -x`
- [ ] `python -m pytest tests/test_volume_breakout_contract.py tests/test_trend_pullback_detection.py tests/test_trend_pullback_rejections.py -x`
- [ ] `python -m pytest`
- [ ] Confirm no same-close fill, order, target, stop, sizing, portfolio-state, fake earnings, fake backtest, Telegram, credential, or classification behavior was added.
</verification>

<success_criteria>
- SET-03 current-bar exclusion remains true inside full detection.
- SET-04 gates reject invalid Volume Breakout candidates with numeric evidence.
- D-05 through D-13 are represented in evaluator logic and tests, including D-10 proxy calculation from broken resistance and explicit projected setup evidence.
- D-14 and D-15 boundaries are preserved: setup results only, no classifications or trading side effects.
</success_criteria>

<output>
After completion, create `.planning/phases/04-volume-breakout/04-02-SUMMARY.md`.
</output>
