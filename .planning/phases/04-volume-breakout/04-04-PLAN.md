---
phase: 04-volume-breakout
plan: "04"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - marketpilot/setups/volume_breakout.py
  - tests/test_volume_breakout_rejections.py
  - docs/volume_breakout.md
  - docs/testing.md
autonomous: true
gap_closure: true
requirements:
  - SET-04
user_setup: []
must_haves:
  truths:
    - "ROADMAP Phase 4 success criterion 3: Volume Breakout rejects stale data before returning a valid setup result."
    - "SET-04: Data readiness remains a hard gate for Volume Breakout, including stale SymbolData readiness."
    - "D-14/D-15: The stale-data gate returns setup evidence and rejection reasons only, with no BUY/WATCH/AVOID, orders, sizing, portfolio state, backtest results, Telegram, Paper deployment, or Live deployment behavior."
  artifacts:
    - path: "tests/test_volume_breakout_rejections.py"
      provides: "Deterministic stale-data rejection coverage"
      contains: "test_rejects_stale_symbol_data_readiness"
    - path: "marketpilot/setups/volume_breakout.py"
      provides: "Volume Breakout stale-data readiness gate"
      contains: "future_signal_ready(REQUIRED_INDICATORS, stale="
    - path: "docs/volume_breakout.md"
      provides: "Documented stale-data rejection behavior"
      contains: "stale"
    - path: "docs/testing.md"
      provides: "Documented Phase 4 stale-data test coverage"
      contains: "stale"
  key_links:
    - from: "marketpilot/setups/volume_breakout.py"
      to: "marketpilot/symbol_data.py"
      via: "SymbolData.future_signal_ready(REQUIRED_INDICATORS, stale=setup_input.symbol_data_stale)"
      pattern: "future_signal_ready\\(REQUIRED_INDICATORS, stale="
    - from: "tests/test_volume_breakout_rejections.py"
      to: "marketpilot/setups/volume_breakout.py"
      via: "evaluate_volume_breakout stale fixture"
      pattern: "symbol_data_stale=True"
---

<objective>
Close the Phase 4 verification gap where `evaluate_volume_breakout()` can return a valid result for stale data.

Purpose: ROADMAP Phase 4 success criterion 3 says signals are rejected for stale data. Current verification found that Volume Breakout calls `SymbolData.future_signal_ready(REQUIRED_INDICATORS)` without passing stale state, so otherwise ready stale data can pass. This gap-closure plan adds deterministic stale-data rejection coverage first, then wires the existing `SymbolData` stale readiness mechanism into the evaluator.
Output: A new stale-data rejection test, a stale readiness gate in `marketpilot/setups/volume_breakout.py`, and synchronized setup/testing docs.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-volume-breakout/04-CONTEXT.md
@.planning/phases/04-volume-breakout/04-RESEARCH.md
@.planning/phases/04-volume-breakout/04-VALIDATION.md
@.planning/phases/04-volume-breakout/04-VERIFICATION.md
@.planning/phases/04-volume-breakout/04-01-SUMMARY.md
@.planning/phases/04-volume-breakout/04-02-SUMMARY.md
@.planning/phases/04-volume-breakout/04-03-SUMMARY.md
@marketpilot/setups/volume_breakout.py
@marketpilot/symbol_data.py
@tests/test_volume_breakout_rejections.py
@docs/volume_breakout.md
@docs/testing.md
</context>

<source_audit>
- GOAL: Phase 4 goal remains an independently testable Volume Breakout setup with current-bar exclusion and volume confirmation; this plan only closes the stale-data rejection gap from verification.
- REQ: SET-04 is covered because stale data is part of data readiness and ROADMAP Phase 4 success criterion 3 explicitly requires stale-data rejection.
- RESEARCH: Phase 4 research says `SymbolData.future_signal_ready()` is the reusable readiness boundary and no new packages or external services are needed.
- CONTEXT: D-14 and D-15 boundaries are preserved. Deferred Phase 5 scoring, Phase 6 portfolio/risk/order behavior, Phase 7 backtests, Telegram, and live/Paper deployment are not planned here.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED stale-data rejection test</name>
  <files>tests/test_volume_breakout_rejections.py</files>
  <read_first>tests/test_volume_breakout_rejections.py, marketpilot/setups/volume_breakout.py, marketpilot/symbol_data.py, .planning/phases/04-volume-breakout/04-VERIFICATION.md</read_first>
  <behavior>
    - Test 1: `test_rejects_stale_symbol_data_readiness` constructs an otherwise valid Volume Breakout input with stale SymbolData readiness and expects `SetupStatus.REJECTED`.
    - Test 2: The rejected result includes `SetupRejectionReason.DATA_NOT_READY`, proving stale readiness remains part of the existing data-readiness gate instead of adding a new trading behavior.
    - Test 3: The rejected result includes `NumericEvidence` named `symbol_data_stale` with `value is True` and `passed is False`.
  </behavior>
  <action>Add only the deterministic stale-data rejection test first. Reuse the existing `valid_input()` and `evidence_item()` helpers in `tests/test_volume_breakout_rejections.py`. Do not create market data, wall-clock calls, fake timestamps, credentials, QuantConnect dependencies, or external services. The test should model stale readiness through a new explicit setup-input field named `symbol_data_stale=True`, because `marketpilot.symbol_data.SymbolData` already exposes stale readiness through `future_signal_ready(..., stale=True)`. The test must fail against the current implementation before Task 2 because `VolumeBreakoutInput` does not yet carry stale state and `evaluate_volume_breakout()` does not pass stale state to `future_signal_ready()`.</action>
  <verify>
    <automated>python -m pytest tests/test_volume_breakout_rejections.py::test_rejects_stale_symbol_data_readiness -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_volume_breakout_rejections.py` contains `test_rejects_stale_symbol_data_readiness`.
    - The test uses existing Volume Breakout fixtures and does not introduce new market data, fake timestamps, credentials, or external dependencies.
    - The RED run is recorded in the summary as failing before production changes, then this same command passes after Task 2.
  </acceptance_criteria>
  <done>A deterministic stale-data rejection test exists and demonstrates the current verification gap before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire stale readiness into Volume Breakout</name>
  <files>marketpilot/setups/volume_breakout.py, tests/test_volume_breakout_rejections.py, docs/volume_breakout.md, docs/testing.md</files>
  <read_first>marketpilot/setups/volume_breakout.py, marketpilot/symbol_data.py, tests/test_volume_breakout_rejections.py, docs/volume_breakout.md, docs/testing.md, .planning/phases/04-volume-breakout/04-VERIFICATION.md</read_first>
  <behavior>
    - Test 1: `symbol_data_stale=True` causes `evaluate_volume_breakout()` to call `SymbolData.future_signal_ready(REQUIRED_INDICATORS, stale=True)` and reject with `SetupRejectionReason.DATA_NOT_READY`.
    - Test 2: `symbol_data_stale=False` preserves existing valid Volume Breakout fixtures when all other gates pass.
    - Test 3: All existing Volume Breakout rejection, detection, explanation, safety, and contract tests still pass.
  </behavior>
  <action>Add `symbol_data_stale: bool = False` to `VolumeBreakoutInput` in `marketpilot/setups/volume_breakout.py`. In `evaluate_volume_breakout()`, append `NumericEvidence("symbol_data_stale", setup_input.symbol_data_stale, False, not setup_input.symbol_data_stale)` before early returns so stale rejection remains auditable. Replace the readiness call with `setup_input.symbol_data.future_signal_ready(REQUIRED_INDICATORS, stale=setup_input.symbol_data_stale)`. Keep the rejection reason as `SetupRejectionReason.DATA_NOT_READY`; do not add a new enum unless an existing test contract requires it. Do not use `datetime.now()`, system time, fake timestamps, QuantConnect APIs, market-data downloads, credentials, external dependencies, BUY/WATCH/AVOID labels, orders, stops, targets, sizing, portfolio state, backtest results, Telegram, Paper deployment, or Live deployment. Update `docs/volume_breakout.md` to state that stale SymbolData readiness is a hard rejection. Update `docs/testing.md` to list stale-data rejection in Phase 4 Volume Breakout test coverage.</action>
  <verify>
    <automated>python -m pytest tests/test_volume_breakout_rejections.py::test_rejects_stale_symbol_data_readiness -x</automated>
    <automated>python -m pytest tests/test_volume_breakout_contract.py tests/test_volume_breakout_detection.py tests/test_volume_breakout_rejections.py tests/test_volume_breakout_explanations.py tests/test_volume_breakout_safety.py -x</automated>
    <automated>python -m pytest</automated>
  </verify>
  <acceptance_criteria>
    - `marketpilot/setups/volume_breakout.py` contains `symbol_data_stale: bool = False`.
    - `marketpilot/setups/volume_breakout.py` contains `future_signal_ready(REQUIRED_INDICATORS, stale=setup_input.symbol_data_stale)`.
    - Stale-data rejection uses `SetupRejectionReason.DATA_NOT_READY` and evidence `symbol_data_stale`.
    - Existing valid Volume Breakout tests remain valid when `symbol_data_stale` is omitted.
    - Docs mention stale data rejection without adding performance claims, credentials, external setup, order behavior, or deployment behavior.
  </acceptance_criteria>
  <done>Volume Breakout uses the existing SymbolData stale readiness mechanism, rejects stale data deterministically, and preserves all Phase 4 setup-only safety boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| setup input -> evaluator | Untrusted stale-readiness state crosses into setup validity and must fail closed. |
| SymbolData readiness -> Volume Breakout result | Existing readiness logic determines whether future setup signals may be considered. |
| docs/tests -> user expectations | Documentation must describe stale-data rejection without implying trade execution, profitability, or deployment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | `evaluate_volume_breakout` | mitigate | Pass stale state into `SymbolData.future_signal_ready(..., stale=...)` and reject stale readiness as `DATA_NOT_READY`. |
| T-04-11 | Repudiation | `SetupResult.evidence` | mitigate | Add `symbol_data_stale` evidence so stale rejection is visible in audit output. |
| T-04-12 | Elevation of Privilege | Volume Breakout stale gate | mitigate | Keep output as setup evidence only; do not add orders, sizing, classifications, backtests, Telegram, Paper, or Live behavior. |
| T-04-SC | Tampering | package installs | mitigate | No package install is planned; if an executor introduces npm/pip/cargo installs, stop for package legitimacy review first. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_volume_breakout_rejections.py::test_rejects_stale_symbol_data_readiness -x`
- [ ] `python -m pytest tests/test_volume_breakout_contract.py tests/test_volume_breakout_detection.py tests/test_volume_breakout_rejections.py tests/test_volume_breakout_explanations.py tests/test_volume_breakout_safety.py -x`
- [ ] `python -m pytest`
- [ ] Confirm `marketpilot/setups/volume_breakout.py` still contains no BUY/WATCH/AVOID, orders, stops, targets, sizing, portfolio state, backtest result creation, Telegram delivery, Paper deployment, Live deployment, credential, token, password, fake performance, or profitability behavior.
</verification>

<success_criteria>
- ROADMAP Phase 4 success criterion 3 is covered for stale-data rejection.
- SET-04 data-readiness gating rejects stale SymbolData readiness deterministically.
- `evaluate_volume_breakout()` uses `SymbolData.future_signal_ready(REQUIRED_INDICATORS, stale=setup_input.symbol_data_stale)`.
- The stale-data rejection test passes with no external services, no real market data, no credentials, and no wall-clock dependency.
- All Phase 4 Volume Breakout tests and full `python -m pytest` pass after execution.
</success_criteria>

<output>
After completion, create `.planning/phases/04-volume-breakout/04-04-SUMMARY.md`.
</output>
