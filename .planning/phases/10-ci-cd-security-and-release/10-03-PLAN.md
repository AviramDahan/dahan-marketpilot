---
phase: 10-ci-cd-security-and-release
plan: "03"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - "docs/operations.md"
  - "docs/troubleshooting.md"
  - "docs/setup.md"
  - "docs/recovery.md"
  - "docs/render_dashboard.md"
autonomous: true
requirements: [CI-03, CI-06]
must_haves:
  truths:
    - "CI-03: Operations, recovery, troubleshooting, setup, and Render/QuantConnect responsibilities are documented."
    - "CI-06: Operational docs distinguish executed checks from skipped and not_run checks."
  artifacts:
    - path: "docs/operations.md"
      provides: "Operator procedures for CI/CD, QuantConnect, weekly validation, dashboard health, and release checks"
    - path: "docs/troubleshooting.md"
      provides: "Symptom-to-safe-action troubleshooting runbook"
    - path: "docs/setup.md"
      provides: "GitHub Actions setup and secret-store instructions"
    - path: "docs/recovery.md"
      provides: "Recovery procedures tied to CI/CD and dashboard health evidence"
    - path: "docs/render_dashboard.md"
      provides: "Dashboard health workflow expectations"
  key_links:
    - from: "docs/operations.md"
      to: ".github/workflows/*.yml"
      via: "workflow names and trigger documentation"
      pattern: "tests.yml|quantconnect.yml|weekly-validation.yml|dashboard-health.yml"
    - from: "docs/troubleshooting.md"
      to: "docs/recovery.md"
      via: "safe recovery and not_run rules"
      pattern: "not_run|QuantConnect remains authoritative"
---

<objective>
Create the operations, setup, recovery, and troubleshooting documentation required by CI-03 and CI-06.

Purpose: Operators need exact procedures for CI/CD, guarded QuantConnect checks, dashboard health, recovery, and incident handling without secrets in repo files and without fake pass claims.
Output: `docs/operations.md`, `docs/troubleshooting.md`, and synchronized setup/recovery/Render docs.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ci-cd-security-and-release/10-RESEARCH.md
@.planning/phases/10-ci-cd-security-and-release/10-PATTERNS.md
@.planning/phases/10-ci-cd-security-and-release/10-01-SUMMARY.md
@.github/workflows/tests.yml
@.github/workflows/quantconnect.yml
@.github/workflows/weekly-validation.yml
@.github/workflows/dashboard-health.yml
@docs/setup.md
@docs/recovery.md
@docs/render_dashboard.md
@docs/safety.md
@docs/licensing.md
@docs/testing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document operations and setup procedures</name>
  <files>docs/operations.md, docs/setup.md</files>
  <action>Create `docs/operations.md` with operator procedures for local offline tests, `tests.yml`, `quantconnect.yml`, `weekly-validation.yml`, `dashboard-health.yml`, GitHub Actions secret names, workflow triggers, expected schedules, required operator confirmations, and evidence statuses. Update `docs/setup.md` with GitHub Actions setup steps and approved secret stores for QuantConnect and dashboard health values. Explicitly state that default CI needs no QuantConnect, Telegram, Render, broker, internet, or real market access. State that QuantConnect sync/backtest and dashboard health checks are guarded external checks and become `not_run` or `skipped` when prerequisites are missing. Do not include secret values or account identifiers.</action>
  <verify>
    <automated>python -c "from pathlib import Path; text=(Path('docs/operations.md').read_text(encoding='utf-8') + Path('docs/setup.md').read_text(encoding='utf-8')); assert 'tests.yml' in text and 'quantconnect.yml' in text and 'weekly-validation.yml' in text and 'dashboard-health.yml' in text and 'GitHub Actions Secrets' in text and 'not_run' in text"</automated>
  </verify>
  <done>Operations and setup docs name all Phase 10 workflows, list secret names only, and describe skipped/not_run behavior for missing external prerequisites.</done>
</task>

<task type="auto">
  <name>Task 2: Document troubleshooting and recovery procedures</name>
  <files>docs/troubleshooting.md, docs/recovery.md, docs/render_dashboard.md</files>
  <action>Create `docs/troubleshooting.md` as a symptom, likely cause, safe action, and prohibited action runbook for CI failures, missing Python dependency installs, failed workflow SHA/action checks, missing QuantConnect secrets, rejected Lean package checkpoint, QuantConnect sync/backtest not_run, dashboard health not_run, Render stale/error states, dashboard auth failures, recovery-required Paper state, secret exposure, and suspected fake-performance/profitability artifacts. Update `docs/recovery.md` with release-era recovery procedures that preserve QuantConnect authority, block new entries on unavailable/mismatched Paper state, and require explicit operator recovery before resuming entries. Update `docs/render_dashboard.md` with dashboard health workflow behavior and read-only GET expectations. Keep Telegram delivery non-authoritative and keep Render read-only.</action>
  <verify>
    <automated>python -c "from pathlib import Path; text=(Path('docs/troubleshooting.md').read_text(encoding='utf-8') + Path('docs/recovery.md').read_text(encoding='utf-8') + Path('docs/render_dashboard.md').read_text(encoding='utf-8')); assert 'not_run' in text and 'skipped' in text and 'QuantConnect remains authoritative' in text and 'read-only' in text and 'DASHBOARD_HEALTH_URL' in text"</automated>
  </verify>
  <done>Troubleshooting, recovery, and Render docs provide safe operator actions while preserving QuantConnect authority, read-only dashboard behavior, and explicit unexecuted-check status labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs -> operator actions | Documentation can cause safe or unsafe operational decisions. |
| Operator -> GitHub Actions Secrets | Setup docs instruct where secrets are stored without exposing values. |
| Operator -> QuantConnect/Render | Operations docs guide external checks that may contact authoritative services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Information Disclosure | `docs/setup.md`, `docs/operations.md` | mitigate | List secret names only and direct operators to approved external secret stores. |
| T-10-03-02 | Repudiation | Operational evidence | mitigate | Docs require `passed`, `failed`, `skipped`, or `not_run` status for every check. |
| T-10-03-03 | Tampering | Recovery procedures | mitigate | Recovery docs preserve QuantConnect authority and prohibit local/cache state promotion. |
| T-10-03-04 | Elevation of Privilege | Dashboard health operations | mitigate | Render docs limit health workflow to read-only GET checks and no order/recovery controls. |
| T-10-03-SC | Tampering | Package/action operations | mitigate | Operations docs require the 10-01 checkpoint records before rotating action SHAs or Lean versions. |
</threat_model>

<verification>
- `python -c "from pathlib import Path; text=(Path('docs/operations.md').read_text(encoding='utf-8') + Path('docs/troubleshooting.md').read_text(encoding='utf-8')); assert 'not_run' in text and 'skipped' in text"`
- `python -m pytest tests/test_project_files.py tests/test_quantconnect_verification_docs.py tests/test_dashboard_render_config.py -q`
</verification>

<success_criteria>
- CI-03 is satisfied for operations, setup, recovery, troubleshooting, QuantConnect responsibilities, GitHub responsibilities, Render responsibilities, safety, and limitations.
- CI-06 is satisfied because operational docs require explicit executed/skipped/not_run evidence.
</success_criteria>

<output>
Create `.planning/phases/10-ci-cd-security-and-release/10-03-SUMMARY.md` when done.
</output>
