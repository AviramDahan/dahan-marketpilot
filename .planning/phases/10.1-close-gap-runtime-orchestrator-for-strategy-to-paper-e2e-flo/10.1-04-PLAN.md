---
phase: 10.1-close-gap-runtime-orchestrator-for-strategy-to-paper-e2e-flo
plan: "04"
type: execute
wave: 4
depends_on: ["10.1-01", "10.1-02", "10.1-03"]
files_modified:
  - "marketpilot/dashboard_export.py"
  - "marketpilot/lean_bridge.py"
  - "marketpilot/runtime_orchestrator.py"
  - "dashboard/config.py"
  - "dashboard/data.py"
  - "config/dashboard.yaml"
  - "tests/test_dashboard_object_store_source.py"
  - "docs/dashboard.md"
  - "docs/render_dashboard.md"
autonomous: true
requirements: [QC-03, DASH-04, TEL-02]
must_haves:
  truths:
    - "D-10.1-04: Dashboard export mirrors QuantConnect-authoritative state but never becomes authority."
    - "Render dashboard remains read-only and cannot write/delete Object Store data or submit orders."
    - "Object Store/API source support uses approved keys/endpoints and degrades safely when not configured, missing, stale, or malformed."
    - "External QuantConnect Object Store/API fetches are not claimed as passed unless actually run."
  artifacts:
    - path: "marketpilot/dashboard_export.py"
      provides: "Serializer/export payload builder for QuantConnect-authoritative dashboard JSON"
    - path: "dashboard/data.py"
      provides: "Read-only Object Store/API-style dashboard source loader"
    - path: "tests/test_dashboard_object_store_source.py"
      provides: "Source/export tests for missing, stale, malformed, and valid payloads"
  key_links:
    - from: "marketpilot/runtime_orchestrator.py"
      to: "marketpilot/dashboard_export.py"
      via: "runtime result and QuantConnect snapshot evidence converted to dashboard payloads"
      pattern: "Dashboard export payload"
    - from: "marketpilot/lean_bridge.py"
      to: "marketpilot/dashboard_export.py"
      via: "approved Object Store producer/fake-writer adapter"
      pattern: "OBJECT_STORE_EXPORT_KEYS"
    - from: "marketpilot/dashboard_export.py"
      to: "dashboard/data.py"
      via: "read-only Object Store/API-style fetch payload parsed into DashboardSnapshot"
      pattern: "load_dashboard_snapshot"
---

<objective>
Close the QuantConnect-authority-to-dashboard runtime gap by adding a dashboard export producer shape and a read-only Object Store/API-style source loader that preserves dashboard degraded states and authority labels.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-close-gap-runtime-orchestrator-for-strategy-to-paper-e2e-flo/CONTEXT.md
@dashboard/data.py
@dashboard/config.py
@dashboard/models.py
@dashboard/cache.py
@dashboard/redaction.py
@config/dashboard.yaml
@marketpilot/quantconnect_paper.py
@marketpilot/runtime_orchestrator.py
@tests/test_dashboard_runtime_source.py
@tests/test_dashboard_data_contracts.py
@docs/dashboard.md
@docs/render_dashboard.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dashboard Object Store/source tests</name>
  <files>tests/test_dashboard_object_store_source.py</files>
  <behavior>
    - Valid exported payload from `marketpilot/dashboard_export.py` loads into `DashboardSnapshot` with QuantConnect authority labels.
    - Fake Object Store producer writes only approved dashboard keys and can be consumed by a fake read-only fetcher.
    - Missing source returns explicit `not_configured` or `not_available`, not a crash.
    - Stale/malformed payload returns safe degraded states with redacted diagnostics.
    - Read-only source loader rejects mutation-like endpoints/keys and secret-like path values.
  </behavior>
  <action>Create tests with local fixtures and fake Object Store/API responses only. Do not call QuantConnect, Render, internet, or credentials.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_object_store_source.py -q</automated>
  </verify>
  <done>Tests prove both sides of INT-03 exist: a producer path for approved Object Store/API-style dashboard payloads and a read-only dashboard fetch/parse path.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dashboard export builder and source loader</name>
  <files>marketpilot/dashboard_export.py, dashboard/config.py, dashboard/data.py, config/dashboard.yaml</files>
  <behavior>
    - Runtime/export builder serializes only supplied QuantConnect-authoritative snapshot/runtime evidence.
    - `lean_bridge` or an adjacent adapter exposes a tested producer path that writes approved Object Store keys through an injected fake writer in tests.
    - Dashboard config supports approved source kinds without storing secret values.
    - Loader supports local exported Object Store/API payload shapes and preserves existing `local_json` behavior.
    - No Object Store write/delete or dashboard mutation paths are added.
  </behavior>
  <action>Add export serialization, a tested Object Store/API-style producer interface with injected fake writer, and read-only source support. Real external execution may remain `not_run`, but code paths for producer and fetcher must exist and be tested with fakes.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_object_store_source.py tests/test_dashboard_runtime_source.py tests/test_dashboard_data_contracts.py tests/test_dashboard_read_only.py -q</automated>
  </verify>
  <done>`marketpilot/dashboard_export.py`, bridge producer hooks, and `dashboard/data.py` provide a complete tested fake-backed producer/fetcher path for QuantConnect-authoritative dashboard data.</done>
</task>

<task type="auto">
  <name>Task 3: Update dashboard docs</name>
  <files>docs/dashboard.md, docs/render_dashboard.md</files>
  <behavior>
    - Docs explain QuantConnect remains authority.
    - Docs list source kinds and secret names only, never values.
    - Docs distinguish local/offline payload tests from real external Object Store/API execution.
  </behavior>
  <action>Update docs after implementation and include `not_configured`, `skipped`, and `not_run` wording for external setup.</action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_object_store_source.py tests/test_dashboard_render_config.py -q</automated>
  </verify>
  <done>Dashboard docs explain Object Store/API producer/fetcher setup, preserve dashboard non-authority, and explicitly prohibit order controls and Object Store mutation from Render.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_dashboard_object_store_source.py tests/test_dashboard_runtime_source.py tests/test_dashboard_data_contracts.py tests/test_dashboard_read_only.py -q`
- `python -m pytest tests/test_dashboard_pages.py tests/test_dashboard_secret_masking.py tests/test_dashboard_render_config.py -q`
</verification>

<success_criteria>
- Dashboard data can be sourced from approved QuantConnect export/Object Store-style payloads.
- Dashboard remains read-only and secret-safe.
- DASH-04 is no longer partial for the planned runtime export/source path.
</success_criteria>

<output>
Create `10.1-04-SUMMARY.md` when done.
</output>
