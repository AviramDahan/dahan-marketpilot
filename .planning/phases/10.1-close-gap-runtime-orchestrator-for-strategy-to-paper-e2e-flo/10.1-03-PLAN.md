---
phase: 10.1-close-gap-runtime-orchestrator-for-strategy-to-paper-e2e-flo
plan: "03"
type: execute
wave: 3
depends_on: ["10.1-01", "10.1-02"]
files_modified:
  - "marketpilot/lean_bridge.py"
  - "lean/main.py"
  - "tests/test_lean_runtime_bridge_static.py"
  - "tests/test_lean_static_safety.py"
autonomous: true
requirements: [QC-03, UNI-01, UNI-02, UNI-03, UNI-04, UNI-05, REG-01, REG-02, REG-03, IND-01, IND-02, IND-03, IND-04, IND-05, MODE-01, MODE-02, MODE-03, TF-01, TF-02, TF-03, TF-04, TF-05, TF-06, TF-07, QC-MTF-01, QC-MTF-02, SET-MTF-01, SET-MTF-02, SET-MTF-03, BT-01]
must_haves:
  truths:
    - "D-10.1-01: LEAN bridge keeps QCAlgorithm-specific objects at the edge and calls pure runtime services."
    - "D-10.1-06: LEAN bridge maps only completed bars into runtime contracts and preserves exchange-timezone closure semantics."
    - "Static safety policy allows only audited paper-only bridge wiring; real broker, live-money, margin, leverage, shorting, and uncontrolled order methods remain forbidden."
    - "INT-02: `lean/main.py` must consume dynamic universe/readiness, completed-bar adapters, regime, indicators, setup registry, scoring/ranking, risk, reconciliation, paper eligibility, notification events, and dashboard export evidence through approved bridge/runtime calls."
    - "External QuantConnect execution remains operator-run/not_run unless actually executed."
  artifacts:
    - path: "marketpilot/lean_bridge.py"
      provides: "Adapter helpers from QuantConnect-like data to MarketPilot runtime inputs"
    - path: "lean/main.py"
      provides: "Thin QCAlgorithm adapter that initializes safe runtime bridge without embedding strategy logic"
    - path: "tests/test_lean_runtime_bridge_static.py"
      provides: "Static bridge and safety allowlist tests"
  key_links:
    - from: "lean/main.py"
      to: "marketpilot/lean_bridge.py"
      via: "approved QuantConnect adapter lifecycle hooks"
      pattern: "initialize|on_securities_changed|consolidated bar"
    - from: "marketpilot/lean_bridge.py"
      to: "marketpilot/runtime_orchestrator.py"
      via: "domain input conversion and runtime pipeline invocation"
      pattern: "runtime input|orchestration result"
    - from: "marketpilot/runtime_orchestrator.py"
      to: "marketpilot/dashboard_export.py"
      via: "runtime result export evidence consumed by Plan 10.1-04"
      pattern: "dashboard export payload"
---

<objective>
Add the safe LEAN bridge and update `lean/main.py` from minimal benchmark shell to thin runtime adapter while preserving paper-only safety and explicit external `not_run` evidence.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-close-gap-runtime-orchestrator-for-strategy-to-paper-e2e-flo/CONTEXT.md
@.planning/phases/10.1-close-gap-runtime-orchestrator-for-strategy-to-paper-e2e-flo/10.1-RESEARCH.md
@lean/main.py
@marketpilot/runtime_orchestrator.py
@marketpilot/symbol_data.py
@marketpilot/universe.py
@marketpilot/regime.py
@marketpilot/indicators.py
@marketpilot/timeframes.py
@tests/test_lean_static_safety.py
@tests/test_quantconnect_verification_docs.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace blanket LEAN safety assertions with audited bridge policy</name>
  <files>tests/test_lean_runtime_bridge_static.py, tests/test_lean_static_safety.py</files>
  <behavior>
    - Real broker models, live-money strings, margin, leverage, shorting, options, futures, crypto, Forex, raw credentials, and uncontrolled order methods remain forbidden.
    - `lean/main.py` imports approved MarketPilot bridge/runtime/export modules and calls them from explicit lifecycle hooks.
    - Static tests prove the runtime path references universe/readiness, completed-bar adapters, regime, indicators, setup registry, scoring/ranking, risk, reconciliation, paper eligibility, notification events, and dashboard export evidence.
    - Any future Paper order call must be behind explicit runtime eligibility and idempotency evidence.
    - Static tests document why former blanket `add_universe`/order-call bans changed.
  </behavior>
  <action>Create new bridge-specific static tests and update existing static safety tests carefully. Do not weaken real-money prohibitions.</action>
  <verify>
    <automated>python -m pytest tests/test_lean_runtime_bridge_static.py tests/test_lean_static_safety.py -q</automated>
  </verify>
  <done>Static safety tests permit only the planned paper-only runtime bridge path and continue to reject real-money, credential, leverage, margin, shorting, and uncontrolled order behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add LEAN bridge adapter tests</name>
  <files>tests/test_lean_runtime_bridge_static.py</files>
  <behavior>
    - Bridge helpers convert QuantConnect-like bar records into completed-bar/runtime-ready structures.
    - Removed securities route to SymbolData cleanup behavior.
    - Partial/incomplete bars are not signal-valid.
    - Missing benchmark/regime/indicator data fails closed.
    - Bridge tests prove dynamic universe changes and completed consolidator bars are converted into runtime inputs without embedding setup/scoring logic in `lean/main.py`.
  </behavior>
  <action>Use simple fake objects or mappings rather than QuantConnect imports. Keep tests offline and deterministic.</action>
  <verify>
    <automated>python -m pytest tests/test_lean_runtime_bridge_static.py tests/test_timeframes.py tests/test_symbol_data.py tests/test_regime.py -q</automated>
  </verify>
  <done>Bridge adapter tests prove QuantConnect-like universe changes, completed bars, readiness, regime, and indicator evidence can feed runtime inputs while incomplete/partial data fails closed.</done>
</task>

<task type="auto">
  <name>Task 3: Implement bridge and thin LEAN adapter</name>
  <files>marketpilot/lean_bridge.py, lean/main.py</files>
  <behavior>
    - `lean/main.py` remains thin but explicitly wires the approved runtime path: dynamic universe/readiness, completed-bar adapters, regime, indicators, setup registry, scoring/ranking, risk, reconciliation, paper eligibility, notification events, and dashboard export evidence.
    - Strategy decisions are delegated to `marketpilot.runtime_orchestrator`; `lean/main.py` contains adapter calls and lifecycle hooks only.
    - Bridge exposes explicit `not_run`/`not_configured` statuses for unsupported external operations.
    - No credentials or secret values are stored or printed.
  </behavior>
  <action>Implement the smallest LEAN bridge necessary for the full INT-02 runtime path tested above. Do not add broad Cloud API clients or real external execution in this plan.</action>
  <verify>
    <automated>python -m pytest tests/test_lean_runtime_bridge_static.py tests/test_lean_static_safety.py -q</automated>
  </verify>
  <done>`lean/main.py` is a thin but real runtime adapter that consumes the phase modules through `marketpilot/lean_bridge.py` and delegates all decisions to pure runtime orchestration.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_lean_runtime_bridge_static.py tests/test_lean_static_safety.py -q`
- `python -m pytest tests/test_timeframes.py tests/test_symbol_data.py tests/test_regime.py tests/test_indicators.py -q`
</verification>

<success_criteria>
- `lean/main.py` is no longer a pure benchmark-only shell; it is a thin approved runtime adapter.
- Static safety still forbids real-money paths and uncontrolled order behavior.
- LEAN bridge can be verified offline with fake QC-like inputs.
</success_criteria>

<output>
Create `10.1-03-SUMMARY.md` when done.
</output>
