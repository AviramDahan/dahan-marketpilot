---
phase: 10.1-close-gap-runtime-orchestrator-for-strategy-to-paper-e2e-flo
plan: "02"
type: execute
wave: 2
depends_on: ["10.1-01"]
files_modified:
  - "marketpilot/runtime_orchestrator.py"
  - "tests/test_runtime_orchestrator.py"
  - "tests/test_runtime_reconciliation_gate.py"
autonomous: true
requirements: [QC-03, SET-01, SET-02, SET-05, SET-07, SCO-01, SCO-02, SCO-03, SCO-04, RISK-01, RISK-02, RISK-03, RISK-04, RISK-05, RISK-06, RISK-07, BT-01, TEL-01, TEL-02]
must_haves:
  truths:
    - "D-10.1-02: Paper-intent creation is gated by validation, paper mode, reconciliation, risk, and idempotency."
    - "D-10.1-03: QuantConnect snapshot authority is required before new entries can be considered."
    - "High score or BUY_CANDIDATE classification is not an order instruction."
    - "Reconciliation mismatch blocks new entries and preserves exit/protective obligations."
  artifacts:
    - path: "marketpilot/runtime_orchestrator.py"
      provides: "Pure strategy pipeline from SetupResult through score, rank, risk, order intent, and notification events"
    - path: "tests/test_runtime_orchestrator.py"
      provides: "Deterministic E2E domain pipeline tests"
    - path: "tests/test_runtime_reconciliation_gate.py"
      provides: "QuantConnect mismatch blocks new entries while keeping exits authoritative"
---

<objective>
Wire the pure MarketPilot domain pipeline from setup results through scoring, ranking, paper eligibility, reconciliation, risk sizing, order intent, lifecycle evidence, and notification-domain events.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-close-gap-runtime-orchestrator-for-strategy-to-paper-e2e-flo/CONTEXT.md
@marketpilot/runtime_orchestrator.py
@marketpilot/scoring.py
@marketpilot/ranking.py
@marketpilot/risk.py
@marketpilot/order_lifecycle.py
@marketpilot/validation.py
@marketpilot/paper_modes.py
@marketpilot/reconciliation.py
@marketpilot/quantconnect_paper.py
@marketpilot/notification_events.py
@tests/test_scoring.py
@tests/test_ranking.py
@tests/test_portfolio_constraints.py
@tests/test_order_lifecycle_contract.py
@tests/test_reconciliation.py
@tests/test_paper_modes.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic runtime pipeline tests</name>
  <files>tests/test_runtime_orchestrator.py</files>
  <behavior>
    - Valid setup evidence can be scored and ranked without producing an order by itself.
    - Risk-accepted candidates produce `OrderIntent` only when paper mode, validation, reconciliation, and risk gates all pass.
    - Risk rejection emits a risk rejection event and no order intent.
    - Disabled or shadow-only paper mode emits evidence but no order intent.
    - Output preserves correlation IDs and no-fake authority labels.
  </behavior>
  <action>Build deterministic fixtures using existing `SetupResult`, `MarketPilotScore`, `RiskConfig`, `PortfolioSnapshot`, `ValidationGateDecision`, and fake QuantConnect paper snapshots. Tests must not require QuantConnect, network, Telegram, or credentials.</action>
  <verify>
    <automated>python -m pytest tests/test_runtime_orchestrator.py -q</automated>
  </verify>
  <done>Tests prove the runtime pipeline distinguishes signal evidence, score/rank output, risk approval, and paper-only order-intent readiness without submitting orders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reconciliation gate tests</name>
  <files>tests/test_runtime_reconciliation_gate.py</files>
  <behavior>
    - QuantConnect unavailable or mismatch blocks new entries.
    - Existing exit/protective obligations remain preserved in result evidence.
    - Reconciliation system events are emitted as notification-domain events, but delivery is not required for safety.
  </behavior>
  <action>Create tests around existing `reconcile_quantconnect_state()` behavior and runtime orchestration results.</action>
  <verify>
    <automated>python -m pytest tests/test_runtime_reconciliation_gate.py tests/test_reconciliation.py -q</automated>
  </verify>
  <done>Reconciliation tests prove QuantConnect mismatch/unavailability blocks new entries, preserves exit/protective obligations, and emits non-authoritative system events.</done>
</task>

<task type="auto">
  <name>Task 3: Implement pure pipeline composition</name>
  <files>marketpilot/runtime_orchestrator.py</files>
  <behavior>
    - Calls `score_setup_result()` and `rank_candidates()` for setup results.
    - Applies validation and paper-mode gates before risk/order-intent readiness.
    - Calls `evaluate_portfolio_risk()` only with QuantConnect-authoritative portfolio snapshot inputs.
    - Uses `make_order_idempotency_key()` and `OrderIntent` for audit intent evidence.
    - Emits `NotificationDomainEvent`s for risk rejection, sizing, order intent, and system/reconciliation outcomes.
  </behavior>
  <action>Implement the pipeline without LEAN imports or external side effects. Return typed results and events; do not deliver Telegram or submit orders here.</action>
  <verify>
    <automated>python -m pytest tests/test_runtime_orchestrator.py tests/test_runtime_reconciliation_gate.py -q</automated>
  </verify>
  <done>The pure pipeline composes scoring, ranking, validation, paper-mode, reconciliation, risk, idempotency, order-intent, and notification-event contracts without LEAN imports or external side effects.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_runtime_orchestrator.py tests/test_runtime_reconciliation_gate.py -q`
- `python -m pytest tests/test_scoring.py tests/test_ranking.py tests/test_portfolio_constraints.py tests/test_order_lifecycle_contract.py tests/test_paper_modes.py tests/test_reconciliation.py -q`
</verification>

<success_criteria>
- Strategy evidence can traverse setup -> scoring -> ranking -> risk -> order-intent evidence in deterministic tests.
- New entries are impossible without validation, paper mode, reconciliation, risk, and idempotency gates.
- Telegram/event emission remains non-authoritative.
</success_criteria>

<output>
Create `10.1-02-SUMMARY.md` when done.
</output>
