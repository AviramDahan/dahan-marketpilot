---
phase: 10.1-close-gap-runtime-orchestrator-for-strategy-to-paper-e2e-flo
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "marketpilot/runtime_orchestrator.py"
  - "marketpilot/setups/__init__.py"
  - "tests/test_runtime_contract.py"
  - "tests/test_runtime_setup_registry.py"
autonomous: true
requirements: [QC-03, UNI-01, UNI-02, UNI-03, UNI-04, UNI-05, REG-01, REG-02, REG-03, IND-01, IND-02, IND-03, IND-04, IND-05, SET-01, SET-02, SET-05, SET-07, MODE-01, MODE-02, MODE-03, TF-01, TF-02, TF-03, TF-04, TF-05, TF-06, TF-07]
must_haves:
  truths:
    - "D-10.1-01: Runtime orchestration lives in pure MarketPilot modules; LEAN/API objects stay at adapter boundaries."
    - "D-10.1-03: QuantConnect remains authoritative; runtime contracts can mirror evidence but cannot invent portfolio, order, fill, Paper, or backtest state."
    - "D-10.1-06: Runtime contract preserves StrategyMode, completed-bar timing, and MTF evidence without future bars or incomplete-bar assumptions."
    - "Setup registry covers Trend Pullback, Volume Breakout, and Relative Strength without adding new strategies or scoring weights."
  artifacts:
    - path: "marketpilot/runtime_orchestrator.py"
      provides: "Typed runtime input/result contracts, statuses, skipped reasons, and setup registry hooks"
    - path: "marketpilot/setups/__init__.py"
      provides: "Exports all implemented setup evaluators for runtime registration"
    - path: "tests/test_runtime_contract.py"
      provides: "Fail-closed runtime contract and no-fake-authority tests"
    - path: "tests/test_runtime_setup_registry.py"
      provides: "Setup registry coverage for all implemented setup modules"
  key_links:
    - from: "marketpilot/runtime_orchestrator.py"
      to: "marketpilot/timeframes.py"
      via: "StrategyMode and completed-bar timing fields"
      pattern: "StrategyMode|BarTimeframe|SetupTiming"
    - from: "marketpilot/runtime_orchestrator.py"
      to: "marketpilot/setups/*.py"
      via: "Setup evaluator registry"
      pattern: "SetupResult"
---

<objective>
Create the Phase 10.1 runtime contract foundation: typed orchestrator statuses, input/output dataclasses, setup registry, skipped/not-eligible evidence, and safe exports for all implemented setup evaluators.

Purpose: The milestone audit found that modules exist but do not connect. This first plan creates a safe contract surface for connecting them without introducing LEAN side effects, Paper order submission, or fake authority.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-close-gap-runtime-orchestrator-for-strategy-to-paper-e2e-flo/CONTEXT.md
@.planning/phases/10.1-close-gap-runtime-orchestrator-for-strategy-to-paper-e2e-flo/10.1-RESEARCH.md
@.planning/phases/10.1-close-gap-runtime-orchestrator-for-strategy-to-paper-e2e-flo/10.1-PATTERNS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@marketpilot/setups/base.py
@marketpilot/setups/trend_pullback.py
@marketpilot/setups/volume_breakout.py
@marketpilot/setups/relative_strength.py
@marketpilot/timeframes.py
@marketpilot/ranking.py
@marketpilot/risk.py
@marketpilot/order_lifecycle.py
@tests/test_scoring.py
@tests/test_ranking.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime contract tests</name>
  <files>tests/test_runtime_contract.py</files>
  <behavior>
    - A default runtime result can represent `not_configured`, `not_ready`, `blocked`, `shadow_only`, and `paper_intent_ready` without side effects.
    - Runtime evidence includes correlation ID, strategy mode, source authority, skipped reasons, ranked candidates, risk decisions, order intents, and notification events.
    - No runtime result claims executed QuantConnect orders, fills, deployment, backtest, or portfolio state unless supplied from QuantConnect-authoritative inputs.
    - Runtime status and skipped reasons are stable strings suitable for dashboard/report evidence.
  </behavior>
  <action>Create failing tests that define the runtime contract before implementation. Use deterministic fixtures only. Do not import `AlgorithmImports`, QuantConnect, Telegram network, Render, or credentials.</action>
  <verify>
    <automated>python -m pytest tests/test_runtime_contract.py -q</automated>
  </verify>
  <done>Runtime contract tests fail before implementation for missing typed statuses/result evidence, then pass with no side effects, no external calls, and no fake authority fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add setup registry tests and exports</name>
  <files>tests/test_runtime_setup_registry.py, marketpilot/setups/__init__.py</files>
  <behavior>
    - Registry includes `trend_pullback`, `volume_breakout`, and `relative_strength`.
    - Registry returns evaluator metadata and never auto-enables a Combined Swing strategy.
    - Setup exports include all implemented setup evaluators without changing their behavior.
  </behavior>
  <action>Add tests first, then update `marketpilot/setups/__init__.py` and runtime registry helpers. Preserve existing import compatibility.</action>
  <verify>
    <automated>python -m pytest tests/test_runtime_setup_registry.py tests/test_trend_pullback_contract.py tests/test_volume_breakout_contract.py tests/test_relative_strength_contract.py -q</automated>
  </verify>
  <done>Setup registry exposes Trend Pullback, Volume Breakout, and Relative Strength evaluators while preserving existing setup contract tests.</done>
</task>

<task type="auto">
  <name>Task 3: Implement pure runtime dataclasses and status helpers</name>
  <files>marketpilot/runtime_orchestrator.py</files>
  <behavior>
    - Use frozen dataclasses and enums consistent with existing modules.
    - Keep ordinary ineligibility as typed status/skipped reasons, not exceptions.
    - Add no order submission methods, broker adapters, LEAN imports, HTTP clients, or dashboard mutation hooks.
  </behavior>
  <action>Implement only the contract skeleton and setup registry hooks. Leave full pipeline execution for Plan 10.1-02.</action>
  <verify>
    <automated>python -m pytest tests/test_runtime_contract.py tests/test_runtime_setup_registry.py -q</automated>
  </verify>
  <done>`marketpilot/runtime_orchestrator.py` provides side-effect-free dataclasses, enums, setup registry helpers, and skipped/not-eligible evidence suitable for later pipeline plans.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_runtime_contract.py tests/test_runtime_setup_registry.py -q`
- `python -m pytest tests/test_trend_pullback_contract.py tests/test_volume_breakout_contract.py tests/test_relative_strength_contract.py -q`
</verification>

<success_criteria>
- Runtime contract exists and is side-effect-free.
- Setup registry covers all implemented setup evaluators.
- No fake QuantConnect, Paper, dashboard, or Telegram evidence can be represented as passed execution by default.
</success_criteria>

<output>
Create `.planning/phases/10.1-close-gap-runtime-orchestrator-for-strategy-to-paper-e2e-flo/10.1-01-SUMMARY.md` when done.
</output>
