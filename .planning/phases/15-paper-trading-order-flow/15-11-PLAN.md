---
phase: 15-paper-trading-order-flow
plan: "11"
type: execute
wave: 10
depends_on:
  - "15-10"
gap_closure: true
files_modified:
  - scripts/qc_object_store_signal_smoke.py
  - tests/test_qc_api.py
  - docs/paper_trading_order_flow.md
  - docs/testing.md
  - .planning/phases/15-paper-trading-order-flow/15-UAT.md
  - .planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md
autonomous: false
requirements:
  - PTD-05
  - FT-01
  - FT-02
  - FT-03
  - FT-04
  - SAFE-05
must_haves:
  truths:
    - "Phase 15-10 externally observed Object Store receipt, acceptance, and a Paper `Submitted` order event in live logs."
    - "`/live/orders/read` is the authoritative order/fill/rejection evidence source and updates on a slower snapshot cadence than live logs."
    - "The Phase 15-10 smoke submitted while the US equity market was closed, so QuantConnect converted the market order for next market open."
    - "Temporary Paper deployments must be stopped automatically unless an operator explicitly chooses a longer observation window."
    - "No real-money brokerage, broker REST order bypass, dashboard order entry, Telegram authority, unsupported asset class, leverage, margin, short selling, options, futures, crypto, or Forex path may be added."
research_anchors:
  - "https://www.quantconnect.com/docs/v2/cloud-platform/api-reference/live-management/read-live-algorithm/orders"
---

<objective>
Close the post-receipt safety gap by making the Object Store fallback smoke
safer for long-running order observation: stop temporary deployments by default,
record slower `/live/orders/read` snapshot cadence, and prepare an explicit
operator-controlled path for market-hours or next-open order/fill/rejection
evidence.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-paper-trading-order-flow/15-UAT.md
@.planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md
@.planning/phases/15-paper-trading-order-flow/15-10-SUMMARY.md
@scripts/qc_object_store_signal_smoke.py
@tests/test_qc_api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deployment stop safety and explicit keep-running override</name>
  <files>scripts/qc_object_store_signal_smoke.py, tests/test_qc_api.py</files>
  <behavior>
    - The smoke should stop a temporary deployment by default after polling.
    - Operators who need next-open or market-hours order observation must pass
      an explicit keep-running flag.
    - The result must include `stop_attempted` and `stop_success` fields when a
      deployment was created.
    - Dry-run and diagnose-only modes must not attempt stop.
    - Tests must prove default stop and explicit keep-running behavior.
  </behavior>
  <action>
    Add a `stop_after_deploy` option and CLI flag such as `--keep-running`.
    Stop by `projectId` through the existing paper-gated client wrapper after
    cleanup. Preserve sanitized output.
  </action>
  <verify>
    <automated>python -m pytest tests/test_qc_api.py -q</automated>
    <manual>python scripts/qc_object_store_signal_smoke.py --dry-run --skip-deploy</manual>
  </verify>
  <done>
    Temporary Paper deployments no longer depend on a manual stop step after
    ordinary external smokes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document order-authority follow-up gate</name>
  <files>docs/paper_trading_order_flow.md, docs/testing.md, .planning/phases/15-paper-trading-order-flow/15-UAT.md, .planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md</files>
  <behavior>
    - Docs must explain that receipt/Submitted log evidence is not fill/order
      authority.
    - Docs must explain that `/live/orders/read` snapshots update slower and may
      require market-hours or next-open observation.
    - Docs must never suggest leaving deployments running unless the operator
      explicitly accepts the cost/safety tradeoff.
  </behavior>
  <action>
    Update verification and user-facing docs with the auto-stop default, the
    explicit keep-running override, and the exact remaining external evidence
    gate.
  </action>
  <verify>
    <automated>python -m pytest tests/test_qc_api.py tests/test_lean_command_flow.py tests/test_paper_order_flow_e2e.py tests/test_paper_order_flow.py tests/test_sync.py -q</automated>
  </verify>
  <done>
    Future operators and AIs know the next external action without confusing log
    receipt with authoritative order/fill state.
  </done>
</task>

</tasks>

<success_criteria>
- External Object Store smokes stop temporary deployments by default.
- `--keep-running` or equivalent is required for next-open order observation.
- Docs and UAT clearly state that `/live/orders/read` order/fill/rejection
  evidence remains pending.
- No secret leakage, fake orders, fake fills, or real-money paths are introduced.
</success_criteria>

<output>
Create `.planning/phases/15-paper-trading-order-flow/15-11-SUMMARY.md` when done.
</output>
