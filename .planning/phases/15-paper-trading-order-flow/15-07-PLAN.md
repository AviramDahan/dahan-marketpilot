---
phase: 15-paper-trading-order-flow
plan: "07"
type: execute
wave: 6
depends_on:
  - "15-06"
gap_closure: true
files_modified:
  - scripts/qc_command_smoke.py
  - scripts/qc_command_dispatch_probe.py
  - marketpilot/qc_api.py
  - lean/main.py
  - marketpilot/lean_command_receiver.py
  - tests/test_qc_api.py
  - tests/test_lean_command_flow.py
  - tests/test_paper_order_flow_e2e.py
  - docs/paper_trading_order_flow.md
  - docs/testing.md
  - .planning/phases/15-paper-trading-order-flow/15-UAT.md
  - .planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md
autonomous: false
requirements:
  - PTD-02
  - PTD-05
  - FT-01
  - FT-02
  - FT-03
  - FT-04
  - SAFE-05
user_setup:
  - service: quantconnect
    why: "Credentialed paper-only command dispatch diagnosis and final external smoke"
    env_vars:
      - name: QUANTCONNECT_USER_ID
        source: "QuantConnect account API credentials"
      - name: QUANTCONNECT_API_TOKEN
        source: "QuantConnect account API credentials"
      - name: QC_PROJECT_ID
        source: "QuantConnect project selected for simulated Paper Trading"
      - name: QC_DEPLOY_ID
        source: "Running QuantConnect paper deployment id, set by smoke script or operator"
      - name: QC_NODE_ID
        source: "QuantConnect paper live node id"
      - name: QC_VERSION_ID
        source: "QuantConnect LEAN version id"
must_haves:
  truths:
    - "Phase 15-06 proved `/live/commands/create` API acceptance but still observed 0 live logs and 0 live orders after typed command polling."
    - "Official QuantConnect Commands docs state that a generic payload without `$type` invokes `on_command`, while `$type` invokes a registered Command class and other flat payload keys become command members."
    - "The next execution must isolate account/API command dispatch from MarketPilot receiver behavior before adding another receiver normalization variant."
    - "Any fallback delivery mechanism must remain paper-only, audited, idempotent, and QuantConnect-authoritative for orders/fills."
    - "No real-money brokerage, direct broker/order REST path, unsupported asset class, leverage, margin, short selling, options, futures, crypto, or Forex path may be added."
    - "No external result can mark Phase 15 complete unless there is real callback-to-order, callback-to-rejection, or proven fallback-to-order/rejection evidence from QuantConnect Paper."
  artifacts:
    - path: "scripts/qc_command_smoke.py"
      provides: "Existing guarded command smoke harness"
      contains: "command_label"
    - path: "scripts/qc_command_dispatch_probe.py"
      provides: "New isolated command-dispatch diagnostic harness"
      contains: "SIMULATED PAPER TRADING ONLY"
    - path: "lean/main.py"
      provides: "MarketPilot running Paper algorithm adapter"
      contains: "on_command"
    - path: ".planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md"
      provides: "Authoritative requirement evidence matrix"
      contains: "blocked_external_callback_not_verified"
  key_links:
    - from: "scripts/qc_command_dispatch_probe.py"
      to: "marketpilot/qc_api.py"
      via: "cloud file sync, compile/deploy, command create, logs/orders read"
      pattern: "create_live_command|read_live_logs|read_live_orders_page"
    - from: "scripts/qc_command_smoke.py"
      to: "lean/main.py"
      via: "MarketPilot command dispatch smoke after diagnostic branch"
      pattern: "marketpilot_signal|on_command"
    - from: ".planning/phases/15-paper-trading-order-flow/15-UAT.md"
      to: ".planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md"
      via: "same sanitized external evidence and status"
      pattern: "callback|order|rejection"
research_anchors:
  - "https://www.quantconnect.com/docs/v2/writing-algorithms/live-trading/commands"
  - "https://www.quantconnect.com/docs/v2/writing-algorithms/object-store"
---

<objective>
Close the remaining Phase 15 external verification blocker by proving one of
two outcomes:

1. QuantConnect Cloud Commands dispatch reaches a Python algorithm and can be
   corrected for MarketPilot's live Paper algorithm, then verified through a
   real callback-to-order or callback-to-rejection smoke.
2. QuantConnect Cloud Commands dispatch does not reach the algorithm in this
   account/project shape, in which case the plan must either prove a supported
   paper-only fallback delivery mechanism or leave a precise external blocker
   without claiming Phase 15 complete.

This plan exists because Phase 15-06 already covered local receiver variants,
cloud sync, compile, live create, Commands API acceptance, log polling, and
order polling. Repeating the same command shape is not useful. The next gap
closure must isolate generic command dispatch from MarketPilot receiver logic.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-paper-trading-order-flow/15-CONTEXT.md
@.planning/phases/15-paper-trading-order-flow/15-RESEARCH.md
@.planning/phases/15-paper-trading-order-flow/15-PATTERNS.md
@.planning/phases/15-paper-trading-order-flow/15-UAT.md
@.planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md
@.planning/phases/15-paper-trading-order-flow/15-06-SUMMARY.md
@scripts/qc_command_smoke.py
@lean/main.py
@marketpilot/qc_api.py
@marketpilot/lean_command_receiver.py
@marketpilot/paper_command_models.py
@marketpilot/paper_order_flow.py
@tests/test_qc_api.py
@tests/test_lean_command_flow.py
@tests/test_paper_order_flow_e2e.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an isolated QuantConnect command-dispatch probe</name>
  <files>scripts/qc_command_dispatch_probe.py, marketpilot/qc_api.py, tests/test_qc_api.py, docs/testing.md</files>
  <behavior>
    - The probe refuses to run unless an explicit enable env var is set.
    - The probe never prints credentials, auth headers, or raw secret-bearing environment values.
    - The probe can run in `--dry-run` mode without network calls.
    - The probe can produce a minimal Python algorithm body that only logs a sanitized `on_command` receipt and returns `True`; it must not submit orders.
    - The probe can send official-doc generic command payloads without `$type`, and can optionally send flat `$type` payloads only as diagnostics.
    - The probe records command API success, log evidence, deployment id, compile id, command shape label, timestamps, and final status separately.
  </behavior>
  <action>
    Create `scripts/qc_command_dispatch_probe.py` as a guarded diagnostic
    runner for an isolated no-order command echo algorithm. Reuse existing
    `QCApiClient` wrappers where available and add narrow wrappers only for
    already-needed file sync/compile/read diagnostic calls if missing. The
    runner must make it clear whether it is using the existing QC project or a
    temporary diagnostic project path, and it must preserve/restore any project
    file it overwrites.
  </action>
  <verify>
    <automated>python -m pytest tests/test_qc_api.py -q</automated>
  </verify>
  <done>
    A disabled-by-default diagnostic can prove whether a trivial Python
    `on_command` receives a generic Commands API payload without placing any
    order.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align MarketPilot command payloads with official dispatch semantics</name>
  <files>scripts/qc_command_smoke.py, marketpilot/lean_command_receiver.py, tests/test_lean_command_flow.py, tests/test_paper_order_flow_e2e.py</files>
  <behavior>
    - The default MarketPilot smoke command is a flat generic payload without `$type`, matching official `on_command` semantics.
    - Any `$type` diagnostic uses flat member fields, not an unverified nested `parameters` object, unless the nested variant is retained only as a historical regression case.
    - Receiver normalization remains strict: paper-only, freshness, supported symbol, positive integer quantity, safe tag, and duplicate idempotency gates still apply.
    - Rejection paths are observable through sanitized state/log evidence but never dump full payloads or secrets.
  </behavior>
  <action>
    Update the command-smoke payload matrix so the primary path mirrors the
    official generic command contract: no `$type`, flat keys, and MarketPilot
    trace fields. Keep typed diagnostics clearly labeled as non-authoritative
    probes. Add or adjust tests so both current and officially aligned payload
    shapes are covered, while unsafe typed or order-like fields remain rejected.
  </action>
  <verify>
    <automated>python -m pytest tests/test_lean_command_flow.py tests/test_paper_order_flow_e2e.py tests/test_qc_api.py -q</automated>
  </verify>
  <done>
    The MarketPilot command smoke no longer depends on an envelope shape that
    the official docs do not require for `on_command`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run branch-based external dispatch diagnosis and choose the safe route</name>
  <files>.planning/phases/15-paper-trading-order-flow/15-UAT.md, .planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md, docs/paper_trading_order_flow.md, docs/testing.md</files>
  <action>
    With operator credentials configured outside repository files, run the
    isolated command-dispatch probe first. If the no-order echo algorithm logs
    the command receipt, then redeploy the MarketPilot Paper algorithm and run
    the aligned generic `marketpilot_signal` smoke. If that produces a real
    order or explicit rejection, record sanitized callback/order/rejection
    evidence and update Phase 15 verification accordingly.

    If the no-order echo algorithm does not receive a generic command despite
    `/live/commands/create` success, stop treating MarketPilot receiver code as
    the likely cause. Record the account/project/API dispatch blocker and
    proceed to Task 4 only if a supported fallback path can be implemented and
    externally proven without bypassing paper-only safety.
  </action>
  <what-built>
    A diagnostic branch that distinguishes QuantConnect command dispatch
    failure from MarketPilot payload/receiver failure.
  </what-built>
  <how-to-verify>
    1. Confirm dry-run output is sanitized.
    2. Confirm the diagnostic algorithm compiles and deploys to Paper.
    3. Confirm the generic command API call returns success or records the exact sanitized failure.
    4. Confirm log polling either contains the diagnostic receipt marker or records 0 observed receipts.
    5. If dispatch works, confirm MarketPilot command smoke records callback-to-order or callback-to-rejection evidence separately from API acceptance.
  </how-to-verify>
  <resume-signal>
    Continue automatically if the diagnostic identifies the branch. Do not ask
    the user to choose between technical branches unless payment, account UI
    approval, or a destructive QC action is required.
  </resume-signal>
  <verify>
    <automated>python -m pytest tests/test_qc_api.py tests/test_lean_command_flow.py tests/test_paper_order_flow_e2e.py tests/test_paper_order_flow.py tests/test_sync.py -q</automated>
  </verify>
  <done>
    The UAT and verification docs state whether generic Commands dispatch works
    in QuantConnect Cloud for this account/project, and whether MarketPilot
    Paper callback-to-order/rejection was externally proven.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Prepare a supported fallback only if Commands dispatch is externally blocked</name>
  <files>marketpilot/qc_api.py, lean/main.py, marketpilot/lean_command_receiver.py, tests/test_lean_command_flow.py, docs/paper_trading_order_flow.md, .planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md</files>
  <behavior>
    - This task is skipped if Task 3 proves MarketPilot callback-to-order or callback-to-rejection through Commands.
    - Any fallback must be based on a supported QuantConnect mechanism verified against current official docs during execution.
    - Object Store polling may be considered only if an external writer path is supported and can be proven with sanitized paper evidence.
    - The fallback must preserve the same MarketPilot validation gates and idempotency semantics as `on_command`.
    - The fallback must not make local files, dashboard state, or Telegram delivery authoritative for orders/fills.
  </behavior>
  <action>
    Research and implement the thinnest supported fallback signal inbox only if
    generic Commands dispatch is externally blocked. Prefer a QuantConnect
    Object Store or similarly official Cloud-supported path if it can be written
    externally and read from the live algorithm. If no supported external writer
    path exists, do not invent one; record `blocked_external_delivery_path_not_verified`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_lean_command_flow.py tests/test_qc_api.py tests/test_paper_order_flow_e2e.py -q</automated>
  </verify>
  <done>
    Either a supported fallback is implemented and externally proven through
    Paper callback-to-order/rejection evidence, or the docs clearly record that
    Phase 15 remains blocked on a QuantConnect delivery-path constraint.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local diagnostic scripts to QuantConnect Cloud | Sends explicit paper-only diagnostic requests and reads sanitized logs/orders. |
| QuantConnect command dispatch to Python algorithm | Determines whether API success results in algorithm callback execution. |
| Fallback delivery path to LEAN algorithm | May carry a signal only if supported, paper-only, idempotent, and externally proven. |
| UAT evidence to future operators/AIs | Must distinguish API acceptance, callback receipt, order/rejection, fill, and portfolio authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07-01 | Spoofing | Command dispatch diagnosis | mitigate | Use unique correlation ids and require log/order evidence, not API success alone. |
| T-15-07-02 | Tampering | QC project file probe | mitigate | Preserve/restore overwritten files and record sanitized file sync/compile ids. |
| T-15-07-03 | Information Disclosure | Diagnostic output | mitigate | Redact env secret markers and never print auth headers or tokens. |
| T-15-07-04 | Elevation of Privilege | Fallback delivery | mitigate | Permit only paper-only, audited, supported delivery; reject broker/order REST bypasses. |
| T-15-07-05 | Repudiation | Verification docs | mitigate | Record sanitized timestamps, deploy ids, command labels, log markers, and order/rejection evidence. |
| T-15-07-SC | Tampering | Dependencies | accept | No new package installation planned; use stdlib, existing client code, and pytest. |
</threat_model>

<verification>
Primary local verification:

`python -m pytest tests/test_qc_api.py tests/test_lean_command_flow.py tests/test_paper_order_flow_e2e.py tests/test_paper_order_flow.py tests/test_sync.py -q`

External verification requires user-managed QuantConnect credentials configured
only in environment variables. The external smoke must record sanitized evidence
in UAT/verification and must never commit credentials, tokens, auth headers, or
raw secret-bearing payloads.
</verification>

<success_criteria>
- A no-order Python command echo algorithm proves whether generic Commands API dispatch reaches `on_command` in this QC account/project.
- MarketPilot's smoke payload defaults to the official generic `on_command` contract.
- If dispatch works, Phase 15 records real callback-to-order or callback-to-rejection evidence from Paper.
- If dispatch does not work, the blocker is recorded precisely and any fallback is implemented only when supported and externally proven.
- No fake fills, fake orders, real-money path, unsafe brokerage path, or secret leakage is introduced.
</success_criteria>

<output>
Create `.planning/phases/15-paper-trading-order-flow/15-07-SUMMARY.md` when done.
</output>
