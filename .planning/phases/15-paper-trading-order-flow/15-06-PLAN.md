---
phase: 15-paper-trading-order-flow
plan: "06"
type: execute
wave: 5
depends_on:
  - "15-05"
gap_closure: true
files_modified:
  - lean/main.py
  - marketpilot/qc_api.py
  - scripts/qc_command_smoke.py
  - tests/test_lean_command_flow.py
  - tests/test_qc_api.py
  - docs/paper_trading_order_flow.md
  - docs/testing.md
  - .planning/phases/15-paper-trading-order-flow/15-UAT.md
  - .planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md
autonomous: false
requirements:
  - PTD-02
  - PTD-05
  - FT-01
  - FT-03
  - FT-04
  - SAFE-05
user_setup:
  - service: quantconnect
    why: "Credentialed paper-only callback-to-order smoke verification"
    env_vars:
      - name: QUANTCONNECT_USER_ID
        source: "QuantConnect account API credentials"
      - name: QUANTCONNECT_API_TOKEN
        source: "QuantConnect account API credentials"
      - name: QC_PROJECT_ID
        source: "QuantConnect project selected for simulated Paper Trading"
      - name: QC_DEPLOY_ID
        source: "Fresh running QuantConnect paper deployment id, set by smoke script or operator"
      - name: QC_NODE_ID
        source: "QuantConnect paper live node id"
      - name: QC_VERSION_ID
        source: "QuantConnect LEAN version id"
must_haves:
  truths:
    - "QuantConnect `/live/commands/create` returned success for a `marketpilot_signal`, but no `on_command` debug log or live order appeared during polling."
    - "Official QuantConnect docs distinguish generic `on_command(self, data)` handling from API command examples that use typed payloads such as `$type: OrderCommand`; this gap must test the boundary instead of assuming API success equals callback execution."
    - "No real-money brokerage, dashboard order entry, direct broker/order REST path, unsupported asset class, leverage, margin, short selling, options, futures, crypto, or Forex path may be added."
    - "Any external smoke result must record sanitized ids, timestamps, command shape label, callback/log evidence, and order/rejection evidence; API acceptance alone is not enough."
  artifacts:
    - path: "scripts/qc_command_smoke.py"
      provides: "Reusable sanitized QuantConnect command smoke runner"
      contains: "marketpilot_signal"
    - path: "lean/main.py"
      provides: "Observable paper-only command receiver and debug evidence"
      contains: "on_command"
    - path: ".planning/phases/15-paper-trading-order-flow/15-UAT.md"
      provides: "External callback-to-order smoke outcome"
      contains: "blocked_external_callback_not_verified"
    - path: ".planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md"
      provides: "Requirement matrix updated from command callback smoke"
      contains: "PTD-02"
  key_links:
    - from: "scripts/qc_command_smoke.py"
      to: "marketpilot/qc_api.py"
      via: "safe command delivery, live logs, and live orders polling"
      pattern: "create_live_command|live/logs/read|read_live_orders_page"
    - from: "lean/main.py"
      to: "marketpilot/lean_command_receiver.py"
      via: "normalize and validate command payload before paper order"
      pattern: "normalize_marketpilot_command|validate_marketpilot_command"
    - from: ".planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md"
      to: ".planning/phases/15-paper-trading-order-flow/15-UAT.md"
      via: "same sanitized external evidence status"
      pattern: "callback"
---

<objective>
Close the Phase 15 QuantConnect command callback gap where `/live/commands/create`
accepts a command but the running Paper algorithm produces no observable
`on_command` log and no live order.

Purpose: Phase 15 already has offline command receiver coverage, cloud file sync,
cloud compile, Paper `/live/create`, read-only live polling, and command API
acceptance. The remaining verification gap is narrower: prove whether
QuantConnect Cloud routes the submitted command shape into Python `on_command`,
or whether the project must use a different supported command envelope or
operator route for external paper command smoke.

Output: A deterministic smoke runner, tighter LEAN command diagnostics,
optionally corrected command envelope handling, and synchronized UAT/verification
evidence. If QuantConnect still accepts but does not dispatch the command, record
that as a precise external blocker without claiming order execution.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-paper-trading-order-flow/15-CONTEXT.md
@.planning/phases/15-paper-trading-order-flow/15-RESEARCH.md
@.planning/phases/15-paper-trading-order-flow/15-PATTERNS.md
@.planning/phases/15-paper-trading-order-flow/15-UAT.md
@.planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md
@.planning/phases/15-paper-trading-order-flow/15-05-SUMMARY.md
@lean/main.py
@marketpilot/qc_api.py
@marketpilot/lean_command_receiver.py
@marketpilot/paper_command_models.py
@marketpilot/paper_order_flow.py
@tests/test_lean_command_flow.py
@tests/test_qc_api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repeatable command-smoke diagnostics without external side effects by default</name>
  <files>scripts/qc_command_smoke.py, tests/test_qc_api.py, docs/testing.md</files>
  <behavior>
    - The script refuses to run unless `MARKETPILOT_QC_COMMAND_SMOKE_ENABLED=1`.
    - The script reads required QuantConnect env vars, but never prints or stores secret values.
    - The script can run in `--dry-run` mode with no network calls and print only sanitized planned actions.
    - The script supports at least two command envelope labels: `marketpilot_signal` and `typed_order_command_probe`.
    - The script records command API success, live log polling, live order polling, deployment id, compile id, and timestamps in sanitized JSON.
  </behavior>
  <action>Create `scripts/qc_command_smoke.py` as an operator-run smoke helper. It must use existing `QCApiClient` wrappers where available and narrow direct `_make_request()` calls only for allowed diagnostic read endpoints such as logs if wrappers do not exist yet. Add tests for dry-run output, enable-gate refusal, redaction, and payload shape construction. Do not run external calls in tests.</action>
  <verify>
    <automated>python -m pytest tests/test_qc_api.py tests/test_lean_command_flow.py -q</automated>
  </verify>
  <done>A reusable command-smoke helper exists, is disabled by default, and has deterministic offline tests for payload construction and secret-safe output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make LEAN command receiver diagnostics unambiguous and schema-tolerant</name>
  <files>lean/main.py, marketpilot/lean_command_receiver.py, tests/test_lean_command_flow.py</files>
  <behavior>
    - `on_command` logs a sanitized receipt marker before payload parsing.
    - The receiver accepts both lower-case dict payloads and common QuantConnect dynamic casing variants without weakening paper-only, expiry, symbol, quantity, or duplicate gates.
    - If QuantConnect passes a typed envelope with nested parameters, the receiver can unwrap a `marketpilot_signal` payload only when the nested payload still satisfies all existing validation.
    - Rejections log only reason and safe symbol/context, never full payloads or secrets.
  </behavior>
  <action>Add failing tests first for command payload variants that may arrive from QuantConnect dynamic commands: attribute-style PascalCase fields, `$type` envelope with `parameters`, and a nested `marketpilot_signal` object. Implement only the minimal normalization needed to pass these tests. Preserve rejection behavior for non-paper commands, expired commands, duplicate idempotency keys, unsupported symbols, and non-integer quantities.</action>
  <verify>
    <automated>python -m pytest tests/test_lean_command_flow.py tests/test_lean_runtime_bridge_static.py -q</automated>
  </verify>
  <done>LEAN command normalization is robust to likely QuantConnect dynamic command shapes while preserving all safety gates and sanitized diagnostics.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run credentialed paper-only callback smoke and record sanitized evidence</name>
  <files>.planning/phases/15-paper-trading-order-flow/15-UAT.md, .planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md, docs/paper_trading_order_flow.md, docs/testing.md</files>
  <action>With operator credentials configured outside repository files, stop any superseded Paper deployment if needed, sync only required project files to QuantConnect, compile, deploy to a Paper node, then run `scripts/qc_command_smoke.py` using the safest command envelope. Poll `/live/logs/read` and `/live/orders/read`. If an order or explicit rejection is observed, record sanitized ids, timestamps, command envelope label, callback/log evidence, order/rejection status, and remaining risks. If API success occurs without callback/order evidence, keep status `blocked_external_callback_not_verified` and record the exact sanitized evidence. Never record credential values, auth headers, full payload secrets, or profitability claims.</action>
  <what-built>Diagnostic command-smoke runner and safer LEAN command receiver normalization.</what-built>
  <how-to-verify>
    1. Confirm local tests pass.
    2. Confirm cloud compile returns `BuildSuccess`.
    3. Confirm Paper deployment reaches `Running`.
    4. Confirm command API result, callback/log evidence, and live order/rejection evidence are recorded separately.
    5. Confirm UAT/verification does not mark Phase 15 complete unless real callback-to-order or callback-to-rejection evidence exists.
  </how-to-verify>
  <resume-signal>Type "approved" only if the sanitized external evidence proves callback-to-order or callback-to-rejection behavior. Otherwise keep the blocker and describe the observed command API/log/order evidence.</resume-signal>
  <verify>
    <automated>python -m pytest tests/test_qc_api.py tests/test_lean_command_flow.py tests/test_paper_order_flow_e2e.py tests/test_paper_order_flow.py tests/test_sync.py -q</automated>
  </verify>
  <done>Phase 15 external command callback status is either passed with real QuantConnect evidence or precisely blocked with sanitized proof that API acceptance did not dispatch to observable callback/order behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local smoke runner to QuantConnect Cloud | Sends operator-approved paper-only commands and reads logs/orders. |
| QuantConnect callback to LEAN paper order | Only `on_command` may translate accepted MarketPilot signals into LEAN paper `market_order`. |
| UAT evidence to future AI/operator | Sanitized evidence must distinguish API acceptance, callback receipt, order/rejection, and fills. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-01 | Spoofing | External smoke evidence | mitigate | Record command API acceptance separately from callback logs and live orders; do not treat API success as order execution. |
| T-15-06-02 | Tampering | Command payload normalization | mitigate | Add tests for supported variants and unsafe variants; preserve paper-only, expiry, duplicate, symbol, and quantity gates. |
| T-15-06-03 | Information Disclosure | Smoke script output | mitigate | Refuse by default, print sanitized ids only, never print env secret values or auth headers. |
| T-15-06-04 | Elevation of Privilege | Command envelope alternatives | mitigate | Test typed/probe envelopes only within QuantConnect Paper; do not add real brokerage, dashboard mutation, or external broker/order REST paths. |
| T-15-06-05 | Repudiation | GSD UAT/verification | mitigate | Record exact sanitized compile/deploy/command/log/order timestamps and statuses. |
| T-15-06-SC | Tampering | package installs | accept | No new package installation; use stdlib, existing `requests`, existing `QCApiClient`, and pytest. |
</threat_model>

<verification>
Run `python -m pytest tests/test_qc_api.py tests/test_lean_command_flow.py tests/test_paper_order_flow_e2e.py tests/test_paper_order_flow.py tests/test_sync.py -q`.

Credentialed verification requires `MARKETPILOT_QC_COMMAND_SMOKE_ENABLED=1` and user-managed QuantConnect env vars outside repository files. Record the smoke result in UAT/verification, not in code fixtures.
</verification>

<success_criteria>
- A repeatable disabled-by-default smoke helper captures sanitized command API, callback/log, and live order/rejection evidence.
- LEAN command receiver handles likely QuantConnect dynamic payload variants without weakening safety gates.
- External evidence proves callback-to-order or callback-to-rejection behavior, or the blocker remains precisely recorded as `blocked_external_callback_not_verified`.
- No secrets, auth headers, real-money paths, or fake execution claims are introduced.
</success_criteria>

<output>
Create `.planning/phases/15-paper-trading-order-flow/15-06-SUMMARY.md` when done.
</output>
