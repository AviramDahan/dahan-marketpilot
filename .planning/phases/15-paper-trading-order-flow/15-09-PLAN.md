---
phase: 15-paper-trading-order-flow
plan: "09"
type: execute
wave: 8
depends_on:
  - "15-08"
gap_closure: true
files_modified:
  - scripts/qc_object_store_signal_smoke.py
  - tests/test_qc_api.py
  - docs/paper_trading_order_flow.md
  - docs/testing.md
  - .planning/phases/15-paper-trading-order-flow/15-UAT.md
  - .planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md
autonomous: false
requirements:
  - PTD-02
  - PTD-05
  - FT-01
  - FT-02
  - FT-03
  - FT-04
  - SAFE-05
user_setup:
  - service: quantconnect
    why: "Object Store organization/permission remediation or paid-tier unlock"
    env_vars:
      - name: QUANTCONNECT_USER_ID
        source: "QuantConnect account API credentials"
      - name: QUANTCONNECT_API_TOKEN
        source: "QuantConnect account API credentials"
      - name: QC_PROJECT_ID
        source: "QuantConnect project selected for simulated Paper Trading"
      - name: QC_ORGANIZATION_ID
        source: "QuantConnect organization id, auto-discovered when absent"
must_haves:
  truths:
    - "Phase 15-08 proved local Object Store fallback behavior but external `/object/set` returned `Organization not found`."
    - "The current smoke wastes time if it deploys before proving Object Store write access."
    - "Official QuantConnect Object Store API upload format uses multipart `objectData` with `organizationId` and `key`."
    - "QuantConnect Object Store permissions/paid-tier state may block access even when account/project reads expose an organization id."
    - "A failed Object Store write is not algorithm delivery evidence and must not trigger a new Paper deployment."
    - "No real-money brokerage, broker REST order bypass, dashboard order entry, Telegram authority, unsupported asset class, leverage, margin, short selling, options, futures, crypto, or Forex path may be added."
  artifacts:
    - path: "scripts/qc_object_store_signal_smoke.py"
      provides: "Object Store fallback smoke runner"
      contains: "MARKETPILOT_QC_OBJECT_STORE_SMOKE_ENABLED"
    - path: ".planning/phases/15-paper-trading-order-flow/15-08-SUMMARY.md"
      provides: "Current Object Store write blocker evidence"
      contains: "Organization not found"
    - path: ".planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md"
      provides: "Authoritative external evidence matrix"
      contains: "blocked_external_object_store_write_not_verified"
research_anchors:
  - "https://www.quantconnect.com/docs/v2/cloud-platform/api-reference/object-store-management/upload-object-store-files"
  - "https://www.quantconnect.com/docs/v2/cloud-platform/organizations/object-store"
  - "https://www.quantconnect.com/docs/v2/lean-cli/api-reference/lean-cloud-object-store-list"
---

<objective>
Close the Phase 15-08 diagnostic ambiguity by making Object Store access a
fail-fast preflight before any Paper deployment is created. If the current
QuantConnect organization still cannot write Object Store objects, record a
precise paid-tier/permission blocker and provide an operator runbook. If
Object Store write becomes available, continue the existing fallback smoke to
algorithm receipt and `/live/orders/read` evidence.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-paper-trading-order-flow/15-CONTEXT.md
@.planning/phases/15-paper-trading-order-flow/15-UAT.md
@.planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md
@.planning/phases/15-paper-trading-order-flow/15-08-SUMMARY.md
@scripts/qc_object_store_signal_smoke.py
@marketpilot/qc_api.py
@lean/main.py
@tests/test_qc_api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Object Store preflight and error classification</name>
  <files>scripts/qc_object_store_signal_smoke.py, tests/test_qc_api.py</files>
  <behavior>
    - The smoke must attempt the Object Store write before any compile/deploy.
    - If `/object/set` fails, the smoke must not deploy.
    - Add a `--diagnose-only` mode that tests account/project/organization id,
      Object Store upload/properties/delete, and exits without compile/deploy
      or live log/order polling.
    - Classify `Organization not found` as an external organization/permission
      blocker, not as algorithm delivery failure.
    - Preserve sanitized output and never print credentials or auth headers.
  </behavior>
  <action>
    Reorder `qc_object_store_signal_smoke.py` so Object Store write/properties
    preflight happens before compile/deploy. Add a machine-readable
    `object_store_status` and final status classification. Add tests with a
    fake client proving deployment is skipped when the preflight fails and
    cleanup runs when a test object is created.
  </action>
  <verify>
    <automated>python -m pytest tests/test_qc_api.py -q</automated>
    <manual>python scripts/qc_object_store_signal_smoke.py --dry-run --skip-deploy --diagnose-only</manual>
  </verify>
  <done>
    The smoke runner no longer spends a Paper deployment when Object Store
    organization/permission access is unavailable.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run credentialed Object Store diagnose-only smoke</name>
  <files>.planning/phases/15-paper-trading-order-flow/15-UAT.md, .planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md, docs/testing.md, docs/paper_trading_order_flow.md</files>
  <action>
    Load the local ignored QuantConnect env vars and run the new diagnose-only
    smoke. If `/object/set` succeeds, proceed to the full fallback smoke. If
    it still returns `Organization not found` or a permission-like error,
    update docs and summary with the precise external blocker and stop before
    Phase 16 planning.
  </action>
  <what-built>
    A reusable operator-safe diagnostic that separates QuantConnect Object
    Store account/permission failure from live algorithm delivery failure.
  </what-built>
  <how-to-verify>
    1. Confirm the diagnose-only run does not compile, deploy, send commands, or poll orders.
    2. Confirm output includes organization id source, object key, object set status, properties status, cleanup status, and final blocker status.
    3. Confirm no secret values are printed or committed.
    4. If the blocker persists, confirm docs tell the operator to unlock Object Store permissions/paid org before rerunning the full smoke.
  </how-to-verify>
  <resume-signal>
    Continue automatically through safe read/write diagnostics. Stop for any
    payment, subscription, permission change, or UI action that only the
    operator can complete.
  </resume-signal>
  <verify>
    <automated>python -m pytest tests/test_qc_api.py tests/test_lean_command_flow.py tests/test_paper_order_flow_e2e.py tests/test_paper_order_flow.py tests/test_sync.py -q</automated>
  </verify>
  <done>
    Phase 15 records either `passed_external_object_store_write_preflight` and
    continues to full fallback smoke, or
    `blocked_external_object_store_permission_or_paid_tier_required`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local diagnose-only script to QuantConnect Object Store API | Writes/deletes one namespaced probe object without deploying an algorithm. |
| Operator subscription/organization permissions | May require payment or admin action outside code. |
| Verification docs to future operators/AIs | Must distinguish organization permission failure from algorithm delivery failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09-01 | Repudiation | Object Store blocker | mitigate | Record sanitized endpoint status, organization id source, object key, and final classification. |
| T-15-09-02 | Information Disclosure | Diagnostic output | mitigate | Keep redaction filters and do not print credentials, auth headers, or raw secret-bearing env values. |
| T-15-09-03 | Denial of Service | Paper nodes | mitigate | Fail before compile/deploy when Object Store write cannot succeed. |
| T-15-09-04 | Elevation of Privilege | Operator remediation | mitigate | Do not make payment, subscription, or permission changes automatically. |
</threat_model>

<verification>
Primary local verification:

`python -m pytest tests/test_qc_api.py tests/test_lean_command_flow.py tests/test_paper_order_flow_e2e.py tests/test_paper_order_flow.py tests/test_sync.py -q`

External diagnose-only verification requires ignored local QuantConnect env
vars. It must not deploy or claim algorithm receipt.
</verification>

<success_criteria>
- Object Store preflight happens before Paper deploy.
- Diagnose-only mode records Object Store account/write/properties/delete status without compile/deploy.
- Persistent `Organization not found` is documented as permission/paid-tier blocker, not algorithm failure.
- No secret leakage, fake orders, fake fills, or real-money paths are introduced.
</success_criteria>

<output>
Create `.planning/phases/15-paper-trading-order-flow/15-09-SUMMARY.md` when done.
</output>
