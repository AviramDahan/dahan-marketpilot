---
phase: 15-paper-trading-order-flow
plan: 04
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - marketpilot/paper_order_flow.py
  - tests/test_paper_order_flow.py
autonomous: true
requirements:
  - FT-01
  - FT-02
  - FT-03
  - FT-04
must_haves:
  truths:
    - "The system polls QuantConnect live orders through QCApiClient and maps order tags back to signal IDs."
    - "Authoritative QuantConnect order statuses, partial fills, fill quantities, prices, and rejection reasons are mirrored into append-only audit JSONL."
    - "The local audit trace can query a signal-to-order-to-fill chain without becoming portfolio authority."
    - "Unknown live order payload shapes fail visible and preserve raw evidence needed for manual fixture refinement."
  artifacts:
    - path: "marketpilot/paper_order_flow.py"
      provides: "Order/fill polling, status mapping, audit append, and trace query helpers"
      exports: ["poll_quantconnect_order_updates", "read_signal_order_fill_trace"]
    - path: "tests/test_paper_order_flow.py"
      provides: "Offline tests for live-orders parsing, partial fill tracking, rejection reasons, audit records, and trace queries"
      contains: "test_fill_poll_appends_audit_record"
  key_links:
    - from: "marketpilot/paper_order_flow.py"
      to: "marketpilot/qc_api.py"
      via: "QCApiClient.read_live_orders_page or read_live_orders"
      pattern: "read_live_orders"
    - from: "marketpilot/paper_order_flow.py"
      to: "marketpilot/audit_journal.py"
      via: "AppendOnlyJsonlAuditJournal"
      pattern: "paper_order_observed|paper_fill_observed"
    - from: "marketpilot/paper_order_flow.py"
      to: "marketpilot/order_lifecycle.py"
      via: "OrderLifecycleState mapping"
      pattern: "PARTIALLY_FILLED|REJECTED|FILLED"
---

<objective>
Mirror authoritative QuantConnect live order and fill status into append-only audit records that preserve signal-to-order-to-fill traceability.

Purpose: FT-01 through FT-04 require order/fill tracking from QuantConnect, not local inference. Local audit JSONL must explain the chain while preserving QuantConnect authority per D-02, D-06, D-07, D-08, D-10, and D-12.
Output: Live-order polling and parsing helpers, audit append logic, trace query helpers, and offline tests for fills, partial fills, rejections, and traceability.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-paper-trading-order-flow/15-CONTEXT.md
@.planning/phases/15-paper-trading-order-flow/15-RESEARCH.md
@.planning/phases/15-paper-trading-order-flow/15-PATTERNS.md
@.planning/phases/15-paper-trading-order-flow/15-01-SUMMARY.md
@.planning/phases/15-paper-trading-order-flow/15-02-SUMMARY.md
@marketpilot/qc_api.py
@marketpilot/order_lifecycle.py
@marketpilot/audit_journal.py
@marketpilot/quantconnect_paper.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add live order parsing and lifecycle mapping tests</name>
  <files>marketpilot/paper_order_flow.py, tests/test_paper_order_flow.py</files>
  <behavior>
    - Parse QuantConnect live order payloads with `tag`, `status`, `quantity`, filled quantity, remaining quantity, average fill price, submitted time, last fill time, and rejection reason when present.
    - Recover `signal_id` and `idempotency_key` from the MarketPilot order tag created in Plan 15-02 per D-06.
    - Map raw QC statuses to local lifecycle states while preserving the raw status string for audit review per D-07.
    - Preserve partial fills with `filled_quantity` less than original quantity and rejection reasons for rejected orders per FT-03.
  </behavior>
  <action>Extend `tests/test_paper_order_flow.py` with fixture-like dictionaries for submitted, partially filled, filled, rejected, canceled, and unknown status cases. Implement parsing helpers in `marketpilot/paper_order_flow.py` that consume the typed order records or raw page mapping from Plan 15-01. Do not infer fills when QuantConnect data is absent; return explicit parse warnings instead per D-02 and D-07.</action>
  <verify>
    <automated>pytest tests/test_paper_order_flow.py -q</automated>
  </verify>
  <done>Tests prove partial fills, filled orders, rejections, canceled orders, unknown statuses, raw status preservation, and tag-to-signal mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append audit records and expose trace queries</name>
  <files>marketpilot/paper_order_flow.py, tests/test_paper_order_flow.py</files>
  <behavior>
    - `poll_quantconnect_order_updates()` calls the mocked `QCApiClient` live orders wrapper and appends one audit record per observed authoritative order update.
    - Fill observations append `paper_fill_observed` records with QC order id, signal id, idempotency key, status, filled quantity, remaining quantity, fill price, and observed timestamp per FT-02 and FT-04.
    - Rejections append `paper_order_rejected` records with rejection reason and raw status per FT-03.
    - `read_signal_order_fill_trace()` reads existing audit records and returns the ordered chain for a signal id or idempotency key without changing any state per D-02.
  </behavior>
  <action>Implement `poll_quantconnect_order_updates()` and `read_signal_order_fill_trace()` in `marketpilot/paper_order_flow.py`. Use `AppendOnlyJsonlAuditJournal` and UTC timestamps. Include `source_authority="quantconnect"`, `local_authority=false`, `paper_trading_only=true`, `correlation_id`, `signal_id`, and `idempotency_key` in each relevant record. Tests must use `tmp_path` for audit files and a mocked client; do not write repo `data/` and do not make external calls per D-10 and D-11.</action>
  <verify>
    <automated>pytest tests/test_paper_order_flow.py -q</automated>
  </verify>
  <done>Polling mocked QC orders appends append-only audit records for submitted, partial, filled, rejected, and unknown statuses; trace queries return command, order, and fill/rejection records in timestamp order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QuantConnect live orders response to parser | External order/fill data enters local code. |
| parser to audit JSONL | Parsed authoritative evidence is persisted as a local mirror. |
| audit JSONL to trace query | Local file records are read for evidence display and verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Tampering | live orders parser | mitigate | Preserve raw QC status and raw payload summary; fail visible for unknown shapes per D-07. |
| T-15-04-02 | Repudiation | signal-order-fill chain | mitigate | Carry correlation id, signal id, idempotency key, QC order id, and timestamps into audit records per FT-04. |
| T-15-04-03 | Information Disclosure | audit records | mitigate | Sanitize credential-like fields and never store secrets per D-08. |
| T-15-04-04 | Tampering | local mirror authority | mitigate | Mark records as `source_authority=quantconnect` and `local_authority=false`; never update authoritative state from JSONL per D-02. |
| T-15-04-05 | Denial of Service | audit file growth/corruption | accept | Phase 15 reads append-only evidence only; corrupt records fail visible in tests and do not submit orders. |
| T-15-04-SC | Tampering | package installs | accept | No package installation occurs; use existing audit and pytest patterns per D-12. |
</threat_model>

<verification>
Run `pytest tests/test_paper_order_flow.py -q`. Run `rg "source_authority.*quantconnect|local_authority" marketpilot/paper_order_flow.py tests/test_paper_order_flow.py` and verify audit records explicitly label authority.
</verification>

<success_criteria>
- QC live orders are parsed into traceable order/fill/rejection audit evidence.
- Partial fills and rejections preserve quantities, prices, raw status, and reasons.
- Trace queries can reconstruct signal-to-order-to-fill or signal-to-order-to-rejection chains from JSONL without becoming authoritative portfolio state.
</success_criteria>

<output>
Create `.planning/phases/15-paper-trading-order-flow/15-04-SUMMARY.md` when done.
</output>
