---
phase: 15-paper-trading-order-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketpilot/qc_api.py
  - marketpilot/quantconnect_paper.py
  - tests/test_qc_api.py
  - tests/fixtures/qc_api/live_command_success.json
  - tests/fixtures/qc_api/live_orders_read_success.json
autonomous: true
requirements:
  - PTD-01
  - PTD-02
  - PTD-03
  - FT-01
must_haves:
  truths:
    - "QCApiClient can construct a hardcoded QuantConnect paper deployment payload."
    - "QCApiClient can deliver MarketPilot signal commands through the QuantConnect Commands API."
    - "QCApiClient can poll the official QuantConnect live orders endpoint without using portfolio sync as an orders substitute."
    - "Paper stop and liquidate wrappers remain safety-gated and available."
  artifacts:
    - path: "marketpilot/qc_api.py"
      provides: "Paper-gated QuantConnect API wrappers for live create, commands create, live orders read, stop, and liquidate"
      contains: "live/commands/create"
    - path: "marketpilot/quantconnect_paper.py"
      provides: "Typed order/fill fields needed by downstream audit mapping"
      contains: "QuantConnectPaperOrder"
    - path: "tests/test_qc_api.py"
      provides: "Offline wrapper and safety tests"
      contains: "test_create_live_command_payload"
  key_links:
    - from: "marketpilot/qc_api.py"
      to: "tests/test_qc_api.py"
      via: "QCApiClient wrapper calls"
      pattern: "create_live_command|read_live_orders_page"
    - from: "marketpilot/qc_api.py"
      to: "marketpilot/quantconnect_paper.py"
      via: "typed QuantConnectPaperOrder parsing"
      pattern: "QuantConnectPaperOrder"
---

<objective>
Add the QuantConnect API boundary required for paper deployment, command delivery, paper stop/liquidate controls, and authoritative order polling.

Purpose: Phase 15 cannot safely deliver or track paper orders until the client uses the official command and live-orders endpoints and keeps every mutation endpoint paper-gated per D-01, D-03, D-07, and D-08.
Output: Updated API client wrappers, typed paper order fields, fixtures, and offline tests.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-paper-trading-order-flow/15-CONTEXT.md
@.planning/phases/15-paper-trading-order-flow/15-RESEARCH.md
@.planning/phases/15-paper-trading-order-flow/15-PATTERNS.md
@.planning/phases/13-qc-api-client-and-safety-foundation/13-02-SUMMARY.md
@.planning/phases/13-qc-api-client-and-safety-foundation/13-03-SUMMARY.md
@.planning/phases/13-qc-api-client-and-safety-foundation/13-04-SUMMARY.md
@marketpilot/qc_api.py
@marketpilot/quantconnect_paper.py
@tests/test_qc_api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add API wrapper tests and fixtures</name>
  <files>tests/test_qc_api.py, tests/fixtures/qc_api/live_command_success.json, tests/fixtures/qc_api/live_orders_read_success.json</files>
  <behavior>
    - Test that `_PAPER_GATED_ENDPOINTS` includes `live/commands/create` and `live/orders/read` while `PAPER_TRADING_ONLY` remains required for mutation endpoints per D-01 and D-03.
    - Test `create_live_algorithm()` sends `projectId`, `compileId`, `nodeId`, `versionId`, `brokerage.id=QuantConnectBrokerage`, `brokerage.environment=live-paper`, and explicit `dataProviders`.
    - Test `create_live_command()` sends `projectId` and a nested `command` mapping without direct order-command fields that bypass LEAN per D-03 and D-06.
    - Test `read_live_orders_page()` calls `live/orders/read` with `projectId`, `algorithmId`, `start`, and `end`, and preserves raw status, fill quantities, order tag, and rejection reason fields needed for FT-01 and FT-03.
  </behavior>
  <action>Extend `tests/test_qc_api.py` and add the two JSON fixtures above. Keep all fixture credentials fake and non-secret per D-08. Do not contact QuantConnect; patch `_make_request` as existing tests do. Update existing `read_live_orders` expectations so the test fails while the method still calls `live/read`, because FT-01 requires the official live orders endpoint per D-07.</action>
  <verify>
    <automated>pytest tests/test_qc_api.py -q</automated>
  </verify>
  <done>Tests assert the intended endpoint names, payload shapes, paper-only fields, and order parser fields; the tests fail before implementation and pass after Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement paper command and live orders wrappers</name>
  <files>marketpilot/qc_api.py, marketpilot/quantconnect_paper.py, tests/test_qc_api.py</files>
  <behavior>
    - `QCApiClient.create_live_algorithm()` hardcodes QuantConnect paper brokerage configuration and rejects non-paper payload flexibility per D-01.
    - `QCApiClient.create_live_command()` calls `live/commands/create` and returns a boolean or response object that does not imply order success per D-03 and D-07.
    - `QCApiClient.read_live_orders_page()` calls `live/orders/read`; `read_live_orders()` may wrap it but must not call `live/read`.
    - `QuantConnectPaperOrder` preserves idempotency key, signal id, raw status, filled quantity, remaining quantity, average fill price, tag, rejection reason, and raw payload where present.
  </behavior>
  <action>Update `_PAPER_GATED_ENDPOINTS` in `marketpilot/qc_api.py` to include `live/commands/create` and `live/orders/read` where the safety gate requires `PAPER_TRADING_ONLY`. Keep `_ALLOWED_ENDPOINTS` for read-only endpoints only. Change `create_live_algorithm()` to require keyword-only `project_id`, `compile_id`, `node_id`, `version_id`, and `data_providers`, and to build only the QuantConnect paper brokerage payload per D-01. Add `create_live_command()` and `read_live_orders_page()`. Update `read_live_orders()` to parse the official orders response through `read_live_orders_page()` and typed `QuantConnectPaperOrder` records. Preserve stop/liquidate wrappers and tests for PTD-03. Do not add package dependencies per D-12.</action>
  <verify>
    <automated>pytest tests/test_qc_api.py -q</automated>
  </verify>
  <done>`pytest tests/test_qc_api.py -q` passes; no QuantConnect URL is constructed outside `QCApiClient`; command API success is represented as command-delivery success only, not as order/fill success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local config to QCApiClient | Non-secret ids and paper deployment inputs enter the API wrapper. |
| QCApiClient to QuantConnect Cloud | Authenticated API requests leave the process. |
| QuantConnect Cloud to parser | Live orders response is untrusted external data until parsed and typed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Elevation of Privilege | `create_live_algorithm` | mitigate | Hardcode QuantConnect paper brokerage and `live-paper`; reject any configurable real broker path per D-01. |
| T-15-01-02 | Tampering | endpoint allowlist | mitigate | Add only official endpoints needed for PTD/FT scope and keep paper mutations behind `PAPER_TRADING_ONLY`. |
| T-15-01-03 | Information Disclosure | tests and fixtures | mitigate | Use fake fixture values and existing credential redaction; no secrets in docs, tests, errors, or logs per D-08. |
| T-15-01-04 | Repudiation | command API result semantics | mitigate | Wrapper must not mark orders/fills complete from command success; authoritative status comes from `/live/orders/read` per D-07. |
| T-15-01-SC | Tampering | package installs | accept | No npm, pip, or cargo install occurs; Phase 15 research package audit says no new packages. |
</threat_model>

<verification>
Run `pytest tests/test_qc_api.py -q`. Also run `rg "quantconnect\\.com/api" marketpilot -g "*.py"` and verify only `marketpilot/qc_api.py` contains the API base URL.
</verification>

<success_criteria>
- API client has paper-gated wrappers for deploy, commands, live orders, stop, and liquidate.
- `/live/orders/read` is used for order polling, not `live/read`.
- Tests prove no external call is made and no real-money endpoint or credential path is introduced.
</success_criteria>

<output>
Create `.planning/phases/15-paper-trading-order-flow/15-01-SUMMARY.md` when done.
</output>
