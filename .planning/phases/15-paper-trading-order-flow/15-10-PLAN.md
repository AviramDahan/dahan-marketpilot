---
phase: 15-paper-trading-order-flow
plan: "10"
type: execute
wave: 9
depends_on:
  - "15-09"
gap_closure: true
files_modified:
  - marketpilot/qc_api.py
  - scripts/qc_object_store_signal_smoke.py
  - tests/test_qc_api.py
  - docs/paper_trading_order_flow.md
  - docs/testing.md
  - .planning/phases/15-paper-trading-order-flow/15-UAT.md
  - .planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md
autonomous: false
requirements:
  - PTD-02
  - PTD-05
  - FT-01
  - FT-02
  - FT-03
  - FT-04
  - SAFE-05
user_setup:
  - service: quantconnect
    why: "Credentialed Paper-only smoke against the existing QC project/node"
    env_vars:
      - name: QUANTCONNECT_USER_ID
        source: "Ignored local .secrets file"
      - name: QUANTCONNECT_API_TOKEN
        source: "Ignored local .secrets file"
      - name: QC_PROJECT_ID
        source: "Ignored local .secrets file"
      - name: QC_NODE_ID
        source: "Ignored local .secrets file"
      - name: QC_VERSION_ID
        source: "Ignored local .secrets file"
must_haves:
  truths:
    - "Phase 15-09 externally verified Object Store writes after fixing multipart uploads."
    - "The full Object Store fallback smoke deployed successfully but observed 0 live logs, 0 tagged orders, and no receipt marker."
    - "Official QuantConnect `/live/logs/read` uses `startLine`, `endLine`, and optional `deploymentLogs`, not `start`/`end`."
    - "A log visibility bug can make a healthy receipt look unobserved, so log pagination must be corrected before changing LEAN receipt logic."
    - "Command delivery success, Object Store write success, live logs, and live orders are separate evidence classes."
    - "No real-money brokerage, broker REST order bypass, dashboard order entry, Telegram authority, unsupported asset class, leverage, margin, short selling, options, futures, crypto, or Forex path may be added."
  artifacts:
    - path: "marketpilot/qc_api.py"
      provides: "QuantConnect API wrappers, including live logs"
      contains: "read_live_logs"
    - path: "scripts/qc_object_store_signal_smoke.py"
      provides: "External Object Store fallback smoke runner"
      contains: "receipt_observed"
    - path: ".planning/phases/15-paper-trading-order-flow/15-09-SUMMARY.md"
      provides: "Current Object Store write pass and no-receipt evidence"
      contains: "object_store_written_no_algorithm_receipt_observed"
research_anchors:
  - "https://www.quantconnect.com/docs/v2/cloud-platform/api-reference/live-management/read-live-algorithm/logs"
---

<objective>
Close the Phase 15-09 observability gap by correcting QuantConnect live-log API
pagination and rerunning the safest Object Store fallback smoke. The goal is to
distinguish true algorithm non-receipt from a local `/live/logs/read` request
shape bug before modifying LEAN runtime behavior.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-paper-trading-order-flow/15-CONTEXT.md
@.planning/phases/15-paper-trading-order-flow/15-UAT.md
@.planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md
@.planning/phases/15-paper-trading-order-flow/15-09-SUMMARY.md
@docs/paper_trading_order_flow.md
@docs/testing.md
@marketpilot/qc_api.py
@scripts/qc_object_store_signal_smoke.py
@tests/test_qc_api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct live-log request shape and smoke log extraction</name>
  <files>marketpilot/qc_api.py, scripts/qc_object_store_signal_smoke.py, tests/test_qc_api.py</files>
  <behavior>
    - `QCApiClient.read_live_logs()` must send `startLine` and `endLine` per
      the official QuantConnect API contract.
    - The wrapper should optionally include `deploymentLogs=true` so smoke
      diagnostics can isolate the current deployment.
    - The Object Store smoke must read a sufficient log window without changing
      order authority or interpreting command/API success as execution.
    - Regression tests must fail against the old `start`/`end` payload.
  </behavior>
  <action>
    Update the live logs wrapper and its tests. Adjust the Object Store smoke
    to call the corrected wrapper with deployment-scoped logs and a wider log
    window. Keep all output sanitized.
  </action>
  <verify>
    <automated>python -m pytest tests/test_qc_api.py -q</automated>
    <manual>python scripts/qc_object_store_signal_smoke.py --dry-run --skip-deploy</manual>
  </verify>
  <done>
    The local API wrapper matches the official live logs request model and the
    smoke can observe deployment logs when QuantConnect returns them.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rerun credentialed Object Store fallback smoke</name>
  <files>.planning/phases/15-paper-trading-order-flow/15-UAT.md, .planning/phases/15-paper-trading-order-flow/15-VERIFICATION.md, .planning/phases/15-paper-trading-order-flow/15-10-SUMMARY.md, docs/paper_trading_order_flow.md, docs/testing.md</files>
  <action>
    Load ignored local QuantConnect env vars and run the corrected full Object
    Store fallback smoke. Stop any temporary Paper deployment afterward. Record
    whether deployment-scoped logs now show initialize/receipt/rejection
    markers, and whether `/live/orders/read` returns tagged orders.
  </action>
  <what-built>
    A corrected live-log evidence path for Paper-only external delivery smokes.
  </what-built>
  <how-to-verify>
    1. Confirm Object Store preflight still returns `object_store_write_available`.
    2. Confirm compile and deploy still complete only in Paper mode.
    3. Confirm live logs are requested with `startLine`, `endLine`, and `deploymentLogs`.
    4. If receipt/order is observed, record the sanitized evidence and mark the matching requirements externally verified.
    5. If logs are still empty, record the blocker as runtime/deployment log visibility or scheduled Object Store read semantics, not API request shape.
    6. Confirm the temporary deployment is stopped and no secrets are printed or committed.
  </how-to-verify>
  <verify>
    <automated>python -m pytest tests/test_qc_api.py tests/test_lean_command_flow.py tests/test_paper_order_flow_e2e.py tests/test_paper_order_flow.py tests/test_sync.py -q</automated>
    <manual>Run the project secret-scan pattern for locally known operator secrets across .planning, docs, scripts, tests, marketpilot, and lean.</manual>
  </verify>
  <done>
    Phase 15 has either external receipt/order evidence or a narrower
    post-log-fix blocker documented for the next gap.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local API wrapper to QuantConnect `/live/logs/read` | Read-only diagnostic evidence path. |
| Object Store smoke to Paper deployment | Paper-only external algorithm launch and polling. |
| Verification docs to future operators/AIs | Must distinguish API observability from execution authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10-01 | Repudiation | Live log evidence | mitigate | Use official `startLine`/`endLine` fields and record sanitized request-shape evidence in tests/docs. |
| T-15-10-02 | Information Disclosure | Smoke output | mitigate | Preserve redaction and never print credentials, auth headers, or raw secret env values. |
| T-15-10-03 | Tampering | Project `main.py` injection | mitigate | Restore original project file after smoke and document restore success. |
| T-15-10-04 | Elevation of Privilege | Paper deployment | mitigate | Keep brokerage hardcoded to QuantConnect live-paper and stop temporary deployment after the smoke. |
</threat_model>

<verification>
Primary local verification:

`python -m pytest tests/test_qc_api.py tests/test_lean_command_flow.py tests/test_paper_order_flow_e2e.py tests/test_paper_order_flow.py tests/test_sync.py -q`

External verification uses the ignored local QuantConnect env vars and must
remain simulated Paper Trading only.
</verification>

<success_criteria>
- `/live/logs/read` wrapper matches official `startLine`/`endLine` payload.
- Object Store smoke requests deployment-scoped logs with the corrected wrapper.
- External smoke evidence is updated and sanitized.
- Temporary Paper deployment is stopped.
- No secret leakage, fake orders, fake fills, or real-money paths are introduced.
</success_criteria>

<output>
Create `.planning/phases/15-paper-trading-order-flow/15-10-SUMMARY.md` when done.
</output>
