---
phase: 01-foundation-and-safety
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - pyproject.toml
  - requirements.txt
  - requirements-dev.txt
  - .env.example
  - config/strategy.yaml
  - config/risk.yaml
  - config/notifications.yaml
  - config/dashboard.yaml
  - config/environments/backtest.yaml
  - config/environments/shadow.yaml
  - config/environments/paper.yaml
  - marketpilot/__init__.py
  - marketpilot/constants.py
  - marketpilot/safety.py
  - marketpilot/configuration.py
  - marketpilot/fx.py
  - tests/test_safety.py
  - tests/test_configuration.py
autonomous: true
requirements:
  - SAF-01
  - SAF-02
  - SAF-05
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
user_setup: []
must_haves:
  truths:
    - "D-04: Only Phase 1-needed safety/config/test/docs package files are created; broad placeholder scaffolding is avoided."
    - "D-05: Safety, configuration, and FX seed logic live in the shared `marketpilot/` package."
    - "D-07: `PAPER_TRADING_ONLY` is defined centrally as true and validation rejects attempts to disable it."
    - "D-08: Unsafe trading, broker, leverage, shorting, options, futures, crypto, Forex, or manual order controls fail configuration validation."
    - "D-09: Unsafe configuration failures are explicit, testable, and safe to show without leaking secrets."
    - "D-10: Offline unit tests prove safety validation and FX seed behavior without external credentials or internet."
    - "D-12: External/manual verification boundaries never require credentials to be stored in the repository or chat."
  artifacts:
    - pyproject.toml
    - .env.example
    - config/strategy.yaml
    - config/risk.yaml
    - config/notifications.yaml
    - config/dashboard.yaml
    - config/environments/backtest.yaml
    - config/environments/shadow.yaml
    - config/environments/paper.yaml
    - marketpilot/constants.py
    - marketpilot/safety.py
    - marketpilot/configuration.py
    - marketpilot/fx.py
    - tests/test_safety.py
    - tests/test_configuration.py
  key_links:
    - "tests/test_safety.py imports the central paper-only guard and verifies unsafe config rejection."
    - "tests/test_configuration.py loads config fixtures and verifies FX calculation and stale-rate metadata handling."
---

<objective>
Implement the shared package foundation for paper-only safety, typed configuration, FX seed calculation, dependency metadata, and offline safety/config tests.

Purpose: This plan creates the enforceable safety gate that later strategy, dashboard, and QuantConnect code must consume.
Output: Python project metadata, config files, shared `marketpilot/` modules, and deterministic pytest coverage for safety/config behavior.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-safety/01-CONTEXT.md
@.planning/phases/01-foundation-and-safety/01-RESEARCH.md
@docs/configuration.md
@docs/safety.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python project metadata and dependencies</name>
  <files>pyproject.toml, requirements.txt, requirements-dev.txt</files>
  <read_first>.planning/phases/01-foundation-and-safety/01-RESEARCH.md</read_first>
  <action>Implement D-04 and D-05 by creating only the Phase 1 shared package/test metadata needed now. Create `pyproject.toml` with project metadata for `dahan-marketpilot`, Python version support, pytest configuration, and package discovery for `marketpilot`. Add runtime dependencies only needed for Phase 1, such as `PyYAML`, and keep heavy future dependencies out unless used. Add dev dependencies including `pytest`. Create `requirements.txt` and `requirements-dev.txt` consistent with `pyproject.toml` so users can install without publishing the package.</action>
  <verify>python -m pytest --version</verify>
  <acceptance_criteria>
    - `pyproject.toml` configures pytest `testpaths = ["tests"]`.
    - Runtime requirements do not include broker, Telegram, QuantConnect credentials, or trading libraries beyond Phase 1 needs.
    - Dev requirements include pytest.
  </acceptance_criteria>
  <done>Python project metadata supports local offline tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add safe configuration files and environment example</name>
  <files>.env.example, config/strategy.yaml, config/risk.yaml, config/notifications.yaml, config/dashboard.yaml, config/environments/backtest.yaml, config/environments/shadow.yaml, config/environments/paper.yaml</files>
  <read_first>docs/Dahan-MarketPilot-Master-Spec.md, docs/configuration.md</read_first>
  <action>Implement D-08 and D-12. Create config files with only safe Phase 1 keys. Include `paper_trading_only: true`, `starting_budget_nis`, `initial_usd_ils_rate`, `starting_cash_usd`, `trading_currency: USD`, `display_currency: NIS`, `fx_rate_timestamp`, and `fx_rate_source`. Include safe notification/dashboard placeholders that do not contain secrets. Create `.env.example` with variable names only, no real values, and comments saying secrets must be stored in approved secret stores.</action>
  <verify>Select-String -Path config/environments/paper.yaml -Pattern "paper_trading_only: true"; Select-String -Path .env.example -Pattern "DO_NOT_PASTE_SECRETS_HERE"</verify>
  <acceptance_criteria>
    - Config files contain no real credentials.
    - Config files do not contain any real broker settings or live-money flags.
    - FX seed keys from the master spec are present.
  </acceptance_criteria>
  <done>Safe Phase 1 configuration exists without secrets or live trading paths.</done>
</task>

<task type="auto">
  <name>Task 3: Implement central safety and configuration modules</name>
  <files>marketpilot/__init__.py, marketpilot/constants.py, marketpilot/safety.py, marketpilot/configuration.py, marketpilot/fx.py</files>
  <read_first>.planning/phases/01-foundation-and-safety/01-CONTEXT.md, docs/configuration.md, docs/safety.md</read_first>
  <action>Implement D-07, D-08, and D-09. Create shared package modules. `marketpilot/constants.py` must define `PAPER_TRADING_ONLY = True` and the disclaimer string. `marketpilot/safety.py` must expose validation that rejects paper-only false, real broker keys, live-money keys, leverage/margin/short/options/futures/crypto/forex flags, and manual dashboard order controls. `marketpilot/fx.py` must calculate `starting_cash_usd = starting_budget_nis / initial_usd_ils_rate` with validation for positive numeric values. `marketpilot/configuration.py` must load YAML with `yaml.safe_load`, validate known sections, call safety validation, and return typed dataclasses or clear structured objects.</action>
  <verify>python -m pytest tests/test_safety.py tests/test_configuration.py</verify>
  <acceptance_criteria>
    - `yaml.safe_load` is used rather than unsafe YAML loading.
    - Validation fails closed for unsafe config keys.
    - Configuration errors do not print secret values.
    - FX calculation is deterministic.
  </acceptance_criteria>
  <done>Shared safety/config/fx foundation works and is covered by tests.</done>
</task>

<task type="auto">
  <name>Task 4: Add offline safety and configuration tests</name>
  <files>tests/test_safety.py, tests/test_configuration.py</files>
  <read_first>marketpilot/constants.py, marketpilot/safety.py, marketpilot/configuration.py, marketpilot/fx.py</read_first>
  <action>Implement D-10 and D-12. Create pytest tests that verify: central `PAPER_TRADING_ONLY` is true; config with paper-only false fails; real broker/live-money/leverage/margin/short/options/futures/crypto/forex/manual-order keys fail; safe config passes; FX seed calculation is correct; invalid FX values fail; secret-like values are not included in exception string representations. Tests must use inline fixtures or temporary files and must not require network, QuantConnect, Telegram, Render, or broker credentials.</action>
  <verify>python -m pytest tests/test_safety.py tests/test_configuration.py</verify>
  <acceptance_criteria>
    - Tests cover every unsafe class listed in SAF-02.
    - Tests can run offline from a clean checkout after dependency install.
    - No test sends Telegram, calls QuantConnect, deploys Render, or touches real market data.
  </acceptance_criteria>
  <done>Safety and configuration gates are enforced by deterministic tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_safety.py tests/test_configuration.py` passes.
- [ ] `Select-String -Path marketpilot/constants.py -Pattern "PAPER_TRADING_ONLY = True"` finds the central guard.
- [ ] `Select-String -Path marketpilot/configuration.py -Pattern "safe_load"` confirms safe YAML loading.
- [ ] No credentials, fake results, or live-money config paths were introduced.
</verification>

<success_criteria>

- Shared package foundation exists.
- Safety config validation fails closed.
- FX seed calculation is implemented and tested.
- Offline tests prove Phase 1 safety/config requirements.

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-safety/01-02-SUMMARY.md`.
</output>
