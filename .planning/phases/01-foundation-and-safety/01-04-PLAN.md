---
phase: 01-foundation-and-safety
plan: "04"
type: execute
wave: 4
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - lean/main.py
  - lean/config.json
  - dashboard/app.py
  - dashboard/__init__.py
  - dashboard/safety_view.py
  - dashboard/models.py
  - tests/test_dashboard.py
  - tests/test_lean_static_safety.py
  - docs/setup.md
  - docs/testing.md
autonomous: true
requirements:
  - SAF-03
  - SAF-05
  - SAF-06
  - CFG-01
  - QC-01
user_setup:
  - "To run the full LEAN compile check, the user may need Docker, Lean CLI, `lean login`, `lean init`, and QuantConnect organization access. Do not store credentials in the repository."
must_haves:
  truths:
    - "D-11: The minimal QuantConnect algorithm compile path is required and contains no order submission calls."
    - "D-13: The minimal QuantConnect algorithm may subscribe only to SPY and QQQ benchmark symbols in Phase 1."
    - "D-14: The minimal QuantConnect algorithm avoids dynamic universe selection, production indicators, strategy signals, sizing, orders, Paper orders, broker setup, and live trading behavior."
    - "D-15: Empty hooks are allowed only when needed for compile/lifecycle clarity and contain no placeholder trading logic."
    - "D-16: The Streamlit dashboard shell is read-only, displays the mandatory disclaimer, and clearly states no live data is connected."
    - "D-17: The Streamlit dashboard shell displays no mock portfolio, fake P&L, fake holdings, fake Backtest metrics, or demo trading cards."
    - "D-18: The dashboard shell structure reinforces read-only and paper-only behavior."
  artifacts:
    - lean/main.py
    - lean/config.json
    - dashboard/app.py
    - dashboard/safety_view.py
    - tests/test_dashboard.py
    - tests/test_lean_static_safety.py
  key_links:
    - "dashboard/app.py imports the shared disclaimer/read-only safety state rather than duplicating unsafe behavior."
    - "tests/test_lean_static_safety.py checks lean/main.py for forbidden order calls such as MarketOrder, LimitOrder, StopMarketOrder, and SetHoldings."
---

<objective>
Create minimal non-trading QuantConnect and read-only Streamlit shells with tests and documented verification.

Purpose: This plan proves Phase 1 can expose the application surfaces without starting strategy implementation or fake portfolio display.
Output: Benchmark-only LEAN algorithm shell, static safety dashboard shell, tests that enforce non-trading/read-only behavior, and setup/testing docs for LEAN compile verification.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-safety/01-CONTEXT.md
@.planning/phases/01-foundation-and-safety/01-RESEARCH.md
@marketpilot/constants.py
@marketpilot/safety.py
@marketpilot/models.py
@docs/setup.md
@docs/testing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark-only QuantConnect shell</name>
  <files>lean/main.py, lean/config.json</files>
  <read_first>.planning/phases/01-foundation-and-safety/01-CONTEXT.md, .planning/phases/01-foundation-and-safety/01-RESEARCH.md</read_first>
  <action>Implement D-11, D-13, D-14, and D-15. Create `lean/main.py` as a minimal Python `QCAlgorithm` with `Initialize` only or minimal safe lifecycle methods. It may set start/end dates and cash for compile/demo purposes and subscribe to SPY and QQQ benchmark equities. It must not contain `SetHoldings`, `MarketOrder`, `LimitOrder`, `StopMarketOrder`, `Liquidate`, dynamic universe selection, strategy signals, indicators intended for production strategy, broker setup, or Paper Trading deployment logic. Create minimal `lean/config.json` without credentials and with safe project metadata only.</action>
  <verify>python -m pytest tests/test_lean_static_safety.py</verify>
  <acceptance_criteria>
    - `lean/main.py` defines a `QCAlgorithm` subclass.
    - `lean/main.py` references SPY and QQQ only as benchmark subscriptions.
    - No forbidden order or live trading APIs appear in `lean/main.py`.
    - `lean/config.json` contains no credentials.
  </acceptance_criteria>
  <done>QuantConnect shell is compile-oriented and non-trading.</done>
</task>

<task type="auto">
  <name>Task 2: Add static read-only Streamlit dashboard shell</name>
  <files>dashboard/app.py, dashboard/__init__.py, dashboard/safety_view.py, dashboard/models.py</files>
  <read_first>marketpilot/constants.py, marketpilot/models.py, docs/safety.md</read_first>
  <action>Implement D-16, D-17, and D-18. Create a minimal Streamlit app that displays `Dahan MarketPilot`, the exact disclaimer `SIMULATED PAPER TRADING ONLY - NOT FINANCIAL ADVICE`, a read-only status, paper-only status, and "No live data connected". Do not display mock portfolio value, fake holdings, fake backtest metrics, fake P&L, order buttons, or controls that imply order submission. Keep reusable view/state helpers in `dashboard/safety_view.py` or `dashboard/models.py` only if they remain simple and testable.</action>
  <verify>python -m pytest tests/test_dashboard.py</verify>
  <acceptance_criteria>
    - Dashboard shell imports or reuses the shared disclaimer.
    - Dashboard shell contains no fake portfolio numbers or mock trading cards.
    - Dashboard shell has no buttons/forms that submit or modify orders.
  </acceptance_criteria>
  <done>Dashboard shell is static, read-only, and safety-forward.</done>
</task>

<task type="auto">
  <name>Task 3: Add static safety tests for LEAN and dashboard shells</name>
  <files>tests/test_lean_static_safety.py, tests/test_dashboard.py</files>
  <read_first>lean/main.py, dashboard/app.py, dashboard/safety_view.py</read_first>
  <action>Implement D-11, D-14, D-16, and D-17 through static tests. Create tests that statically inspect `lean/main.py` for forbidden order/live trading method names and verify only SPY/QQQ benchmark subscriptions appear. Create dashboard tests that inspect shell text/helpers for mandatory disclaimer, read-only status, paper-only status, no live data connected status, and absence of fake portfolio/P&L/order-control language. Tests must not import QuantConnect or require Streamlit server execution unless Streamlit is already installed as a dependency; prefer testing pure helpers and file text to keep offline tests deterministic.</action>
  <verify>python -m pytest tests/test_lean_static_safety.py tests/test_dashboard.py</verify>
  <acceptance_criteria>
    - Tests fail if forbidden order calls are added to `lean/main.py`.
    - Tests fail if dashboard fake portfolio data or order controls appear.
    - Tests run offline without QuantConnect credentials, Docker, Telegram, Render, or market data.
  </acceptance_criteria>
  <done>Static safety gates protect the minimal shells.</done>
</task>

<task type="auto">
  <name>Task 4: Document LEAN compile and dashboard verification</name>
  <files>docs/setup.md, docs/testing.md</files>
  <read_first>.planning/phases/01-foundation-and-safety/01-RESEARCH.md, lean/main.py, dashboard/app.py</read_first>
  <action>Update setup/testing docs with exact verification commands for offline tests and the external LEAN compile check. Document that `lean build` may require Docker and LEAN workspace setup, and that `lean login`/QuantConnect organization access must be handled outside the repository. Document that running Streamlit is local-only in Phase 1 and does not connect to QuantConnect, Render, Telegram, or brokers.</action>
  <verify>Select-String -Path docs/setup.md, docs/testing.md -Pattern "lean build", "Docker", "streamlit run", "No live data connected"</verify>
  <acceptance_criteria>
    - Docs include offline `python -m pytest` command.
    - Docs include external/manual LEAN compile command and prerequisites.
    - Docs do not ask the user to paste secrets into chat or files.
  </acceptance_criteria>
  <done>Verification docs match the shell behavior and external setup boundaries.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest` passes for all Phase 1 tests.
- [ ] Static checks confirm no forbidden order methods in `lean/main.py`.
- [ ] If available, run the documented LEAN compile command; otherwise record the missing prerequisite in `01-04-SUMMARY.md`.
- [ ] `streamlit run dashboard/app.py` is documented as local-only and not required for automated tests unless feasible.
</verification>

<success_criteria>

- Minimal QuantConnect shell exists and has no trading behavior.
- Minimal dashboard shell exists and has no fake portfolio or order controls.
- Tests protect non-trading and read-only constraints.
- Docs explain external LEAN compile requirements.

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-safety/01-04-SUMMARY.md`.
</output>
