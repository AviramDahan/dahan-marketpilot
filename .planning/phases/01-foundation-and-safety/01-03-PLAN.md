---
phase: 01-foundation-and-safety
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - marketpilot/models.py
  - tests/test_models.py
  - tests/test_project_files.py
  - docs/configuration.md
  - docs/testing.md
autonomous: true
requirements:
  - CFG-01
  - CFG-05
  - SAF-05
must_haves:
  truths:
    - "D-06: Domain models exist for Phase 1-safe concepts without implementing strategy entries, orders, fills, exits, or live trading behavior."
    - "D-10: Tests verify model validation and required project files without using external services."
    - "D-20: Documentation is updated when Phase 1 model/test behavior changes."
    - "Project file tests prevent accidental removal of core safety/licensing/disclaimer docs."
  artifacts:
    - marketpilot/models.py
    - tests/test_models.py
    - tests/test_project_files.py
  key_links:
    - "tests/test_models.py imports marketpilot.models and validates only Phase 1-safe model behavior."
    - "tests/test_project_files.py verifies that LICENSE, NOTICE, THIRD_PARTY_NOTICES.md, DISCLAIMER.md, README.md, and safety docs exist."
---

<objective>
Add Phase 1-safe domain model foundations and tests that protect required repository artifacts.

Purpose: This plan gives later phases typed vocabulary without creating trading logic prematurely.
Output: `marketpilot.models` with safe foundational dataclasses/enums and deterministic tests for models and required project files.
</objective>

<execution_context>
@C:/Users/User/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/User/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-safety/01-CONTEXT.md
@.planning/phases/01-foundation-and-safety/01-RESEARCH.md
@marketpilot/constants.py
@marketpilot/configuration.py
@docs/configuration.md
@docs/testing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 1-safe domain models</name>
  <files>marketpilot/models.py</files>
  <read_first>.planning/phases/01-foundation-and-safety/01-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Implement D-06. Create `marketpilot/models.py` with dataclasses/enums for safe foundation concepts such as `TradingMode`, `EnvironmentName`, `CurrencyCode`, `Money`, `FxSeed`, `SafetyStatus`, `ReadOnlyStatus`, and `ValidationIssue`. Do not model strategy signals, candidate scores, orders, fills, stops, targets, positions, exits, or portfolio state yet. Include validation helpers for positive money amounts, supported currencies (`USD`, `NIS`), and paper-only mode status.</action>
  <verify>python -m pytest tests/test_models.py</verify>
  <acceptance_criteria>
    - Models are typed and importable from `marketpilot.models`.
    - No class names imply production signal/order execution in Phase 1.
    - Validation raises clear exceptions without secrets.
  </acceptance_criteria>
  <done>Phase 1 model layer provides safe primitives only.</done>
</task>

<task type="auto">
  <name>Task 2: Add model tests</name>
  <files>tests/test_models.py</files>
  <read_first>marketpilot/models.py</read_first>
  <action>Implement D-10. Create tests for valid money, invalid money, supported/unsupported currencies, FX seed model behavior, paper-only/read-only status models, and validation issue formatting. Tests must be offline and deterministic.</action>
  <verify>python -m pytest tests/test_models.py</verify>
  <acceptance_criteria>
    - Tests fail if invalid money or unsupported currency is accepted.
    - Tests fail if model formatting leaks secret-like field names/values.
    - Tests do not depend on any external service.
  </acceptance_criteria>
  <done>Model behavior is covered by focused tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add required project file tests</name>
  <files>tests/test_project_files.py</files>
  <read_first>docs/AI-COLLABORATION.md, AGENTS.md, README.md, DISCLAIMER.md</read_first>
  <action>Create tests that verify required foundation files exist and contain key safety strings. Check for `SIMULATED PAPER TRADING ONLY - NOT FINANCIAL ADVICE`, `PAPER_TRADING_ONLY`, and references to QuantConnect source-of-truth and read-only Render where appropriate. These tests should protect docs from drifting away from the safety contract.</action>
  <verify>python -m pytest tests/test_project_files.py</verify>
  <acceptance_criteria>
    - Tests verify required foundation files exist.
    - Tests verify disclaimer and safety language remain visible.
    - Tests do not require external credentials or network.
  </acceptance_criteria>
  <done>Required project files are protected by tests.</done>
</task>

<task type="auto">
  <name>Task 4: Synchronize docs with model and test boundaries</name>
  <files>docs/configuration.md, docs/testing.md</files>
  <read_first>marketpilot/models.py, tests/test_models.py, tests/test_project_files.py</read_first>
  <action>Implement D-20. Update `docs/configuration.md` to mention the foundational model concepts introduced in Phase 1. Update `docs/testing.md` to list the model and project-file test suites, and to reiterate that Phase 1 does not test strategy signals, orders, portfolio state, Telegram delivery, Render deployment, or QuantConnect Paper Trading.</action>
  <verify>Select-String -Path docs/configuration.md -Pattern "FxSeed", "SafetyStatus"; Select-String -Path docs/testing.md -Pattern "test_models.py", "test_project_files.py"</verify>
  <acceptance_criteria>
    - Docs describe what models exist and what remains deferred.
    - Docs accurately list the tests added by this plan.
  </acceptance_criteria>
  <done>Documentation stays aligned with the Phase 1 model/test foundation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_models.py tests/test_project_files.py` passes.
- [ ] `python -m pytest` passes for all tests created through Plan 03.
- [ ] `Select-String -Path marketpilot/models.py -Pattern "Order", "Fill", "Position", "Signal"` returns no production trading model classes.
</verification>

<success_criteria>

- Domain model foundation exists without premature trading behavior.
- Tests protect model validation and foundation file presence.
- Documentation reflects model/test boundaries.

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-safety/01-03-SUMMARY.md`.
</output>
